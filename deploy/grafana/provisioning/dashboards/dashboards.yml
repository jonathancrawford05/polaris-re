# Grafana dashboard provider — loads JSON dashboards from the mounted folder.
apiVersion: 1

providers:
  - name: polaris-re
    orgId: 1
    folder: Polaris RE
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
