{{- if .Values.apiKeys }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "polaris-re.fullname" . }}-secrets
  labels:
    {{- include "polaris-re.labels" . | nindent 4 }}
type: Opaque
stringData:
  api-keys: {{ .Values.apiKeys | quote }}
{{- end }}
