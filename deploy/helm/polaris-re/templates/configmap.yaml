apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "polaris-re.fullname" . }}-config
  labels:
    {{- include "polaris-re.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
