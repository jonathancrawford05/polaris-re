# The `mgcv` conformance ledger

Append-only, one row per hypothesis tried (`docs/ROUTINE_MGCV_PARITY.md`). The `tier +
digest` column is not bookkeeping: without it, a row measured on local apt R and a row
measured on the pinned image read identically, and the ledger's whole purpose —
stopping session N+3 from re-running session N's dead end — depends on a later reader
being able to tell whether a "no movement" verdict was a real result or a tier-1
artefact.

**Provenance is required on every new row (ADR-193,
`docs/VERIFICATION_STANDARD.md`).** The `metric` column must name *what produced
each side* of every compared quantity, and the `verdict` column must say
`CONFIRMED (parity)` only when the two producers are independent — neither
reading the other's output, and neither having supplied the quantity to the
other. A row that cannot name two independent producers is a harness row: it
records a real check (no-tampering, or a working round trip) and its verdict
reads `CONFIRMED (harness)`.

**Every row currently in the table below predates the rule, and all of them are
harness rows.** Naming them here rather than leaving "the earlier rows" to the
reader is the point of ADR-193 — a caveat that attaches to nothing does not
travel, which is the failure these rows themselves illustrate:

- the four **2026-08-15 slice-1** rows read `**exact**` / `**agrees**` on the
  `raw`/`paraPen` path, where Polaris supplied the design and penalties that
  `mgcv` was fitted on and then read back — **ECHO** on `design_X`, `penalty_S`
  and `index_range`; only `rank` (`numpy.linalg.matrix_rank` vs `mgcv`'s own rank
  determination) is INDEPENDENT;
- the two **2026-08-16 slice-1b** rows read `**exact**` on the mgcv-native path,
  where `extract_smooth_terms` parses the R payload and is then compared against
  that same payload — **TRANSPORT** on every column. What those rows do establish
  is the R script's own internal guard (`smoothCon` vs `lpmatrix`/`m$smooth[[j]]`,
  ADR-191), which is independent *inside R*.

Neither set is basis-parity evidence. §5 of the standard carries the full audit,
including what the conformance levels (which ARE independent) prove.

| date | slice | hypothesis | the one change | metric | before | after | tier + digest | verdict |
|---|---|---|---|---|---|---|---|---|
| 2026-08-15 | 1 | `predict(gam(...), type="lpmatrix")`'s smooth-term block and `smoothCon(s(x, bs="cr"), absorb.cons=TRUE)$X` are the SAME object (PLAN §5 risk 1), not two competing Stage-A referents | added `scripts/smoothcon_lpmatrix_probe.R`, ran it against a fresh `bs="cr"` case (no exchange dependency) | `max_abs_diff_lpmatrix_vs_smoothcon_x`, `max_abs_diff_gam_smooth_S_vs_smoothcon_S` | n/a (first measurement) | **0.0 exactly**, 3 cases (k=8 default knots, k=13 default knots, k=8 supplied knots) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed below |
| 2026-08-15 | 1 | same hypothesis, re-measured on the authoritative oracle per `ROUTINE_MGCV_PARITY.md` step 2 (a tier-1 structural claim about which mgcv function calls which is exactly the "version change is different code" case the routine names) | none — same probe, dispatched via CI `workflow_dispatch` on `173d186` | same two metrics | tier-1 reading above | **0.0 exactly**, all 3 cases, identical to the tier-1 reading at every printed digit | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [31907362222](https://github.com/jonathancrawford05/polaris-re/actions/runs/31907362222) | **CONFIRMED — settled.** `smoothCon(absorb.cons=TRUE)` is Stage A's referent; recorded in `CONTINUATION_mgcv_parity_engine.md`. Required levels 1-3 of the existing suite also still agree on this run — no regression from the CI workflow edit. |
| 2026-08-15 | 1 | the R-side per-term extractor (`scripts/gam_term_extract.R`, reading `m$paraPen$S`/`$rank` and `predict(type="lpmatrix")` off the fitted object) and its Python comparator (`gam_stage_a.py`'s `extract_raw_terms`/`compare_term_extract`, reading the already-fitted `DesignExport`) agree term by term on the existing verified `raw`/`paraPen` basis — Anchor 1's "prove the harness on a known-good basis first" | wrote both scripts; ran the R extractor against `data/mgcv_exchange/synthetic` and compared to the Python side for designs `d1` (tensor only) and `d2` (tensor + factor block) | `max_abs_design_diff`, `max_abs_s_diff` (per penalty), `rank_diff`, index-range agreement, per term | n/a (first measurement) | **exact** — design diff ≤5e-16 (float round-trip noise, not a disagreement), S diff `0.0`, rank diff `(0, 0)`, index ranges agree, all 3 terms (`d1/tensor`, `d2/tensor`, `d2/factor:sex`) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed below. Caught one real bug in the R script's own harness proof: the JSON key for the factor term was the literal `"factor"` while its `label` field said `"factor:sex"` — fixed before this reading. |
| 2026-08-15 | 1 | same hypothesis, re-measured on the authoritative oracle (a structural claim about the harness's own extraction/serialization logic, not a numeric value near precision limits, but still "does this code path work correctly on this mgcv version" — earns the same tier-3 requirement as ADR-191) | none — same two scripts, dispatched via CI `workflow_dispatch` on `d005061` | same metrics, read as the CI step's own pass/fail rather than transcribed per-digit (see note) | tier-1 reading above | **agrees** — both the R extraction step and the Python comparison step completed with `conclusion: success` (the comparison step is `continue-on-error: true` and would show `failure` on any disagreement or exception; it did not) | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [31915145674](https://github.com/jonathancrawford05/polaris-re/actions/runs/31915145674), both jobs completed in 55s | **CONFIRMED — settled.** Note what this does NOT claim: read from the step conclusion, not the per-metric job-summary table (artifact `9254708641` on that run) — same honesty framing as ADR-189 amendment 2's "I read the gate's verdicts, not the per-metric values." Required levels 1-3 of the existing suite also still agree on this run. |
| 2026-08-16 | 1b | `gam_term_extract.R`'s new `smoothCon()` branch (work order §2), with its own internal consistency guard against `predict(type="lpmatrix")`/`m$smooth[[1]]`, and its Python counterpart `extract_smooth_terms` (packaging, not independent computation — ADR-192) agree on 3 isolated `bs="cr"` cases (default knots `k=8`/`k=13`, supplied knots `k=8`), same synthetic generation as `smoothcon_lpmatrix_probe.R` | wrote `extract_smooth_one` in the R script and `extract_smooth_terms`/knots-comparison in `gam_stage_a.py`; ran the R extractor and compared via `compare_term_extract` | `max_abs_design_diff`, `max_abs_s_diff`, `rank_diff`, `max_abs_knots_diff`, index-range agreement, per case | n/a (first measurement) | **exact** — all diffs `0.0`, rank diff `(0,)`, knots agree, all 3 cases (`default-knots-k8`: dim 200×7 rank 6; `default-knots-k13`: dim 400×12 rank 11; `supplied-knots-k8`: dim 200×7 rank 6) — matches ADR-191's own `bs="cr"` measurements exactly | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed below. **Caught one real bug on the first run**, exactly what Anchor 1's harness-first discipline is for: jsonlite's `auto_unbox` silently collapsed the single-penalty `rank = sm$rank` (a length-1 vector) to a bare JSON scalar, and the Python side's `for v in r_term["rank"]` raised `TypeError: 'int' object is not iterable`. The `raw` path never hit this because it always carries two penalties. Fixed with `rank = I(sm$rank)`, which forces jsonlite to keep it an array regardless of length. |
| 2026-08-16 | 1b | same hypothesis, re-measured on the authoritative oracle (a structural claim about the R script's own `stop()`-gated internal guard and the Python packaging's exception-freedom on the pinned mgcv version, the same class of claim ADR-191 and slice 1's tier-3 rows earned) | none — same two scripts, dispatched via CI `workflow_dispatch` on `7f06ebd` | R step: did `extract_smooth_one`'s internal guard (against `lpmatrix`/`m$smooth[[j]]`) `stop()` on any of the 3 cases? Python step: did `extract_smooth_terms`/`compare_term_extract` raise on the tier-3 payload? | tier-1 reading above | **agrees on both** — job 1's "Extract per-term Stage-A artefacts…" step (which runs the same `gam_term_extract.R` invocation, smoothCon branch included) completed `conclusion: success`, meaning none of the 4 `stop()` guards fired on any of the 3 smooth cases; job 2's "Compare the per-term extraction… (slice 1 / 1b)" step also completed `conclusion: success`, meaning `extract_smooth_terms`/`compare_term_extract` raised no exception reading the tier-3 payload | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [31946132947](https://github.com/jonathancrawford05/polaris-re/actions/runs/31946132947), both jobs completed in ~58s | **CONFIRMED for the R-side guard — settled**; **weaker confirmation for the Python comparison.** What this DOES claim: the R script's `stop()` guard is a genuine hard assertion (not a print), so its `success` conclusion on the pinned image is real evidence, same standing as ADR-191's own measurement on this exact check. What this does NOT claim: the per-metric diff table (`gam_term_extract_report.md`) was not read — this environment's egress policy blocks the Actions artifact blob-storage host (`*.blob.core.windows.net`), and the job summary is not exposed through the workflow-run/job API this session used, so the exact printed diffs are unread, unlike slice 1's tier-1 reading above where every digit is `0.0`. The Python comparison step's `success` conclusion is weaker evidence than the R step's — that script does not `sys.exit` on `any_smooth_disagree`, only on an exception — so it rules out a structural break but not, on its own, a printed nonzero diff. Given (a) the R-side hard guard passed at tier 3, (b) the tier-1 reading showed exact `0.0` on identical code, and (c) ADR-191 already established tier-1/tier-3 identity on this exact `bs="cr"` construction to every printed digit, a tier-3 value disagreement here would require new nondeterminism ADR-189 amendment 2's build-8 fix was meant to close — but this row records what was actually read, not what is inferred, per the routine's own discipline. |
| 2026-08-17 | 2 | `gam_basis_cr.py`'s Wood natural-cubic-spline construction (`cr_default_knots`, `cr_basis`, `absorb_sum_to_zero_constraint` — an independent Python implementation, never reading mgcv's output) equals `mgcv::smoothCon(s(x, bs="cr", k), absorb.cons=TRUE)` on `design_X`/`penalty_S`/`rank`/`knots`, for all 5 of `gam_term_extract.R`'s isolated `bs="cr"` cases (default knots k=8/k=13, supplied knots k=8, and — extending the harness's original 3 — PLAN §1's own target-formula knot vectors, `AttdAge` k=13 and `PolYear` k=6) — the epic's first INDEPENDENT Stage-A claim (ADR-193) and the first slice-2 measurement against the literal target knots rather than a stand-in | derived every formula from `mgcv`'s own R source (`smooth.construct.cr.smooth.spec`, `smoothCon`) read directly off the installed tier-1 package (`deparse()`), not guessed: default knots are `quantile(unique(x), seq(0,1,length=k))`; the identifiability constraint is `colMeans(X)` (not `colSums`) absorbed via a full QR of the constraint's transpose, confirmed `numpy.linalg.qr(mode="complete")` reproduces R's `qr()`/`qr.Q()` bit-for-bit on a single-column input; `smoothCon`'s OWN `scale.penalty=TRUE` (distinct from `gam.control(scalePenalty=)` used on the `raw` path) rescales `S` by `norm_1(S)/norm_inf(X)^2`, found by ratio when the unscaled penalty was off by a constant factor. Extended `gam_term_extract.R` to also export the covariate `x` (shared recipe, not a compared quantity) so Python can evaluate its own basis at the same points R's RNG drew; added an `x_range` parameter so the two target-knot cases draw `x` from inside `[knots[0], knots[-1]]` (extrapolation is explicitly unverified, module docstring) | `max_abs_design_diff`, `max_abs_s_diff`, `rank_diff`, `max_abs_knots_diff`, index-range agreement, per case | n/a (first measurement) | **exact** — all diffs ~1e-14 (float round-trip noise), rank diff `(0,)`, knots agree, all 5 cases (`default-knots-k8`: max X diff 8.4e-15, S diff 9.3e-15; `default-knots-k13`: max X diff 1.2e-14, S diff 1.3e-14; `supplied-knots-k8`: max X diff 7.6e-15, S diff 4.4e-15; `target-attdage-k13`: max X diff 1.5e-14, S diff 3.6e-15; `target-polyear-k6`: max X diff 1.4e-14, S diff 8.0e-15) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed below. `design_X`/`penalty_S`/`rank` are INDEPENDENT (`CR_BASIS_CLAIM`, `gam_stage_a.py`): neither `build_python_cr_term` nor `gam_term_extract.R`'s `smoothCon` branch reads the other's `X`/`S`/`rank` output, only the shared covariate `x`. **`knots` agreement is a separate recipe-consistency check, not part of `CR_BASIS_CLAIM`** (PR #201 review [P1], corrected same-day): it is a genuine independent computation only in the two default-knot cases (each side's own quantile placement); in the three supplied-knot cases neither side computes it, both relay the same hand-declared literal, which is ECHO — a single `knots` entry cannot honestly claim both, so it was removed from the claim rather than mistagged. A disagreement on `design_X`/`penalty_S`/`rank` would be a real result about the basis, not a broken round trip — this row records the first pass and it agreed, confirming PLAN §6's registered prediction ("Stage A on `cr` agrees exactly once knots match, and any disagreement is knot placement rather than the spline recursion") on the first measurement. |
| 2026-08-17 | 2 | same hypothesis, re-measured on the authoritative oracle (a genuine numeric parity claim, INDEPENDENT on every quantity — the class of claim ADR-193 exists to let this epic finally make) | none — same two producers (`gam_basis_cr.py`'s `build_python_cr_term`, `gam_term_extract.R`'s `extract_smooth_one`), dispatched via CI `workflow_dispatch` on `6e619ce`; the first dispatch's per-term comparison step wrote its report only to the job-summary file (behind the blob-storage host this environment's egress policy blocks, same limitation slice 1b's tier-3 row hit) — fixed by also `print()`-ing the same report string to stdout (`7408ecc`), then re-dispatched, so this reading is the **actual per-metric table**, not a masked step conclusion | `max_abs_design_diff`, `max_abs_s_diff`, `rank_diff`, index-range agreement, knots agreement, per case, **read directly from job-log stdout** (`get_job_logs`), not inferred from a `continue-on-error` step's `conclusion` | tier-1 reading above | **exact** — all diffs ~1e-14 (float round-trip noise, same order as tier 1, last-bit differences consistent with the different mgcv version / BLAS the routine's tier discipline predicts), rank diff `(0,)`, knots agree, all 5 cases (`default-knots-k8`: max X diff 8.327e-15, S diff 9.548e-15; `default-knots-k13`: max X diff 1.213e-14, S diff 1.243e-14; `supplied-knots-k8`: max X diff 7.605e-15, S diff 4.441e-15; `target-attdage-k13`: max X diff 1.454e-14, S diff 3.553e-15; `target-polyear-k6`: max X diff 1.379e-14, S diff 7.994e-15) | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32033738454](https://github.com/jonathancrawford05/polaris-re/actions/runs/32033738454), both jobs completed in ~56s | **CONFIRMED (parity) — settled.** `design_X`/`penalty_S`/`rank` — every quantity `CR_BASIS_CLAIM` actually declares — are `INDEPENDENT`, so this is genuine Stage-A basis parity on those three, not a harness check — the first row in this ledger entitled to that verdict. `knots` agreement is reported alongside (all 5 cases agree) but, per the same-day correction above, is a recipe-consistency check outside the claim, not a fourth parity column. Required levels 1-3 of the existing suite also still agree on this run (no regression from the workflow edit). **What this ALSO settles, as a methodology fix:** slice 1b's tier-3 row (above) could only confirm "the comparison step raised no exception," because `continue-on-error: true` makes a step's API-visible `conclusion` read `success` whether the script actually agreed or silently disagreed without exiting non-zero, and the per-metric job-summary artifact sat behind a blocked host. Printing the identical report string to stdout put the real numbers in plain job-log text, which `get_job_logs` reads without touching the blocked host — closing that limitation for every later slice's tier-3 reading, not only this one. |
| 2026-08-17 | 3 | `gam_fit.penalized_irls_general` (an independent Python IRLS implementation for an arbitrary family/link, reading only the shared recipe — `x`, `s`, `y`, `weights`, `offset`, `sp` — off `scripts/gam_family_probe.R`'s payload, never its `eta`/`coef`/`dispersion`) reproduces `mgcv::gam(family=..., weights=..., paraPen=list(X=list(S, sp=sp)))` on `eta` (all 4 cases) and `dispersion` (the 1 case mgcv estimates it, quasi-Poisson), at a fixed `sp` on a shared design, for binomial logit/cloglog with prior weights, quasi-Poisson, and Poisson with a log offset (PLAN slice 3) | wrote `gam_family.py` (Family/Link abstraction, standard GLM IRLS theory per Wood §3.1.2 — no R-source archaeology needed, ADR-195 decision 2), `gam_fit.py` (`penalized_irls_general`, `effective_degrees_of_freedom`, `pearson_dispersion`) and `gam_family_conformance.py` (`FAMILY_CLAIM`, `fit_family_case`, `compare_family_case`); ran `scripts/gam_family_probe.R` against a deterministic shared `(X, S)` it builds itself (`set.seed(20260817)`, ADR-074) and compared | `max_abs_eta_diff` per case, `dispersion_diff` (quasipoisson-log only) | n/a (first measurement) | **agrees, all 4 cases** — `binomial-logit`: eta diff 1.998e-15; `binomial-cloglog`: eta diff 1.488e-14; `quasipoisson-log`: eta diff 7.994e-15, dispersion diff 9.671e-06; `poisson-log-offset`: eta diff 9.326e-15 | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed below. `eta`/`dispersion` are INDEPENDENT (`FAMILY_CLAIM`, `gam_family_conformance.py`): `fit_family_case`'s signature takes only the shared recipe, never the R script's own fit output (ADR-193's mechanical test). Coefficients are NOT compared, per Anchor 2 (never a Stage-B acceptance criterion). Confirms PLAN §6's registered-prediction spirit — the recursion agreed once family/link/weights matched, no iteration needed, same shape of result as ADR-194's `cr` basis. |
| 2026-08-17 | 3 | same hypothesis, re-measured on the authoritative oracle (a genuine INDEPENDENT parity claim, same class ADR-193 exists to let this epic make) | none — same two producers (`gam_fit.penalized_irls_general`, `scripts/gam_family_probe.R`), dispatched via CI `workflow_dispatch` on `90cbdba`; the comparison step `print()`s its report to stdout from the first version (the methodology fix slice 2 needed is now standard practice for every new probe), so this reading is the actual per-metric table via `get_job_logs`, not a masked step conclusion | same metrics as the tier-1 row above, read directly from job-log stdout | tier-1 reading above | **agrees, all 4 cases** — `binomial-logit`: eta diff 1.221e-15; `binomial-cloglog`: eta diff 1.488e-14; `quasipoisson-log`: eta diff 8.438e-15, dispersion diff 9.671e-06; `poisson-log-offset`: eta diff 9.326e-15 — same order as tier 1 at every case, dispersion diff identical to the last printed digit | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32057694949](https://github.com/jonathancrawford05/polaris-re/actions/runs/32057694949), both jobs completed in ~52s | **CONFIRMED (parity) — settled.** `eta` and `dispersion` — every quantity `FAMILY_CLAIM` declares — are `INDEPENDENT`, so this is genuine Stage-B parity across family/link/weight, the epic's first outside the already-verified Poisson case. Required levels 1-3 of the existing ten-cell suite also still agree on this run (`Required levels [1, 2, 3] all agree.`) — no regression from the workflow edit. See ADR-195. |
| 2026-08-17 | 3 | (PR #202 review [P2]) the `quasipoisson-log` dispersion diff (9.671e-06, bit-identical at both tiers) is `mgcv`'s `sig2` formula, not float noise or a bug in `gam_fit.pearson_dispersion` — is `pearson_rss/(n-edf)` (textbook Pearson dispersion, using `mgcv`'s OWN `edf` and fitted values) the wrong denominator, or is `mgcv`'s `m$sig2` itself not that formula? | none — a diagnostic-only tier-1 R script re-fitting the exact `quasipoisson-log` case and comparing `m$sig2` against `pearson_rss/(n-edf)`, `deviance/(n-edf)` and `pearson_rss/(n-rank(X))`, plus reading `mgcv:::gam.fit`'s source for how `G$sig2`/`magic()`'s `scale`/`gcv` arguments interact across IRLS iterations | `m$sig2` vs three candidate formulas | n/a (diagnostic) | **none of the three candidate formulas reproduce `m$sig2` exactly**: `pearson_rss/(n-edf)` off by 9.6709e-06 (the observed residual, bit for bit), `deviance/(n-edf)` off by 1.5299e-06 (closer, still not exact), `n-rank(X)` denominator also does not match. Source reading: for a scale-unknown family, `gam.fit`'s IRLS loop calls `magic(..., scale=G$sig2, gcv=(G$sig2<0), ...)`, and `G$sig2` is negative only on the FIRST call — every subsequent call passes `gcv=FALSE` (UBRE mode), under which `magic()` returns the SUPPLIED scale rather than a fresh GCV/Pearson recomputation (`?magic`: "scale: the estimated (GCV) or supplied (UBRE) scale parameter") | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt), diagnostic script not committed | **CHARACTERISED, not fully traced.** The evidence is consistent with `m$sig2` being the Pearson-dispersion estimate from an IRLS iteration at or near convergence but not necessarily the literal final one — a deterministic function of the fitting trajectory (explaining the cross-tier bit-stability) rather than a different formula in the ordinary sense. Full confirmation would need instrumenting `gam.fit`'s loop directly; not attempted (CLAUDE.md: mark the uncertainty rather than push a hypothesis past what was shown). **Decision (ADR-195 amendment): keep the textbook Pearson formula** rather than chase whichever candidate numerically landed closer, which would be tuning to a possibly-transient `mgcv` implementation artifact, not deriving the correct quantity (Anchor 8). The 1e-4 relative tolerance is unchanged and is now known to be absorbing a real, understood-in-shape ~0.2% discrepancy rather than pure headroom — a future dispersion diff materially larger than ~1e-5 on a similarly-conditioned case is a new finding worth the fuller trace this row stopped short of, not something to wave through under the same tolerance. |
| 2026-08-18 | 4 part A | `gam_reml.reml_score_general` (an independent generalization of the already-verified `experience_gam_penalized.reml_score` onto `gam_fit`'s general IRLS core — swaps only the deviance/working-weight source for a general `Family`, reduces bit-for-bit to the old score at `gamma=1` — superseded, see the RESOLUTION row below) reproduces `mgcv::gam(family=binomial(link='logit'), paraPen=list(X=list(S1,S2,sp=c(sp1,sp2))), method='REML')$gcv.ubre`'s PAIRWISE DIFFERENCE across three fixed `(sp1,sp2)` points on a shared two-block binomial/logit design — the first slice-4 measurement, and the first Stage-C REML-score comparison of any kind in this epic. **`deviance` is a second declared INDEPENDENT quantity** (`compare_reml_deviance`, added same day, PR #203 review [P1-1]), using the same independent fit `score_reml_point` scores | wrote `gam_reml.py` (the generalized score), `scripts/gam_reml_probe.R` (shared recipe, three points, `method="REML"` with a fully-supplied `sp` so no optimisation runs), `gam_reml_conformance.py` (`score_reml_point`/`deviance_reml_point` take plain arrays + the `sp` setting, no R-payload-shaped argument at all — a stronger form of the ADR-193 mechanical test); ran the probe and compared PAIRWISE score differences (not the absolute value — ADR-189 amendment 1 already found an unresolved offset there for the single-block Poisson case; differencing cancels any purely additive offset) and per-point deviance (absolute — no offset to cancel there) | `python_diff - r_diff` (residual) per pair, tolerance 1e-6; `python_deviance - r_deviance` per point, same tolerance | n/a (first measurement) | **First isolated the fit**: `deviance` matches `mgcv`'s to ~1e-11 at all 3 points, `agrees=True` on every point (the fit is correct, matching slice 3's eta parity). **Then the score: ALL 3 pairwise differences DISAGREE** against the 1e-6 tolerance — `(1,1)-(5,0.2)`: residual -0.741273; `(1,1)-(0.5,8)`: residual 0.000935 (smaller than the other two by ~800x, but still ~935x the tolerance — NOT agreement); `(5,0.2)-(0.5,8)`: residual 0.742208. Internally consistent (the three residuals sum as pairwise differences of one function must), ruling out a comparison-arithmetic slip | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS ("pairwise differencing cancels any offset and reproduces mgcv's score shape") — **REFUTED on all 3 pairs** (varying magnitude), confirmed below; deviance hypothesis CONFIRMED. An INDEPENDENT comparison that disagrees (ADR-193): a real result about the naive multi-block `log|S_lambda|_+`/`log|X'WX+S|` generalization, not a fit bug. See ADR-196 for the (corrected, same-day, PR #203 review [P1-3]) localizing evidence and the named next hypothesis. |
| 2026-08-18 | 4 part A | same hypothesis, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison whose disagreement magnitude, ~0.74, is five orders of magnitude above BLAS/version noise — still earns the tier-3 re-measurement per the routine's no-magnitude-carve-out rule, ADR-190 decision 5, since a formula question is "different code," not noise, regardless of size) — **score only**, `deviance` had no committed producer yet at this run (see the next row) | none — same producer (`gam_reml.reml_score_general`, `scripts/gam_reml_probe.R`), dispatched via CI `workflow_dispatch` on `6564b79`; comparison step prints its report to stdout from the start (the methodology fix ADR-194 needed partway through slice 2, applied here from the first measurement) | `python_diff - r_diff` (residual) per pair, tolerance 1e-6 | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — same three score residuals (-0.741273, 0.000934569, 0.742208), `agrees=False` on ALL 3 pairs | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32086738495](https://github.com/jonathancrawford05/polaris-re/actions/runs/32086738495), both jobs completed in ~57s | **CONFIRMED (parity) on `reml_score_pairwise_diff` — the disagreement is settled as real, not a tier-1 or BLAS artifact.** Required levels 1-3 of the existing suite also still agree on this run — no regression from the workflow edit. See ADR-196: the formula gap itself is OPEN, characterized with a named next hypothesis, not resolved this session. |
| 2026-08-18 | 4 part A | PR #203 review [P1-1]: does the SAME independent fit `score_reml_point` uses also reproduce `mgcv`'s deviance at every point — the committed comparison that licenses "the fit is correct," previously cited only in session prose with no producer in the repo | added `deviance_reml_point`/`compare_reml_deviance` to `gam_reml_conformance.py`, declared `deviance` as a second `ComparedQuantity` on `REML_SCORE_CLAIM`; re-ran the probe (unchanged) and the now-two-quantity comparator | `python_deviance - r_deviance` per point, tolerance 1e-6 | n/a (deviance had no committed comparison before this fix) | **tier 1: `agrees=True` on all 3 points**, diffs -8.06e-12/-3.88e-12/-8.20e-12 (all `(sp)`: `(1,1)`/`(5,0.2)`/`(0.5,8)` respectively) — float round-trip noise | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS ("the fit itself matches mgcv, only the score criterion diverges") — confirmed below |
| 2026-08-18 | 4 part A | same hypothesis, re-measured on the authoritative oracle — the first tier-3 run for BOTH declared quantities on `REML_SCORE_CLAIM` together | none — same two producers (`gam_reml.reml_score_general`, `gam_reml_conformance.deviance_reml_point`, `scripts/gam_reml_probe.R`), dispatched via CI `workflow_dispatch` on `a517800`; comparison step prints both tables to stdout | both metrics, read directly from job-log stdout via `get_job_logs` | tier-1 readings above (both rows) | **IDENTICAL to tier 1 at every printed digit, both quantities** — score: same three residuals (-0.741273, 0.000934569, 0.742208), `agrees=False` on all 3 pairs; deviance: `agrees=True` on all 3 points, diffs -8.06e-12/-3.98e-12/-8.14e-12 (same order as tier 1) | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32090689399](https://github.com/jonathancrawford05/polaris-re/actions/runs/32090689399), R job 34s + compare job 25s | **CONFIRMED (parity) on BOTH `reml_score_pairwise_diff` and `deviance` — the two quantities `REML_SCORE_CLAIM` declares.** `deviance` agreeing at tier 3 is what settles "the fit is correct" as a committed, re-runnable claim rather than an ad-hoc session observation (PR #203 review [P1-1], closed). Required levels 1-3 of the existing suite also still agree on this run — no regression from the workflow edit. See ADR-196 (also corrected same-day, review [P1-3]): the formula gap itself is OPEN, characterized with a corrected next hypothesis, not resolved this session. |
| 2026-08-18 | 4 part A | RESOLUTION (maintainer supplied Wood 2011 directly): does adding the penalized-deviance term `Dp = D(beta_hat) + beta_hat^T S beta_hat` (Wood 2011 §2 eq. 4 — a term the score's first generalization omitted entirely, using plain deviance instead) close the pairwise-score-difference gap this ledger's prior two rows measured | edited `gam_reml.reml_score_general` to compute `penalized_deviance = deviance + coef @ penalty @ coef` and use it in place of plain deviance; no other term changed; re-ran the same probe (`scripts/gam_reml_probe.R`, unchanged) and comparator | `python_diff - r_diff` (residual) per pair, tolerance 1e-6 — same metric, same tolerance, same fixture as the original characterization | the DISAGREEING readings above (rows 1-2 of this slice) | **CLOSED to float round-trip precision, all 3 pairs** — `(1,1)-(5,0.2)`: residual -1.90e-12 (was -0.741273); `(1,1)-(0.5,8)`: residual 8.42e-13 (was 0.000935); `(5,0.2)-(0.5,8)`: residual 2.74e-12 (was 0.742208). `agrees=True` on all 3 | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS ("the missing beta_hat^T S beta_hat term is the entire gap") — **CONFIRMED**, tier-3 re-measurement below. §3.1 of the same paper (the multi-penalty log-determinant numerical-stability machinery this ADR's original next-hypothesis pointed at) turned out to be inapplicable to this fixture for a well-grounded reason — its two blocks have disjoint column supports, so no cross-block "zero leakage" is possible — not merely coincidentally matching at two points, closing the [P1-3] question the review round raised. |
| 2026-08-18 | 4 part A | same hypothesis, re-measured on the authoritative oracle (a formula fix, not a magnitude question — earns tier 3 regardless of how large the closed gap was, ADR-190 decision 5) | none — same fixed producer, dispatched via CI `workflow_dispatch` on `1ae300d`; comparison step prints both tables to stdout as before | both metrics, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — same three near-zero residuals (-1.90e-12, 8.42e-13, 2.74e-12), `agrees=True` on all 3 pairs; deviance unchanged and still `agrees=True` on all 3 points | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32142352655](https://github.com/jonathancrawford05/polaris-re/actions/runs/32142352655), R job ~28s + compare job ~30s | **CONFIRMED (parity) — RESOLVED, not merely characterized.** `reml_score_pairwise_diff` and `deviance` — every quantity `REML_SCORE_CLAIM` declares — are both INDEPENDENT and both agree. Required levels 1-3 of the existing ten-cell suite also still agree — no regression from the fix. Slice 4 part A is DONE (ADR-196 resolution). Whether the SAME missing term affects the already-shipped `experience_gam_penalized.reml_score` is explicitly NOT settled by this row — scoped as `docs/WORK_ORDER_reml_penalized_deviance_production_check.md`, the epic's next session, ahead of slice 4 part B. |
| 2026-08-18 | work order (production REML check) | §3.1: does `experience_gam_penalized.reml_score`'s (production, `INDEPENDENT`) score, evaluated at each free-sp cell's own already-fitted `(design, coef, penalty)`, collapse its offset-adjusted residual against `mgcv`'s own `m$gcv.ubre` (`INDEPENDENT`) when re-scored with the corrected (Dp-based) criterion — same offset-removal convention as ADR-189 amendment 1 (`≈ -l_sat/gamma`) | added `gam_reml_production_check.corrected_reml_score`/`measure_production_score_gap` (a one-line call into the already-verified `gam_reml.reml_score_general(poisson_log())`, not a new formula) and `scripts/reml_production_check_probe.py`; ran against the committed exchange + `python_reference.json` + a locally-generated `mgcv_reference.json` | offset-adjusted residual (`gap + l_sat/gamma`) per free-sp cell, current vs corrected | current: -1.836194 / -2.497827 / -1.658464 (`l2-free-sp` / `-factors` / `-kb`) | **residual does NOT collapse — roughly DOUBLES**: corrected -3.739483 / -4.649192 / -3.786146. Not a refutation: each side selects a DIFFERENT lambda at a free-sp cell, so this comparison mixes any formula gap with the point mismatch (named in the work order itself; §3.2 below is the matched-point-free measurement built to separate the two) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed identical below. Both quantities `INDEPENDENT` (`PRODUCTION_REML_CHECK_CLAIM`, `gam_reml_production_check.py`) — neither producer's signature accepts mgcv's own score/coef. |
| 2026-08-18 | work order (production REML check) | same measurement, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison, ADR-193) | none — same producers, dispatched via CI `workflow_dispatch` on `170bd76`; new diagnostic step prints its report to stdout from the start | same metric, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — same three offset-adjusted residuals, current and corrected | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32181109927](https://github.com/jonathancrawford05/polaris-re/actions/runs/32181109927), R job ~33s + compare job ~29s | **CONFIRMED (parity) on `reml_score (current)` and `reml_score (corrected)` — both agree with tier 1.** Required levels 1-3 of the existing ten-cell suite also still agree on this run. See ADR-197: §3.1 alone is NOT the decisive measurement (see §3.2 below); it is a real, honestly-reported non-collapse. |
| 2026-08-18 | work order (production REML check) | §3.2, REGISTERED PREDICTION (per the work order's own Anchor-9-style discipline, before running): if the missing term is a real bug, re-scoring `select_lambdas_reml`'s SAME 2-D grid search with the corrected criterion should select a point measurably CLOSER to mgcv's own free-sp selection than the current shipped selection — landing on the same point, or not closer, is an equally valid finding | added `select_lambdas_corrected` — a diagnostic REPLICA of `select_lambdas_reml`'s exact coarse+refine grid search (same bounds/rejection rule), scored with the corrected criterion; `select_lambdas_reml` itself not called, imported, or modified | log10 distance from Python's selected `(λ_age, λ_year)` to mgcv's own free-sp selection, current vs corrected selection, per cell | current: 0.3149 / 0.1870 / 0.4559 (`l2-free-sp` / `-factors` / `-kb`) | **PREDICTION HELD on all 3 cells**: corrected 0.0663 / 0.1097 / 0.1248 — closer on every cell, not just on average. `l2-free-sp`'s move (λ_age 3162.28→5623.41, λ_year unchanged) independently reproduces the exact grid-step the maintainer's own local patch-and-refit experiment found (ADR-196 resolution, work order §2) — a second, structurally different confirmation of that number | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed identical below. This is the decisive measurement (work order §6: "the harder question"). |
| 2026-08-18 | work order (production REML check) | same measurement, re-measured on the authoritative oracle | none — same replica, same CI dispatch (`170bd76`, run 32181109927) as the §3.1 tier-3 row above | same metric, read directly from job-log stdout | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — same log10 distances, same 3-of-3 "closer" verdict, same grid-step move on `l2-free-sp` | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32181109927](https://github.com/jonathancrawford05/polaris-re/actions/runs/32181109927) | **CONFIRMED (parity) — the registered prediction is a settled result, not tier-1-only.** See ADR-197 decision 2: recommends fixing `experience_gam_penalized.reml_score`, re-baseline maintainer-gated. |
| 2026-08-19 | work order (production REML check), PR #204 review [P1] | §3.2 point 2, closed: does the corrected selection also land closer to mgcv on `edf_total`/`edf_tensor`/`edf_factors`, not only `(λ_age, λ_year)` — the original PR only reported lambda | `select_lambdas_corrected` now does one extra fit AT its own selected point (mirrors `fit_reml` refitting at the selected point after `select_lambdas_reml`'s own sweep) and returns `edf_total`/`edf_tensor`/`edf_factors` alongside the selection; `current`/`mgcv` EDF read straight off the already-committed `python_reference.json`/`mgcv_reference.json`, no new fit needed for either | `|edf_total - mgcv edf_total|`, current vs corrected, per cell (all three columns `INDEPENDENT` pairwise, same ADR-193 mechanical test as the λ-distance columns) | current: 0.6514 / 0.4806 / 0.8734 (`l2-free-sp` / `-factors` / `-kb`) | **CLOSER on all 3 cells, roughly an order of magnitude**: corrected 0.1014 / 0.0262 / 0.0109. `edf_tensor` moves identically (no factor block on `l2-free-sp`/`-kb`; `-factors`' `edf_factors` is a constant 1.0000 across current/corrected/mgcv — the single factor dummy is effectively unpenalized under every criterion, so it carries no signal either way). Agrees in direction and order of magnitude with the already-confirmed λ-distance result on the same 3 cells (rows above) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed identical below. Second, independent line of evidence for ADR-197 decision 2 (fix it; re-baseline maintainer-gated) — did not exist when decision 2 was written, does not change it. |
| 2026-08-19 | work order (production REML check), PR #204 review [P1] | same measurement, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison, ADR-193) | none — same producers, dispatched via CI `workflow_dispatch` on `138683b`; same diagnostic step (now emitting the EDF table) prints its report to stdout | same metric, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — same `edf_total`/`edf_tensor`/`edf_factors` for current, corrected and mgcv, all 3 cells | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8, same digest as the original ADR-197 measurement), run [32201059741](https://github.com/jonathancrawford05/polaris-re/actions/runs/32201059741), commit `138683b`, both jobs completed in ~62s | **CONFIRMED (parity) — settled, not tier-1-only.** See ADR-197 amendment (2026-08-19, PR #204 review). Also settled this run: the §3.2 null control (`select_lambdas_corrected(..., use_corrected_score=False)` reproduces the shipped `l2-free-sp` selection `(3162.2776601683795, 1000.0)` exactly, PR #204 review [P2]) — a unit test, not a CI-dispatched measurement, so it is not a separate ledger row, but it is what licenses reading this row's "closer" verdict as a statement about the score formula rather than a divergent replica. |
| 2026-08-18 | work order (production REML check) | §3.3: does the corrected score change `smoothing_uncertainty`'s finite-difference Hessian/eigenvalues/Kass-Steffey-correction magnitude at the ALREADY-SELECTED (not re-selected) `(lambda_age, lambda_year)` | added `score_shape_diagnostic` — a diagnostic REPLICA of `smoothing_uncertainty`'s exact 9-point central-difference construction, fitting once and scoring both ways per point so both Hessians share bit-identical coefficients; `smoothing_uncertainty` itself not called or modified | Hessian eigenvalues and resulting inflation ratio (`mean(diag(Vb+correction))/mean(diag(Vb))`), current vs corrected, per cell | current eigenvalues [0.4187,0.6711]/[0.5212,0.7925]/[0.2771,0.4133], current inflation 1.1109x/1.1591x/1.2139x (reproduces ADR-189 amendment 1's own published numbers exactly — an internal cross-check that this replica is faithful) | **shifts materially but does not close the level-4 gap**: corrected eigenvalues [0.2940,0.4977]/[0.3590,0.7306]/[0.2289,0.3915] (~25-40% relative shift), corrected inflation 1.1538x/1.1851x/1.2418x — a small move toward mgcv's 1.7392x/1.4863x/1.8670x, far short of the 3.2-4.1x gap ADR-190 already characterized via a separate, unrelated derivation | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS ("this bug materially explains the level-4 BLOCKER") — **REFUTED as a material contributor**, confirmed identical below; ADR-190's `dw/drho` diagnosis stands unaffected. |
| 2026-08-18 | work order (production REML check) | same measurement, re-measured on the authoritative oracle | none — same replica, same CI dispatch (`170bd76`, run 32181109927) | same metrics, read directly from job-log stdout | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — same eigenvalues, same inflation ratios, `n_floored` 0 everywhere both formulas | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32181109927](https://github.com/jonathancrawford05/polaris-re/actions/runs/32181109927) | **CONFIRMED — settled.** §3.3 is characterized: the bug's effect on level 4 is real but small, not the explanation for the standing BLOCKER. See ADR-197. |
| 2026-08-19 | work order (production REML check) — RESOLUTION | maintainer-authorized: fix `experience_gam_penalized.reml_score` for real (not a diagnostic replica) the same way ADR-196 fixed `gam_reml.reml_score_general`, re-baseline `python_reference.json` via `export_mgcv_case.py`, and re-run the existing ten-cell conformance suite end to end against the fixed module | one-line fix (`penalized_deviance = deviance + coef @ penalty @ coef`, used in place of plain `deviance`) applied directly to `experience_gam_penalized.reml_score`; `python_reference.json` regenerated via `write_python_reference` (the test's own regeneration path, not hand-edited); `scripts/mgcv_conformance.R` + `scripts/compare_mgcv_conformance.py` re-run against the SAME committed exchange, unmodified | `max_abs_log10_sp_diff`/`abs_edf_total_diff_free_sp` (levels 2), `max_abs_log10_sp_diff_gamma`/`abs_edf_total_diff_gamma` (level 5), `rel_unconditional_inflation_diff` (level 4) — all already-INDEPENDENT conformance metrics (`VERIFICATION_STANDARD.md` §5), now read against the FIXED module instead of the buggy one | before (buggy, same numbers as the original ADR-197 §3.1/§3.2/[P1] tables): `l2-free-sp`/`-factors`/`-kb` `max_abs_log10_sp_diff` 0.3145/0.1709/0.4322, `abs_edf_total_diff_free_sp` 0.6514/-0.4806/0.8733; `l5-gamma` `max_abs_log10_sp_diff_gamma` 0.6724 (FAIL), `abs_edf_total_diff_gamma` 1.1270 (FAIL); levels: `1 AGREES 2 AGREES 3 AGREES 4 DISAGREES 5 DISAGREES` | **after (fixed): closer to `mgcv` on every free-`sp` metric, matching §3.2's prediction to the printed digit** — `max_abs_log10_sp_diff` 0.0645/0.0791/0.1048, `abs_edf_total_diff_free_sp` 0.1013/-0.0262/0.0109; `l5-gamma` now **PASSES both metrics**: `max_abs_log10_sp_diff_gamma` 0.0776, `abs_edf_total_diff_gamma` -0.0024. Levels: `1 AGREES 2 AGREES 3 AGREES 4 DISAGREES (unchanged, ADR-190's separate dw/drho gap) 5 AGREES`. Required levels 1-3 unchanged (still AGREE) — no regression | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS ("the real fix reproduces §3.2's predicted improvement and does not regress levels 1-3") — **CONFIRMED**, and level 5 moved from DISAGREES to AGREES, an improvement beyond what §3.2 measured (which covered only the three plain free-`sp` cells). See ADR-197's 2026-08-19 resolution amendment. |
| 2026-08-19 | work order (production REML check) — RESOLUTION | same measurement, re-measured on the authoritative oracle | none — same fixed module, same regenerated reference, dispatched via CI `workflow_dispatch` on `ce0b9f1` | same metrics, read directly from job-log stdout | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — `max_abs_log10_sp_diff` 0.0645/0.0791/0.1048, `abs_edf_total_diff_free_sp` 0.1013/-0.0262/0.0109, `l5-gamma` `max_abs_log10_sp_diff_gamma` 0.0776 (PASS)/`abs_edf_total_diff_gamma` -0.0024 (PASS), `rel_unconditional_inflation_diff` still FAILs on `l2-free-sp`/`l2-free-sp-kb` (-0.322/-0.334, level 4 unaffected); levels `1 AGREES 2 AGREES 3 AGREES 4 DISAGREES 5 AGREES` | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8, same digest as every prior measurement in this epic), run [32204739991](https://github.com/jonathancrawford05/polaris-re/actions/runs/32204739991), both jobs completed in ~63s | **CONFIRMED — settled.** Required levels 1-3 still AGREE (no regression); level 5 moves from DISAGREES (pre-fix) to AGREES. Level 4 unchanged in kind (ADR-190's separate `dw/drho` gap). See ADR-197's 2026-08-19 resolution amendment. |
| 2026-08-22 | 4 part B (ADR-199) | ADR-198's registered decisive test: does a continuous (quasi-Newton) search over `log10(lambda)` — `gam_reml_optimize.select_lambdas_continuous`, built on the already-verified `gam_fit.penalized_irls_general`/`gam_reml.reml_score_general`, never reading mgcv's own selection — collapse `max_abs_log10_sp_diff` toward its own convergence tolerance rather than leaving it near 0.1, on the same four free-sp cells ADR-198 measured post-fix | wrote `gam_reml_optimize.py` (the search), `gam_reml_optimize_conformance.py` (`CONTINUOUS_LAMBDA_CLAIM`), `scripts/reml_continuous_optimizer_probe.py`; ran the probe against the committed exchange + `python_reference.json` (grid's own selection, read not recomputed) + a locally-generated `mgcv_reference.json` | `max_abs_log10_sp_diff` (continuous search) vs mgcv's own free-sp selection, per cell, against the grid's own value from the same reference | grid (ADR-198, post-fix): `l2-free-sp` 0.0645, `l2-free-sp-factors` 0.0791, `l2-free-sp-kb` 0.1048, `l5-gamma` 0.0776 | **collapses on every cell, 2-3 orders of magnitude**: `l2-free-sp` 5.002e-04, `l2-free-sp-factors` 4.393e-05, `l2-free-sp-kb` 5.429e-04, `l5-gamma` 7.283e-04 — all four `converged=True` (SciPy L-BFGS-B, `gtol=1e-8`, 27-48 function evaluations), landing at the search's own convergence floor rather than anywhere near 0.1. `edf_total` diffs also shrink (order 1e-3-1e-4, down from the grid's already-small 0.10/-0.03/0.01) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — ADR-198's prediction HOLDS, decisively, at tier 1; confirmed identical below. |
| 2026-08-22 | 4 part B (ADR-199) | same measurement, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison whose effect size — 2-3 orders of magnitude — earns the tier-3 re-measurement per the routine's no-magnitude-carve-out rule, ADR-190 decision 5, regardless of how large the tier-1 finding was) | none — same producer (`gam_reml_optimize.select_lambdas_continuous`), same probe, dispatched via CI `workflow_dispatch` on `601b73f`; comparison step prints its report to stdout from the start | same metric, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL IN VERDICT to tier 1, same order of magnitude, last-bit differences consistent with the different mgcv release/BLAS the routine's tier discipline predicts** — `l2-free-sp` 6.904e-04, `l2-free-sp-factors` 5.084e-05, `l2-free-sp-kb` 1.656e-04, `l5-gamma` 9.764e-04, all four `converged=True` | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32544930172](https://github.com/jonathancrawford05/polaris-re/actions/runs/32544930172), both jobs completed in ~68s | **CONFIRMED (parity) — settled, not tier-1-only.** `max_abs_log10_sp_diff` (continuous search) and `edf_total` (continuous search) — every quantity `CONTINUOUS_LAMBDA_CLAIM` declares — are INDEPENDENT, so this is genuine parity evidence about the search, not a harness check. Required levels 1-3 of the existing ten-cell suite also still agree on this run — no regression from the workflow edit. See ADR-199. |
| 2026-08-22 | 5 (the MI term) | Slice 5's first hypothesis, derived by direct R probe before being written into `gam_term_extract.R` (Anchor 8 — not guessed): a numeric-`by` `cr` smooth (`s(AttdAge, by=StudyYear_C)`, the MI term's own basis) has `smoothCon(..., absorb.cons=TRUE)$C` with **zero rows** — mgcv absorbs NO identifiability constraint on it — so `build_python_cr_term`'s design for a by-term should be the UNCONSTRAINED k-column `cr_basis` output with each row scaled by `by` (`gam_basis_cr.by_scale_design`, new), penalty `S` untouched by the scaling and left unconstrained | extended `gam_term_extract.R`'s `extract_smooth_one` with a `with_by` branch (`s(x, by=z, ...)`, `z` drawn after `x`/`y` so no other case's RNG stream moves) and one new case `mi-term-attdage-by-k13` (target AttdAge k=13 knots); added `gam_basis_cr.by_scale_design`; extended `build_python_cr_term` to take an optional `by` array and skip `absorb_sum_to_zero_constraint` when it is set; extended the CI report's `smooth_cases` dict and the test's `_SMOOTH_CASES` dict to cover the new case | `max_abs_design_diff`, `max_abs_s_diff`, `rank_diff`, index-range agreement, knots agreement (recipe check only) | n/a (first measurement) | **agrees** — `max_X_diff=2.176e-14`, `max_S_diff=3.775e-15` (float round-trip noise, same order as slice 2's other 5 cases), `rank_diff=(0,)` (rank 11 both sides, k=13), `index_range` `[0, 13)` both sides (unconstrained — no column dropped, unlike every non-`by` case's `[0, 12)`), `knots_agree=True`. The R-side internal guard (`smoothCon()` vs `lpmatrix`/`m$smooth[[1]]`, ADR-191) also passed on the by-case at tier 1 — its column-name pattern `s(x):z.N` still matches the existing `^s\\(x\\)` grep | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS ("mgcv drops the sum-to-zero constraint entirely for a numeric-`by` smooth, rather than constraining it some other way") — confirmed by direct measurement before commit, tier-3 re-measurement below. `design_X`/`penalty_S`/`rank` are INDEPENDENT. (Originally shipped reusing `CR_BASIS_CLAIM`; corrected same day to a distinct `CR_BY_BASIS_CLAIM` after PR #206 review [P1] found that claim's published producer strings misdescribe the by-row — the classification and every number here are unaffected.) |
| 2026-08-22 | 5 (the MI term) | same hypothesis, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison, ADR-193 — same class as slice 2's own tier-3 row) | none — same two producers (`gam_basis_cr` via `build_python_cr_term`, `gam_term_extract.R`'s `smoothCon` `with_by` branch), dispatched via CI `workflow_dispatch` on `08472b3`; the existing "Compare the per-term extraction against the Python side (slice 1 / 1b / 2)" step already prints its report to stdout (no new step needed — the CI script's own `smooth_cases` dict was extended alongside the test's) | same metrics, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — `mi-term-attdage-by-k13`: `max_X_diff=2.176e-14`, `max_S_diff=3.775e-15`, `rank_diff=(0,)`, `index_range agrees=True`, `knots_agree=True` | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32571764900](https://github.com/jonathancrawford05/polaris-re/actions/runs/32571764900), R job ~40s + compare job ~35s | **CONFIRMED (parity) — settled, not tier-1-only.** `design_X`/`penalty_S`/`rank` — every quantity `CR_BY_BASIS_CLAIM` declares (see the tier-1 row's note on the same-day claim split) — are INDEPENDENT, so this is genuine Stage-A basis parity for the MI term's numeric-`by` construction, the same class of result as ADR-194's original `cr` basis. Required levels 1-3 of the existing ten-cell suite also still agree on this run (levels 1-3 gate step completed successfully) — no regression from the workflow/extractor edit. See ADR-200. |
| 2026-08-22 | level-4 prerequisite (ADR-201) | Wood (2011) sec 3.4 + Appendix D give `dbeta/drho` and `dw/drho` — the ingredient ADR-190 decision 2 named as missing ("it needs `dw/drho`, which nothing in the fitter currently computes"). Hypothesis: an analytic implementation reproduces `mgcv`'s own behaviour, i.e. it is the h -> 0 limit of central differences of `mgcv` refits at perturbed `sp`. REGISTERED IN ADVANCE (work order sec 3): agreement on the canonical-link cells, and possible material DISAGREEMENT on `binomial-cloglog`, because Wood's derivation uses the OBSERVED (Newton) Hessian while `gam_fit.penalized_irls_general` uses FISHER weights | wrote `gam_derivatives.py` (`d_beta_d_rho`, `d_eta_d_rho`, `newton_alpha`, `newton_working_weights`, `dw_deta`, `dw_drho`), `gam_derivatives_conformance.py` (`DERIVATIVE_CLAIM`), `scripts/gam_deriv_probe.R` (mgcv refits at rho +/- h over 3 h values) and `scripts/gam_deriv_compare.py`; 26 R-free tests in the gating pytest job | `max_abs_deta_drho_diff`, `max_abs_dw_drho_diff` (both vs a central difference of mgcv's OWN eta / `m$weights`), `max_abs_eta_diff` (control), and the Richardson ratio at the truncation-dominated h pair | n/a (first measurement) | **the prediction HELD, then was RESOLVED.** With FISHER weights in the Hessian: `poisson-log` 6.6e-12, `binomial-logit` 2.7e-11, **`binomial-cloglog` 6.9e-06** — six orders worse, exactly as registered, with `max\|alpha-1\|` = 6.7e-16 / 0.0 / 4.3e-03 confirming alpha is identically 1 on the canonical links and not on cloglog. Switching to the OBSERVED-Hessian weights (`newton_working_weights`) collapses cloglog to **1.1e-11**, the same floor as the canonical cells. Final vs mgcv: `poisson-log` d(eta)/d(rho) 6.409e-11 / dw/d(rho) 1.725e-07; `binomial-logit` 5.437e-11 / 8.606e-11; `binomial-cloglog` 7.004e-11 / 5.834e-10; Richardson ratio **4.00** on all three | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed below. Both quantities INDEPENDENT (`DERIVATIVE_CLAIM`): `analytic_derivatives`' signature takes only the shared recipe (design, penalties, y, prior weights, family spec, rho) and never the R side's eta/w or any difference of them. Compared on `d_eta_d_rho`, NEVER on `dbeta/drho` (Anchor 2 — mgcv reparameterises). **An earlier version of the probe reported only small h and a Richardson ratio of ~0.6**, which would have overstated the convergence evidence; the probe now brackets both the truncation- and round-off-dominated regimes and reports which is which. |
| 2026-08-22 | level-4 prerequisite (ADR-201) | same hypothesis, re-measured on the authoritative oracle (a genuine INDEPENDENT parity claim, ADR-193) | none — same two producers (`gam_derivatives`, `scripts/gam_deriv_probe.R`), dispatched via CI `workflow_dispatch` on `d5cc7b5`; the comparison step prints its report to stdout from the first version (standard practice since ADR-194) | same metrics, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **AGREES on all three cases, same order as tier 1** — `poisson-log` d(eta)/d(rho) 5.757e-11 / dw/d(rho) 1.725e-07; `binomial-logit` 5.511e-11 / 7.397e-11; `binomial-cloglog` 5.316e-11 / 5.834e-10; eta control 8.442e-13 / 7.772e-15 / 8.362e-12; **Richardson ratio 4.00 on all three**, confirming the analytic derivative is the h -> 0 limit of mgcv's own behaviour rather than merely near it at one step. Last-bit differences from tier 1 are consistent with the different mgcv release / BLAS the routine's tier discipline predicts | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32586279901](https://github.com/jonathancrawford05/polaris-re/actions/runs/32586279901), both jobs green | **CONFIRMED (parity) — settled.** `d_eta_d_rho` and `dw_drho` — every quantity `DERIVATIVE_CLAIM` declares — are INDEPENDENT, so this is genuine parity evidence about the derivative. Required levels 1-3 of the ten-cell suite also still agree on this run; level 4 DISAGREES exactly as before. **This slice does NOT close level 4** and must not be read as doing so: Wood (2011) contains no unconditional-covariance formula (searched — zero occurrences of "unconditional"); it supplies the ingredient, and the assembly of `dw/drho` into `Vc` is Wood, Pya & Saefken (2016)'s own contribution, still outstanding. See ADR-201. |
| 2026-08-22 | level 4 (ADR-202) | Wood, Pya & Säfken (2016) eq. (7) `V'_beta = V_beta + J Vrho J^T + V''` reproduces `mgcv`'s `vcov(unconditional = TRUE)`, closing ADR-190's blocker. Three sub-hypotheses, each MEASURED not chosen: (a) `Vrho`'s regularisation is a ridge of exactly 0.1 — `m$V.sp == solve(H + 0.1 I)` to 1.78e-15, a 1-D search returning `0.1000000000`; (b) `V''` is NOT invariant to the square root, and the factor is Wood (2011) §3.3's lower-triangular `G = L^-1`, not a Cholesky of `V_beta`; (c) the two terms use DIFFERENT inverses of the rho Hessian — `V'` the unregularised `H^-1`, `V''` the ridged one | wrote `gam_uncertainty.py` (`cholesky_factor_derivative`, `wood_factor_and_derivative`, `regularized_v_rho`, `second_order_correction`, `unconditional_covariance`), `gam_uncertainty_conformance.py` (`VC_CLAIM`), `scripts/gam_vc_probe.R` and `scripts/gam_vc_compare.py`; 15 R-free tests in the gating job | `max_rel_correction_diff` ELEMENT-WISE on `Vc - Vp` (the gate) and the mean-diagonal `inflation_ratio` (reported alongside), per case | ADR-190: ours inflates 1.11-1.21x where mgcv inflates 1.49-1.87x; ratio of mgcv's correction to `J Vrho J^T` non-constant at 3.2-4.1x | **CLOSED** — `poisson-log` 0.904% element-wise (1.1319x vs 1.1317x), `binomial-logit` 0.023% (1.3650x vs 1.3650x), `binomial-cloglog` 0.150% (1.2310x vs 1.2312x). Sub-hypothesis (c) found by LOCALISATION not search: the residual left by a single ridged `Vrho` was rank-1 (rel. singular values 1.000, 0.084, 0.0006), `|cos| = 0.9994` with `J[1]`, best multiple of `J1 J1^T` = 3210 vs unregularised `H^-1[1,1]` = 3184. Validated on 5 HELD-OUT cases (different seeds/n/p, incl. a non-canonical cloglog), worst element-wise residual 0.730% | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed identical below. Both quantities INDEPENDENT (`VC_CLAIM`). **Element-wise is the gate, not the inflation ratio**: mid-slice the ratio read 0.39% while the element-wise residual was 26.7%, so the probe now exports full `Vc`/`Vp` matrices. |
| 2026-08-22 | level 4 (ADR-202) | same, re-measured on the authoritative oracle | none — same producers, dispatched via CI `workflow_dispatch` on `e5bb615`; comparison step prints to stdout from the first version | same metrics, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — `poisson-log` 0.904% / 1.1319x vs 1.1317x; `binomial-logit` 0.023% / 1.3650x vs 1.3650x; `binomial-cloglog` 0.150% / 1.2310x vs 1.2312x; all `agrees=True` | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32589501512](https://github.com/jonathancrawford05/polaris-re/actions/runs/32589501512) | **CONFIRMED (parity) — ADR-190's level-4 FORMULA gap is CLOSED.** What this does NOT do: the ten-cell suite's level 4 still reads `DISAGREES` on this very run, correctly — it exercises the SHIPPED `experience_gam_penalized.smoothing_uncertainty`, untouched here (Anchor 7). Re-pointing production needs Anchor 7 sign-off and its own determinism answer (ADR-186); ADR-188's coverage gate is a separate, unrun measurement. See ADR-202. |
| 2026-08-23 | ADR-188 coverage gate (ADR-203) | ADR-190 decision 4's registered prediction: implementing Wood, Pya & Saefken (2016) eq. (7) "should move coverage toward or past" the 0.9192 floor. Measured on the **production** path (select lambda per replicate, fit, form the band, count), 200 replicates x 2 truths x 4 bands from the same fits | wrote `analytics/gam_uncertainty_mi.py` (`wps_correction`, `_floored_hessian`) which READS a fit `experience_gam_penalized` produced rather than editing it — PLAN Anchor 7 intact, no production path changed; extended `scripts/unconditional_coverage_study.py` with `ks-analytic` (shipped formula, analytic `J`) and `wps2016` (eq. (7) full) bands; 11 new R-free tests, mutation-verified (zeroing `V''` fails the suite) | select-per-replicate coverage `overall`/`young<=50`/`old>=80`, mean band width, and mean coefficient-variance inflation, per (truth, band) | ADR-188: 0.8516 / 0.8581 vs a 0.9192 floor | **CONFIRMED IN DIRECTION, REFUTED IN SUFFICIENCY.** age-flat 0.7815 -> **0.8167** (+0.0352); age-varying 0.8090 -> **0.8354** (+0.0264); floor 0.9192, so the gate **still fails by up to 0.1025**. Inflation 1.2285x -> 1.6324x and 1.1023x -> 1.3207x (correction 2.76x / 3.14x larger, inside ADR-190's 3.2-4.1x band). Mechanism separation: `ks-analytic` lands within 0.0003 of the shipped band on both truths against a formula effect of 0.027-0.036, so the movement is eq. (7)'s and not the derivative method's | **local, no oracle** — this is a Monte-Carlo coverage study against a known truth, not an `mgcv` comparison; no tier applies and none is claimed | **The formula was A gap, not THE gap.** ADR-190 decision 4's contingency applies in substance though its literal trigger did not fire: a second cause remains, not reachable by any covariance eq. (7) can form. **Coverage is NOT a reason to re-point production** — ADR-202's parity is the case for that and it is a different case. **Finding 0, unplanned:** the committed baseline was STALE — `ce0b9f1` (2026-08-19, the correct maintainer-authorized ADR-197 REML fix) changed lambda selection and moved coverage -0.0432 / -0.0410, bisected under monkeypatch; nothing in CI or the `Makefile` re-runs this study, so 0.8516 / 0.8581 stayed cited as current for four days across four live documents including ADR-190 decision 4 itself. See ADR-203. |
| 2026-08-24 | 5 (`ti()`) | Slice 5's remaining piece, derived by instrumenting `mgcv:::smooth.construct.tensor.smooth.spec` directly (`assign()`-ing its internal locals mid-execution, Anchor 8 — not guessed) before writing any Python: `ti(x1, x2, k=(k1,k2), bs="cr")` is (1) each margin's own `smoothCon(absorb.cons=TRUE)` `cr` basis/penalty — `mc[i]=TRUE` is `ti()`'s default; (2) NO further reparameterization, because `smooth.construct.cr.smooth.spec` sets `noterp <- TRUE` on every `cr` margin, which the tensor constructor's `is.null(object$margin[[i]]$noterp)` gate skips (the `np=TRUE` SVD reparameterization is real but never fires for an all-`cr` tensor — measured, not assumed, after an earlier hand-replica that DID apply it disagreed by up to 182 in `X`); (3) each margin's penalty rescaled by its own leading eigenvalue; (4) the tensor design is the row-wise Kronecker of the (untouched) marginal designs, tensor penalties `S_1=Sm_1⊗I`, `S_2=I⊗Sm_2` (`mgcv::tensor.prod.model.matrix`/`tensor.prod.penalties`, column order confirmed on a small hand-built example, not assumed from the name); (5) a SECOND, tensor-level `scale.penalty` rescaling — `smoothCon()`'s own step, found by instrumentation after step-4-only output's `S` blocks disagreed with `smoothCon()`'s by a constant ratio (8.06x on one case) despite `X` already agreeing exactly | wrote `gam_basis_cr.ti_basis` (+ `sum_to_zero_null_space`, split out of `absorb_sum_to_zero_constraint` so both call sites share one null-space computation) and `gam_stage_a.build_python_ti_term`/`TI_BASIS_CLAIM`; extended `gam_term_extract.R` with `extract_smooth_ti` (its own `smoothCon` vs `lpmatrix`/`m$smooth[[1]]` internal guard, ADR-191's discipline) and two cases, a small synthetic one and the target formula's own `ti(AttdAge, PolYear, k=c(13,6))` knots; extended `mgcv-conformance.yml`'s diagnostic step with a third table | `max_abs_design_diff`, `max_abs_s_diff` (both blocks), `rank_diff` (both blocks), per case | n/a (first measurement) | **exact** — all diffs ~1e-14 (float round-trip noise, same order as slice 2's `cr` basis), rank diff `(0, 0)` both cases (`ti-default-knots-k6-k5`: k=(6,5), design 300×20, rank (16,15); `ti-target-attdage-polyear`: k=(13,6), design 500×60, rank (55,48)) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed identical below. `design_X`/`penalty_S`/`rank` are INDEPENDENT (`TI_BASIS_CLAIM`, `gam_stage_a.py`): `build_python_ti_term`'s signature takes only `x1`/`x2`/`term`, never mgcv's `X`/`S`/`rank` output — the epic's second INDEPENDENT Stage-A claim (after ADR-194's `cr` basis) and its first for a two-margin term. |
| 2026-08-24 | 5 (`ti()`) | same hypothesis, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison, ADR-193 — same class as slice 2's own tier-3 row) | none — same two producers (`gam_basis_cr.ti_basis` via `build_python_ti_term`, `gam_term_extract.R`'s `extract_smooth_ti`), dispatched via CI `workflow_dispatch` on `c24d59d`; the existing "Compare the per-term extraction against the Python side (slice 1 / 1b / 2)" step (already prints its report to stdout, ADR-194's methodology fix) carries the new third table | same metrics, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL IN ORDER OF MAGNITUDE, last-bit differences consistent with the different mgcv release/BLAS the routine's tier discipline predicts** — `ti-default-knots-k6-k5`: max X diff 1.549e-14, S diffs 2.975e-14/3.997e-14 (tier 1: 2.975e-14/4.130e-14), rank diff `(0,0)`; `ti-target-attdage-polyear`: max X diff 1.504e-14, S diffs 4.974e-14/3.908e-14 — **identical to tier 1 at every printed digit on this case**, rank diff `(0,0)` | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32677470292](https://github.com/jonathancrawford05/polaris-re/actions/runs/32677470292), R job ~40s + compare job ~30s | **CONFIRMED (parity) — settled, not tier-1-only.** `design_X`/`penalty_S`/`rank` — every quantity `TI_BASIS_CLAIM` declares, both penalty blocks — are INDEPENDENT, so this is genuine Stage-A basis parity for the `ti()` tensor interaction, the same class of result as ADR-194's original `cr` basis and ADR-200's numeric-`by` basis. Required levels 1-3 of the existing ten-cell suite also still agree on this run (`Required levels [1, 2, 3] all agree.`) — no regression from the workflow/extractor edit. Level 4 still DISAGREES (ADR-190's unchanged, unrelated `dw/drho` gap) and level 5 still AGREES. See ADR-205. |
| 2026-08-24 | 5 (remaining scope) | The first multi-term mgcv-native model: `polaris_re` assembles `s(AttdAge,k=13,bs="cr")` + `s(AttdAge,by=StudyYear_C,k=13,bs="cr")` + `ti(AttdAge,PolYear,k=c(13,6),bs="cr")` from the three already-independently-verified basis producers (`gam_basis_cr` via `gam_stage_a.build_python_cr_term`/`build_python_ti_term` — ADR-194, ADR-200, ADR-205) and fits it with `gam_fit.penalized_irls_general` at a fixed sp per block (binomial/cloglog, `ExposCnt` weights, Anchor 5's absolute idiom); `mgcv` fits the identical three-term formula natively via `gam()` at the same fixed sp — compared on `eta` at the training design | wrote `scripts/gam_multiterm_probe.R` (shared recipe: `AttdAge`/`PolYear`/`StudyYear_C`/`ExposCnt`/`y`, the target formula's own knot vectors, fixed `sp=(2.0,3.0,1.5,4.0)`) and `gam_multiterm_conformance.py` (`assemble_multiterm_design`, `fit_multiterm_case`, `MULTITERM_CLAIM`); ran the probe and compared | `max_abs_eta_diff`, `agrees` | n/a (first measurement) | **agrees** — `max_abs_eta_diff=1.242e-10` (median row diff 4.3e-13, mean 1.6e-12), `agrees=True`, `n=900`, `p=86` (1 intercept + 12 reference + 13 by-term + 60 `ti()`, matching `1 + (13-1) + 13 + (13-1)*(6-1)`). `cond(XᵀWX+S)≈5000` (well-conditioned), converges in 9 IRLS iterations — the diff's small magnitude and its concentration in a handful of rows are consistent with the shared IRLS convergence floor (`gam_fit._IRLS_TOL=1e-10`), not a basis or assembly defect | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed identical below. `eta` is INDEPENDENT (`MULTITERM_CLAIM`, `gam_multiterm_conformance.py`): `fit_multiterm_case`'s signature takes only `RMultiTermRecipe`, which structurally excludes `eta`/`coef` (same ADR-193 mechanical-test-by-type discipline PR #202 established for `gam_family_conformance`). Confirms PLAN §6's registered-prediction spirit once more: the first multi-term assembly built from three independently-verified bases agreed with `mgcv` on the first measurement, no iteration needed — same shape of result as ADR-194's `cr` basis, ADR-195's family/link cases and ADR-205's `ti()` basis. |
| 2026-08-24 | 5 (remaining scope) | same hypothesis, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison, ADR-193 — same class as ADR-194/ADR-205's own tier-3 rows) | none — same two producers (`gam_multiterm_conformance.fit_multiterm_case`, `scripts/gam_multiterm_probe.R`), dispatched via CI `workflow_dispatch` on `39c49bf`; the new diagnostic step prints its report to stdout from the start (ADR-194's methodology fix, standard practice for every new probe since) | same metrics, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — `max_abs_eta_diff=1.242e-10`, `agrees=True`, `n=900`, `p=86` | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32722872476](https://github.com/jonathancrawford05/polaris-re/actions/runs/32722872476), R job ~36s + compare job ~40s | **CONFIRMED (parity) — settled, not tier-1-only.** `eta` — the only quantity `MULTITERM_CLAIM` declares — is INDEPENDENT, so this is genuine Stage-B parity for the epic's first multi-term mgcv-native model. Required levels 1-3 of the existing ten-cell suite also still agree on this run (`Required levels [1, 2, 3] all agree.`) — no regression from the workflow edit. Level 4 still DISAGREES (ADR-190's unchanged, unrelated `dw/drho` gap) and level 5 still AGREES. Closes slice 5's remaining-scope line in `docs/CONTINUATION_mgcv_parity_engine.md`. See ADR-206. |
| 2026-08-25 | 5b (`PolarisGAM`) | Work order §4's registered prediction: `PolarisGAM` (`gam_model.fit_polaris_gam`, assembling the SAME three-term formula from `ModelSpec` via the already-independently-verified `cr`/`by`/`ti` producers, selecting its own smoothing parameters via `select_lambdas_continuous`) selects `log10(sp)` within the ADR-199 2-block range (6.9e-04 to 9.8e-04) of `mgcv`'s own FREE-sp REML selection (`gam(method="REML")`, `scripts/gam_multiterm_free_sp_probe.R`) — the asymmetry vs ADR-206: `sp` moves from a shared input to a compared quantity here (work order §3) | wrote `gam_model.py` (`assemble_model_design`, generalizing `assemble_multiterm_design`'s arithmetic to any `ModelSpec`; `fit_polaris_gam`), `gam_model_conformance.py` (`FREE_SP_MODEL_CLAIM`, `fit_free_sp_case`, `compare_free_sp_case`) and `scripts/gam_multiterm_free_sp_probe.R` (same formula, `method="REML"`, no `sp=`); ran the probe and compared | `max_abs_eta_diff`, `max_abs_log10_sp_diff` (primary), `edf_total_diff`, `max_abs_term_edf_diff`, per block/term | ADR-199 (N=2, free sp): 6.9e-04 to 9.8e-04 | **PREDICTION REFUTED** — `max_abs_log10_sp_diff=0.7766` (three orders of magnitude above the N=2 range), concentrated in one block (the by-term: Python `log10(sp)=9.096`, mgcv `9.872`); `max_abs_eta_diff=3.677e-02`; `edf_total_diff=+0.7263` (Python 17.16 vs mgcv 16.44, ≈4%); both sides converged. Diagnosis (two diagnostic-only checks, not part of the committed comparison — see ADR-208): the shared, already-verified REML criterion scores Python's own optimum LOWER than mgcv's own selected point (611.892 vs 612.618), and starting the search AT mgcv's own point converges to yet a THIRD point, still lower-scoring than mgcv's exact selection — a flat REML surface along this direction (PLAN §5 risk 3, now measured rather than anticipated), not a criterion defect. PLAN §6's separate prediction ("edf agrees far better than sp does") holds again: 0.777 log10 units of sp disagreement vs ≈4% edf disagreement | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | REFUTED — confirmed identical below. `eta`/`log10(sp)`/`edf_total`/per-term `edf` are all INDEPENDENT (`FREE_SP_MODEL_CLAIM`, `gam_model_conformance.py`): `fit_free_sp_case`'s signature takes only `RFreeSpRecipe`, which structurally excludes every one of mgcv's own fit outputs, including `sp` itself (unlike `RMultiTermRecipe`, which carries `sp` as a shared input for the fixed-sp case). This is a genuine INDEPENDENT comparison that disagreed — a real result about λ selection at N=4, per ADR-193/the routine's own framing ("An INDEPENDENT comparison that DISAGREES is a SUCCESS"), not a defect in the criterion or the search, both unchanged and both still tier-3-verified at their prior scope. See ADR-208. |
| 2026-08-25 | 5b (`PolarisGAM`) | same hypothesis, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison that disagreed at tier 1, earning the tier-3 re-measurement per the routine's no-magnitude-carve-out rule, ADR-190 decision 5 — a formula/search question, not noise, regardless of size) | none — same two producers (`gam_model_conformance.fit_free_sp_case`, `scripts/gam_multiterm_free_sp_probe.R`), dispatched via CI `workflow_dispatch` on `c7c5ac6`; the new comparison step prints its report to stdout from the start (ADR-194's methodology fix, standard practice for every new probe since) | same metrics, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL IN VERDICT to tier 1, same order of magnitude on every metric** — `max_abs_eta_diff=3.775e-02`, `max_abs_log10_sp_diff=0.6398` (tier 1: 0.7766), `edf_total_diff=+1.1482` (tier 1: +0.7263), `max_abs_term_edf_diff=0.8776`, `at_bound=False`, `agrees=False` — last-bit-through-decimal differences from tier 1 consistent with the different mgcv release (1.9.1 vs 1.9.4) / BLAS the routine's tier discipline predicts, not a new finding | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32855338611](https://github.com/jonathancrawford05/polaris-re/actions/runs/32855338611), R job ~43s + compare job ~40s | **CONFIRMED (parity) — REFUTED, settled, not tier-1-only.** `eta`/`log10(sp)`/`edf_total`/per-term `edf` — every quantity `FREE_SP_MODEL_CLAIM` declares — are INDEPENDENT, so this genuine disagreement is a real result about λ selection at N=4, confirmed at both tiers. Required levels 1-3 of the existing ten-cell suite also still agree on this run — no regression from the workflow edit. Level 4 still DISAGREES (ADR-190's unchanged, unrelated `dw/drho` gap) and level 5 still AGREES. See ADR-208's tier-3 confirmation amendment. |
| 2026-08-25 | 5b follow-up (PR #212 review [P1]) | ADR-208's original diagnosis ("flat REML surface, no criterion gap") read only OUR OWN criterion at mgcv's point and Python's point, which cannot distinguish that from an `sp`-dependent criterion discrepancy — does `mgcv`'s OWN score rank the two points the same way ours does? | wrote `scripts/gam_multiterm_sp_delta_probe.R` (diagnostic-only, reproduces the free-sp probe's exact recipe, seed `20260825`; reads `mgcv`'s own `gcv.ubre` at its own free-sp optimum AND at Python's own tier-1 selected point via a fixed-sp refit); ran it with Python's tier-1 `log10(sp) = [6.75256951, 9.09550509, 3.09851263, 3.05446081]` | `delta_mgcv = score_at_mgcv_pt - score_at_python_pt` (mgcv's own criterion, both points), compared against `delta_ours = +0.725193` (already measured, ADR-208, our criterion, same two points) | n/a (first measurement of this quantity) | **OPPOSITE SIGN from `delta_ours`** — `score_at_mgcv_pt=2347.433463`, `score_at_python_pt=2347.554852`, `delta_mgcv=-0.121389`. Under `mgcv`'s own criterion, `mgcv`'s point is BETTER (lower); under ours, Python's point is better. Magnitudes also differ substantially (0.121 vs 0.725, ~6x) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | **REFUTES ADR-208's original diagnosis.** Neither side of `delta_mgcv` vs `delta_ours` is an INDEPENDENT comparison by itself (both quantities are computed by ONE producer reading a point the OTHER side selected — TRANSPORT-adjacent by construction, same status as `gam_deriv_probe.R`/`gam_vc_probe.R`'s diagnostics) — but the sign flip between two internally-consistent single-producer readings is real evidence that the two REML criteria disagree about which of these two points is better, on this N=4/`ti()`-sharing-a-span structure. This is a genuine reopened formula question (hypothesis (b) in ADR-208's amendment), not (only) optimiser path-sensitivity on a shared surface (hypothesis (a)). **Tier 1 — confirmed identical below.** See ADR-208's amendment for the full framing and the named next hypothesis (Wood 2011 §3.1's log-determinant machinery, previously ruled out for ADR-196's disjoint-support fixture). Slice 6 (`bs="sz"`) is BLOCKED until this is localised or closed (`docs/PLAN_mgcv_parity_engine.md`). |
| 2026-08-25 | 5b follow-up (PR #212 review [P1]) | same hypothesis, re-measured on the authoritative oracle (a diagnostic reading whose sign flip against `delta_ours` is large enough that BLAS/version noise cannot explain it, but earns the tier-3 re-measurement anyway per the routine's no-magnitude-carve-out rule, ADR-190 decision 5 — a formula question, not noise) | none — same script (`scripts/gam_multiterm_sp_delta_probe.R`), same hand-supplied `python_pt` (the tier-1 measurement's own `log10(sp)`, per the script's own documented convention — not re-derived from a tier-3 Python run), dispatched via CI on `fbf6770`; wired as a standing R-job diagnostic step, printed to `$GITHUB_STEP_SUMMARY` from the start | `delta_mgcv`, read directly from the step's own JSON output via `get_job_logs` | tier-1 reading above (`delta_mgcv=-0.121389`) | **IDENTICAL to tier 1 at every printed digit** — `delta_mgcv=-0.121389`. `mgcv`'s own ranking of the two points does not move between mgcv 1.9.1 (tier 1) and mgcv 1.9.4 (tier 3, the pinned production oracle) for this fixed pair of points | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32874213883](https://github.com/jonathancrawford05/polaris-re/actions/runs/32874213883), R job ~50s + compare job ~45s | **CONFIRMED — the sign-flip finding is settled as real, not a tier-1 or BLAS artefact.** Required levels 1-3 of the ten-cell suite also still agree on this run — no regression from the new diagnostic step or the `gam_model.PRODUCTION_LOG10_BOUNDS`/at-bound-raise fix landed in the same commit. **What this does NOT do:** localise the discrepancy further than "real, `sp`-dependent, on this N=4/`ti()`-sharing-a-span structure" — the next hypothesis (Wood 2011 §3.1's log-determinant machinery) is untested. Slice 6 (`bs="sz"`) stays BLOCKED until that happens. See ADR-208's tier-3 confirmation amendment. |
| 2026-08-25 | recalibration (maintainer-directed, PR #213) | Does the `sp`-dependent criterion discrepancy exist at FIXED `sp`? ADR-208's amendment measured it only at each side's own free-`sp` optimum, which cannot separate a criterion difference from an optimiser one. Evaluating BOTH criteria at the SAME fixed `sp`, at eight well-separated points, removes the optimiser entirely | wrote `scripts/gam_fixed_sp_score_probe.R` (emits `m$gcv.ubre` at eight supplied `sp` vectors) and `scripts/gam_fixed_sp_score_compare.py` (our `reml_score_general` at the same points, plus rank-at-tolerance and the analytic null-space correction) | the SPREAD of `ours - mgcv` across the eight points; the rank of `S = sum_j lambda_j S_j` at three tolerances | ADR-208: `delta_ours=+0.7252` vs `delta_mgcv=-0.1214`, opposite sign, mechanism unknown | **the difference is NOT constant** (spread 3.910776), so the discrepancy is in the CRITERION, not the search. Departures are discrete and occur only where the lambdas span many decades. True rank 81, stable at `1e-12` and `eps*p` everywhere; **79-80 at the shipped `1e-10`** at exactly the departing points. Correcting only that cut collapses the spread to 0.003281 (**1192x**) and removes ADR-208's ranking flip (corrected `delta_ours=-0.1211` vs `delta_mgcv=-0.1208`) | **tier 1**, R 4.3.3 / mgcv 1.9.1 (local apt) | **HYPOTHESIS — LOCALISED, not confirmed.** Wood (2011) section 3.1's "numerical zero leakage", and his own trigger condition (ratio of the dominant term's largest positive eigenvalue to a subordinate's smallest) is what the spread-lambda points hit; "the problem vanishes for a full rank S_1" explains why every flat-lambda point agreed. **Tier 3 outstanding** — PLAN slice 5c sequencing step 1. `1e-12` is NOT the fix (Anchor 8; Wood rules the tolerance approach out explicitly) |
| 2026-08-25 | recalibration (maintainer-directed, PR #213) | **Anchor 2's PRIMARY metric, measured for the first time.** ADR-206 and ADR-208 both scoped the MI contrast out as needing a pinned prediction grid. Correct for a grid, but not for the metric: `StudyYear_C` enters only through `s(AttdAge, by=StudyYear_C)`, linear in the by variable, so `eta(age, sy+1) - eta(age, sy) = f(age)` exactly — the contrast cancels the intercept, the reference smooth and `ti()` and collapses to the by-term's own smooth, available on the training rows | wrote `scripts/gam_mi_contrast_probe.R` (`predict(m, type="terms")[, by] / StudyYear_C`) and `scripts/gam_mi_contrast_compare.py` (our own fit, then `X[:, by] @ coef[by] / StudyYear_C`), at a SHARED fixed `sp` supplied to both sides so the metric is isolated from the free-`sp` gap | `mi_contrast` element-wise (PRIMARY, Anchor 2); `eta` element-wise (secondary) | never measured — zero occurrences in this ledger in six weeks | **MI contrast max abs diff 8.805e-13** (rms 2.378e-13); mean-centred 8.549e-13; **`eta` 3.544e-11** (rms 4.549e-12). Contrast range identical to every printed digit on both sides: `[-0.018087, 0.041162]` | **tier 1**, R 4.3.3 / mgcv 1.9.1 (local apt) | **HYPOTHESIS.** Both quantities INDEPENDENT (mechanical test on `gam_mi_contrast_compare.py`'s signature: it reads only shared inputs, never `payload["mi_contrast"]`). **PLAN section 6's registered prediction — "the MI contrast agrees better than eta does" — CONFIRMED**, by ~40x, and open since before slice 1. Measured on the TRAINING DESIGN, not a pinned grid: a partial delivery of Anchor 2's primary metric. **Tier 3 outstanding**; not a committed conformance case (no `VerificationClaim`) |
| 2026-08-25 | recalibration (maintainer-directed, PR #213) | **Defect B, prompted by the maintainer in one word: "Newton".** Wood (2011) eq. (4) builds the REML criterion on `H = -d2l/dbeta dbeta^T`, the OBSERVED Hessian that Newton-based PIRLS produces (section 3.2, weights carrying `alpha_i = 1 + (y-mu)(V'/V + g''/g')`). `gam_reml.reml_score_general:147` uses the EXPECTED (Fisher) weight -- Wood's `alpha_i == 1` -- and Wood flags exactly that substitution: the expected Hessian "gave worse performance than GCV when non-canonical links were used". Binomial's canonical link is logit, so binomial/cloglog is NON-canonical. `W` depends on `mu` hence `beta-hat` hence `sp`, so the discrepancy is `sp`-DEPENDENT | wrote `scripts/gam_hessian_weight_probe.py`: obtains the observed Hessian by central-differencing the per-observation deviance in `eta` (terms are independent, so all `i` differ at once), substitutes it into the score's `logdet_h`, and reports the spread of `ours - mgcv` under Defect-A-only versus both corrections | the SPREAD of `ours - mgcv` across the eight fixed-`sp` points, under each correction; plus the count of NEGATIVE observed weights | Defect A alone leaves a residual of 0.003281 (the row above) | **the Hessian term's own contribution has spread 0.003277 against that 0.003281 residual -- a match to four significant figures.** Applying both corrections collapses the spread to **0.000078**, a further ~40x and ~50,000x in total; the remainder is consistent with the finite-difference step rather than a third cause. **Negative observed weights: ZERO at every point** (minimum ~0.37) | **tier 1**, R 4.3.3 / mgcv 1.9.1 (local apt) | **HYPOTHESIS -- CONFIRMED as a second, independent defect.** A FORMULA error, of the class ADR-196/197 already fixed once in this same function, not a numerical one. **Corrects the prior session conclusion that "the fitter is not implicated"**: beta-hat IS fine (Fisher and Newton share a fixed point, differing in path -- `eta` agreed to 1.3e-09 at 12 decades), but the CRITERION needs a Hessian Fisher scoring never computes, so the score is wrong where the fit is right and no `eta` check could have found it. **Tier 3 outstanding.** The fix must derive `alpha_i` ANALYTICALLY per section 3.2 -- this probe's finite-difference `H` cannot demonstrate closure, its step error sits at the level being measured. Negative weights are absent on THIS data, not excluded in general (Wood: they "need not all be positive"), which is what keeps Appendix B's `E` justified. See PLAN slice 5c Defect B |
| 2026-08-29 | 5c | Wood (2011) Appendix B, implemented whole (`gam_reml_appendix_b.py`) and the analytic observed-Hessian weight (`Family.observed_information_weight`), wired into `reml_score_general` in place of the shipped `1e-10` cut and the plain Fisher weight -- does the PRODUCTION function (not a diagnostic replica) now reproduce `mgcv`'s fixed-`sp` score at the same 8-point fixture ADR-208's amendment and this slice's own recalibration measured? | changed `reml_score_general`'s signature to take `penalty_blocks`/`lambdas` (Appendix B needs the individual blocks, not a caller-summed matrix); `log|S|+` now calls `appendix_b_transform`; `log\|X'WX+S\|` now uses `observed_information_weight` in place of the Fisher weight | the SPREAD of `ours - mgcv` across the same 8 fixed-`sp` points, computed via the ACTUAL production `reml_score_general` (not a diagnostic script standing in for it) | raw/shipped: **3.910776** (this session's own re-measurement of the standing defect, identical to the recalibration note's reading) | **4.271e-07** -- a ~9.2-million-times reduction, at float round-trip precision. Individually: Defect A alone (Appendix B, Fisher weight retained) reduces it to 0.003281, matching the recalibration note's own tighter-cut reading to 4 decimal places; A+B together is the 4.271e-07 figure, ~230x further than the finite-difference cross-check's own 0.000078 could demonstrate | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt); **tier 3 confirmed same session**, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33267879635](https://github.com/jonathancrawford05/polaris-re/actions/runs/33267879635) -- see ADR-210 for the tier-3 spread value read from job-log stdout (`tee`'d per this row's own methodology fix, below) | **CONFIRMED (parity) -- both defects close the fixed-`sp` gap to float precision.** Declared as `gam_reml_optimize_conformance.FIXED_SP_MULTITERM_REML_CLAIM` (added in PR #215 review round 2, [P1-1]) -- NOT a reuse of ADR-196's `REML_SCORE_CLAIM`, which covers a different fixture/producer (2-block, binomial-logit, `paraPen`-supplied `score_reml_point`, versus this row's 4-block, binomial-cloglog, formula-built `penalized_fit_and_score`); the original entry here cited the wrong claim. Both quantities (`reml_score` spread and a `deviance` companion, added to the R probe's payload same round) are INDEPENDENT by the mechanical test on `penalized_fit_and_score`'s own signature. Required levels 1-3 of the ten-cell suite still agree on this run (no regression). **Methodology fix, filed same session:** the first dispatch (run 33267701996) wrote this step's output ONLY into `$GITHUB_STEP_SUMMARY` via a group redirect, repeating slice 2's own already-fixed job-summary-artifact limitation (row above, 2026-08-17) -- `tee`'d to a file and re-dispatched (run 33267879635) so the spread is read from job-log stdout directly, not inferred from a `continue-on-error` step's conclusion. |
| 2026-08-29 | 5c (free-`sp`, N=4) | With BOTH defects fixed, does `select_lambdas_continuous`'s free-`sp` selection on ADR-208's own N=4 structure now land closer to `mgcv`'s own free-`sp` selection? (PLAN slice 5c section 4's registered prediction) | none to the search itself -- re-ran `gam_model_conformance.fit_free_sp_case`/`compare_free_sp_case` against the identical `gam_multiterm_free_sp_probe.R` fixture, now scored by the fixed `reml_score_general` | `max_abs_log10_sp_diff` against `mgcv`'s own free-`sp` selection | ADR-208's pre-fix reading: **0.7766** (tier 1) / **0.6398** (tier 3) | tier 1: **0.7560** -- barely moved. **tier 3: 1.0996** -- WORSE than the pre-fix tier-3 reading, and materially different from this session's own tier-1 reading on nominally the same fixture (both readings independently re-drew the underlying synthetic data under `set.seed(20260825)`, so a same-seed draw under a different R/BLAS is not guaranteed identical -- itself informative given how sensitive this quantity already is, below). **The prediction is REFUTED at both tiers**, in the SAME direction (no improvement) it was refuted at pre-fix | tier 1 + **tier 3**, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33267879635](https://github.com/jonathancrawford05/polaris-re/actions/runs/33267879635) | **REFUTED -- and RE-DIAGNOSED, not merely re-confirmed.** The fixed-`sp` row above proves the criterion itself is now correct to float precision at BOTH sides' own optima. The discriminating follow-up (scoring mgcv's point and Python's own converged point under our OWN now-fixed criterion) shows mgcv's point scores measurably BETTER (612.611 vs 612.663, delta +0.052) -- an OPTIMISER CONVERGENCE finding, not a criterion one. See ADR-210 for the full argument and the registered next hypothesis (PLAN slice 5d). |
| 2026-08-29 | 5d (cheap step) | PLAN slice 5d's registered "cheap step": re-run the fixed-`sp` discriminating measurement (score both sides' free-`sp` points under both criteria) at tier 3, since ADR-210's own reading was tier 1 only, and refresh the stale Python free-`sp` point both discriminator scripts still carried from ADR-208 | refreshed `python_opt_log10` in `gam_fixed_sp_score_probe.R` and the `gam_multiterm_sp_delta_probe.R` workflow invocation from ADR-208's pre-Appendix-B reading to Python's CURRENT `fit_free_sp_case` selection; no new code | our criterion at both points (via `gam_fixed_sp_score_compare.py`, INDEPENDENT, `FIXED_SP_MULTITERM_REML_CLAIM`); `mgcv`'s own criterion at both points (via `gam_multiterm_sp_delta_probe.R`, DIAGNOSTIC -- mgcv computes both sides, at a point Python supplied) | ADR-210 tier-1-only reading: our criterion ranks mgcv's point better by +0.0523 | **CONFIRMED IDENTICAL at tier 3**: our criterion (INDEPENDENT) +0.05229 (612.61076 vs 612.66305), `mgcv`'s own criterion (DIAGNOSTIC) -0.052286 (`delta_mgcv`) -- both directions agree on sign and magnitude, both tiers agree to printed precision | tier 1 + **tier 3**, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33275225043](https://github.com/jonathancrawford05/polaris-re/actions/runs/33275225043) | **CONFIRMED, and hypothesis (b) (a residual sp-dependent criterion difference) is now RULED OUT entirely**: since the fixed-`sp` spread is 0 everywhere (ADR-210), any two criteria differing only by a constant cannot disagree about ranking two points -- the agreement here is mechanically guaranteed, not a new independent confirmation, but it closes off the one alternative to "this is purely an optimiser question" that remained open. See ADR-212. |
| 2026-08-29 | 5d (root cause) | Is the free-`sp` residual from an imprecise finite-difference gradient (hypothesis 1) or a genuinely multi-modal surface (hypothesis 2)? Interpolate our own criterion between the optimiser's converged point and mgcv's point; separately, measure the objective's own noise floor via a forward-difference step scan at one point | no code change for the diagnostic itself -- scored `penalized_fit_and_score` along the line between the two points (11 steps) and at one point across step sizes `1e-1` to `1e-10` | shape of the score along the interpolation (monotonic vs. barrier); forward-difference derivative estimate vs. step size at a fixed point | none (first measurement of this quantity) | **Interpolation: monotonic, no barrier** -- score falls smoothly from 612.663 (t=0) to 612.611 (t=1) at every one of 11 steps, refuting hypothesis 2 for this specific pair of points. **Noise floor: forward differences stable (0.231-0.236) for `h` in `[1e-5, 1e-1]`, break down at `h<=1e-9` (wrong sign at `1e-9`)**. SciPy's own L-BFGS-B default step (`eps=1.49e-8`) sits inside the broken region -- the true gradient at the "converged" point is `~0.55` (central-difference, stable `1e-2` to `1e-6`), nowhere near SciPy's own `gtol=1e-8` | tier 1 only (pure Python diagnostic, no `mgcv` involved in either measurement) | **HYPOTHESIS 1 CONFIRMED with a specific, measured mechanism; HYPOTHESIS 2 REFUTED for this pair of points.** `penalized_fit_and_score`'s nested IRLS solve only converges to `_IRLS_TOL=1e-10` relative on deviance, which sets a noise floor SciPy's default step is far inside of. See ADR-212 for the fix. |
| 2026-08-29 | 5d (fix) | Does deriving a finite-difference step from `select_lambdas_continuous`'s OWN measured noise floor (never from a comparison against `mgcv`) fix the spurious-convergence defect the row above found? | added `gam_reml_optimize._FINITE_DIFF_STEP = 1e-5` (two orders of magnitude above the measured stable/unstable boundary), wired via `options={"eps": ...}` into the one `scipy.optimize.minimize` call `select_lambdas_continuous` makes | `delta_mgcv` (mgcv's own criterion, mgcv's point vs. Python's default-start point); `max_abs_log10_sp_diff`, `max_abs_eta_diff`, `edf_total_diff` (`compare_free_sp_case`); score consistency across a 6-start scatter plus 2 adversarial starts | pre-fix: `delta_mgcv=-0.052286`; `max_abs_log10_sp_diff=0.7560`; `max_abs_eta_diff` not separately re-measured this session (ADR-208 pre-Appendix-B: 3.677e-02) | **`delta_mgcv` (DIAGNOSTIC, not independent -- mgcv computes both sides, at a point Python supplied) closes to +0.000668 (~78x tighter)** -- Python's default-start point is now essentially tied with mgcv's own optimum under mgcv's own criterion. **`max_abs_eta_diff` (INDEPENDENT, `FREE_SP_MODEL_CLAIM`) closes to 8.068e-04** and **`edf_total_diff` to -0.01838** (both far tighter than before). **`max_abs_log10_sp_diff` gets WORSE (0.8777)** -- localised entirely to the by-term's own lambda (component 2), which ranges `9.6`-`11` across converged runs while the fitted surface barely moves, i.e. this one dimension is weakly identified by the criterion on this fixture, not under-optimised. 6 of 8 tried starts (near the default or near either known point) now land in a `~0.001`-wide score band; the 2 adversarial starts (far outside the region either point sits in) still land poorly, as expected for a bounded search from an unreasonable start | tier 1, R 4.3.3/mgcv 1.9.1 (local apt); local test suite `uv run pytest tests/ -m "not slow"` unaffected (3504 passed / 3 skipped, identical to baseline), `tests/qa/` byte-identical (94/94) | **FIX CONFIRMED at tier 1 AND tier 3 on genuine INDEPENDENT evidence (`FREE_SP_MODEL_CLAIM`'s `max_abs_eta_diff`/`edf_total_diff`), corroborated by the diagnostic `delta_mgcv` reading; PARTIALLY MET on the registered `max_abs_log10_sp_diff` metric.** The finding survives without `delta_mgcv` at all: `max_abs_eta_diff` alone closes `3.677e-02 -> 8.068e-04` (tier 1) / `7.593e-04` (tier 3) (PR #216 review [P1-3] -- the fix's headline evidence is `FREE_SP_MODEL_CLAIM`, not the single-producer `delta_mgcv` diagnostic, which is corroborating, not primary). Tier 3 (run [33279785437](https://github.com/jonathancrawford05/polaris-re/actions/runs/33279785437)): `delta_mgcv=0.000668` (identical to tier 1 to every printed digit, diagnostic); `max_abs_eta_diff=7.593e-04` (tier 1: 8.068e-04, consistent, INDEPENDENT); `edf_total_diff=+0.0150` (tier 1: -0.01838, consistent small magnitude, INDEPENDENT); but **`max_abs_log10_sp_diff=0.2606` at tier 3 against `0.8777` at tier 1 -- a 3.4x swing on the nominally identical comparison**, while `eta` barely moves between the two runs. That instability is itself the decisive evidence for weak identifiability rather than a residual defect: a genuine criterion/optimiser bug would reproduce a STABLE gap across R builds the way the fixed-`sp` spread does (row above), not one that swings 3.4x while the fitted surface stays fixed. The metric itself is now understood to be the wrong one to chase to zero on this one weakly-identified dimension (PLAN section 6's own prior prediction, "edf agrees far better than sp does", now holds by a much wider margin, at both tiers). See ADR-212. |
| 2026-08-29 | 5d | Does the tier-1-vs-tier-3 inconsistency in the 2026-08-29 `5c (free-`sp`, N=4)` row (0.7560 vs 1.0996, "nominally the same fixture") trace to the data draw, or to something else? Re-drew the identical fixture fresh (same seed) and read `mgcv`'s own selection back -- if it matches ADR-208/210's own reading exactly, the draw is stable and the inconsistency is on OUR side | none -- re-ran `gam_multiterm_free_sp_probe.R` fresh, no code change | `mgcv`'s own `log10(sp)` | ADR-208/210: `[6.696, 9.872, 3.292, 3.029]` | **identical to the printed digit**: `[6.69596, 9.87212, 3.29184, 3.02918]`. The mgcv-side draw is perfectly stable | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | **CONFIRMED (harness) -- the data draw is not the source.** `mgcv`'s own selection is bit-stable across sessions; the inconsistency is entirely on the Python side, which the next two rows localise |
| 2026-08-29 | 5d | Given the draw is stable, does `select_lambdas_continuous`'s OWN result vary run to run on identical (data, code, starting point)? | none -- called `fit_free_sp_case` on the SAME freshly-drawn payload 3 times, separate process invocations, `OPENBLAS_NUM_THREADS` unset (this container's default) | Python's own converged `log10(lambda)` and REML score | n/a (first measurement of this question) | **bit-identical across all 3 runs**: `log10(sp)=[6.996, 8.773, 3.280, 3.053]`, `score=612.6759596253515`, to the last printed digit. Then varied `OPENBLAS_NUM_THREADS` on the SAME fixture: 1 thread -> `log10(sp)[by-term]=9.116`, score `612.663047` (= ADR-210's own tier-1 reading, to the digit); 2 threads -> `8.519`, score `612.673344`; 4 threads (this container's default) -> `8.773`, score `612.675960` (= this row's own tier-3-reproducing reading, `max_abs_log10_sp_diff=1.0996`, without touching R at all) | tier 1 (R side unused for this measurement beyond the one fixed payload above), pure-Python | **CONFIRMED (harness) -- localises the entire inconsistency to `select_lambdas_continuous`'s own sensitivity to OpenBLAS thread count** on this by-term-dominated direction. A companion check -- `penalized_fit_and_score` at `mgcv`'s own FIXED `log10(sp)` (no search) across the same thread counts -- moves by `~4e-10` (`612.6107604228214` vs `612.6107604232177`), consistent with ADR-210's float-precision fixed-`sp` finding regardless of threads. **The criterion is thread-independent; the SEARCH is not.** See ADR-211 |
| 2026-08-29 | 5d | Is `mgcv`'s own free-`sp` point a genuine, reachable optimum of our OWN (now-corrected) criterion, or is it somewhere our search structurally cannot reach (PLAN slice 5d hypothesis 2)? | wrote `scripts/gam_free_sp_warmstart_diagnostic.py`: re-run `select_lambdas_continuous` a second time, `x0` = `mgcv`'s own selected `log10(sp)` (a warm start), and compare its score/point against the existing blind (bounds-centre) start's own result, `OPENBLAS_NUM_THREADS=1` pinned for reproducibility | score and `log10(sp)` of the warm-start result vs the blind-start result, both Python-computed under the SAME (now-verified) criterion | blind start (ADR-210, 1 thread): `log10(sp)=[6.913, 9.116, 3.359, 2.972]`, score `612.663047` | **warm start converges to within `1e-6` of `mgcv`'s own point** (`log10(sp)=[6.69596, 9.872119, 3.291836, 3.029185]`) at score `612.610760` -- **0.052286 BETTER (lower) than the blind start's own result.** A blind, non-cheating multi-start check (bounds-centre + 8 uniform-random draws in the search box, no information from `mgcv`) reaches as low as `612.6149` in 9 tries but never matches the warm-start reading, and 2 of 9 far-corner starts fail to converge at all (`~636.7`, at a search bound) | tier 1 + **tier 3**, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33279913273](https://github.com/jonathancrawford05/polaris-re/actions/runs/33279913273) | **DIAGNOSTIC, not `FREE_SP_MODEL_CLAIM` parity evidence (ADR-193): the warm start's `x0` is `mgcv`'s own output, so this is TRANSPORT-adjacent by the mechanical test, same status as `gam_multiterm_sp_delta_probe.R`.** What it establishes is real regardless: **hypothesis 2 REFUTED at both tiers** (mgcv's point is reachable and better-scoring under our own criterion, not somewhere structurally unreachable — tier 3: warm start lands within `1.09e-4` of mgcv's point, score `612.610760`, `0.030862` better than a blind start that this time reports `converged=False`); **hypothesis 1 CONFIRMED at both tiers** (the blind default start's own convergence precision, on a direction weak enough that the thread-count noise above can move the landing point by most of a log10 decade, is the entire residual — and even with threads pinned identically, the SAME algorithm on two different hosts, tier 1 (this container) vs tier 3 (a GitHub Actions runner), reaches two different blind results, one of which fails SciPy's own convergence check, which is a second, independent confirmation that the search itself is the sensitive part). Slice 6 unblocked per slice 5d's own acceptance criterion. See ADR-211 |
| 2026-08-30 | 5d (PR #217 review [P1-1]) | With `OPENBLAS_NUM_THREADS=1` now pinned for the CI "compare" job (this PR's own fix, moved job-level in round 1's [P2-2]), what does the ALREADY-COMMITTED `FREE_SP_MODEL_CLAIM` comparison itself read at tier 3 -- does the confound's removal move the epic's own standing headline number, and if so is that a regression? | none to the comparator -- re-ran the existing `gam_model_conformance.compare_free_sp_case` path unchanged, only the environment's thread count changed (workflow-level `OPENBLAS_NUM_THREADS=1` at the time of this row, later re-scoped to job-level, same effective pin) | `max_abs_log10_sp_diff` (the claim's own primary metric, INDEPENDENT, unchanged provenance from ADR-208/210) | ADR-210's own standing tier-3 reading: **1.0996** (CI run 33267879635, threads unpinned) | **1.1536** (CI run 33279913273) -- WORSE in absolute terms than the unpinned reading, on the SAME blind-start code path this PR's own ADR-211 diagnoses as the artefact (this run's blind fit reports `converged=False`) | **tier 3**, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33279913273](https://github.com/jonathancrawford05/polaris-re/actions/runs/33279913273) | **INDEPENDENT provenance, unchanged from ADR-208/210 -- metric moved 1.0996 -> 1.1536, NOT closed, NOT a fresh regression -- see body.** (PR #217 review round 2 [P2-3]: an earlier revision of this cell opened "CONFIRMED (parity)", which is the ledger's vocabulary for a *result* and reads as agreement to a skimmer, when the row's own point is the opposite -- the number moved AWAY from zero.) The provenance claim is right -- `FREE_SP_MODEL_CLAIM`'s quantities are still genuinely INDEPENDENT, so this is not a harness row -- but what moved is which point on a poorly-converging blind search this particular CI run happened to land on, which is exactly `select_lambdas_continuous`'s own convergence-precision defect (ADR-211, the row above) rather than a new disagreement with `mgcv`. **A future session diffing this ledger against `1.0996` should read this row first** -- the PR review that caught this omission is the reason the row exists: a committed comparator's headline moving with no explanation is precisely what the ledger is for. Not closed by this row: the underlying `select_lambdas_continuous` convergence-precision defect itself, tracked as PLAN slice 5e. |
| 2026-08-30 | 5e | Does best-of-N multi-start (`select_lambdas_continuous_multistart`, new) measurably help `select_lambdas_continuous`'s own convergence at N=4 (the ADR-211/212 fixture) and at N>4 (a synthetic 8-block stress case)? **NO mgcv COMPARISON -- see verdict column.** | wrote `select_lambdas_continuous_multistart` (best-of-9, deterministic starts, `gam_reml_optimize.py`) and `scripts/gam_multistart_robustness_diagnostic.py`; ran both parts at `OPENBLAS_NUM_THREADS` in `{1, 4}`, pinned per-run via subprocess re-exec | single-start vs. best-of-9 REML score and `converged` flag, at N=4 (the committed near-flat fixture) and a synthetic N=8 (PART 1's own 3-term shape duplicated onto an independent covariate draw) | ADR-211 pre-fix: blind 9-start check reached 612.6149 vs single's 612.6630, 2/9 far-corner starts failed outright (no post-fix or N=8 reading existed before this row) | **N=4:** 1 thread -- single converged (612.610092), multi 612.610032 (gap 6e-5, consistent with ADR-212's "closed (mostly)"). **4 threads -- single FAILED TO CONVERGE (`success=False`, score 612.611509); multi-start (best-of-9) found the SAME converged, better point (612.610038) it found at 1 thread.** **N=8 (synthetic, no mgcv fit exists for this design):** both thread counts, single-start CONVERGED and multi-start matched or beat it by at most 1.5e-5 -- no failure, unlike N=4. Cost: multi-start ~9-17x a single search's own function evaluations (N=4: 1935-2190 vs 120-230; N=8: 5886-8874 vs 414-513) | pure Python, no R/mgcv anywhere -- N/A tier | **CONFIRMED (not a comparison -- see below).** This row measures Polaris's own search against itself; there is no second producer, so ADR-193's provenance axis (INDEPENDENT/ECHO/TRANSPORT) does not apply -- it is neither a parity nor a harness reading, just an internal robustness measurement, the same class ADR-211's own BLAS-thread table already was. **Real finding, both directions:** multi-start recovers a genuine, reproducible single-start convergence failure at N=4/4-threads (the slice's own acceptance criterion, met on the N=4 structure it was registered against, not literally "at N>4" as first framed); on the ONE N=8 stress structure tested (two covariate-DECOUPLED copies of the same shape), single-start already sufficed -- evidence the pathology does not always compound with block count, not proof it never does. A covariate-SHARING N>4 structure (closer to the target formula's own 13-21 blocks) remains untested. See ADR-213. |
| 2026-08-30 | 5e (PR #218 review) | Same question as the row above, at the diagnostic's own THIRD thread count (`_THREAD_SWEEP` already declared `{1, 2, 4}`, but the row above and ADR-213 published only 1 and 4 -- an automated PR review [P2] named the gap between what the script sweeps and what got reported) | none to the code -- ran `--worker 2` for both PART 1 (N=4) and PART 2 (N=8) | same metrics as the row above, now all three thread counts | row above: N=4 single scores {612.610092, 612.611509} (1, 4 threads only); N=8 single scores {621.069367, 621.070305} | **N=4, 2 threads: single-start CONVERGED (`True`) but still lands at the WORST of the three single-start readings** (612.611575) -- a "successful" SciPy termination is not itself evidence of landing near the true optimum on this surface. multi-start at 2 threads: 612.610032, identical to the 1-thread reading. **Full three-point spread: single-start 0.001483, best-of-9 0.000006 -- a 247x tighter reproducibility band**, the sharper form of the row above's two-point reading. **N=8, 2 threads: single and multi IDENTICAL to the printed digit** (621.069125), the same pattern as 1 thread; spread across all three threads is now single 0.001180 vs multi 0.001165 -- essentially EQUAL, not "an order of magnitude tighter" as the row above's two-point reading suggested (that comparison used only the single WORST single-start point, which happened to be a case multi-start also nearly matched) | pure Python, no R/mgcv anywhere -- N/A tier | **CONFIRMED (not a comparison -- same status as the row above).** Sharpens rather than reverses either finding: N=4's improvement is now a stated 247x spread reduction, not a single before/after pair; N=8's "single suffices" reading holds at a third, independently-measured thread count. See ADR-213 amendment 1 and PR #218's review response for the full corrected tables, including the DoD wording fix the same review raised. |
| 2026-08-31 | 5f | Does a covariate-SHARING N=8 structure (extra blocks reusing `AttdAge`/`PolYear` from the existing N=4 fixture, standing in for the target's own `sz(factor, AttdAge/PolYear)` terms) behave differently from ADR-213's own covariate-DECOUPLED N=8 case -- does single-start still suffice, or does sharing covariates reproduce the N=4 near-flat pathology? **NO mgcv COMPARISON -- see verdict column.** | wrote `scripts/gam_multistart_shared_covariates_diagnostic.py`: `ref`+`by`+`ti` (the N=4 fixture's own three terms) plus four `s(x, by=GroupA..D)` terms, two on `AttdAge` and two on `PolYear`, four INDEPENDENT binary indicators (a first two-indicator draft reusing one indicator across both variables was exactly rank-deficient by 2, SVD-confirmed -- see ADR-214); ran at `OPENBLAS_NUM_THREADS` in `{1, 2, 4}`, pinned per-run via subprocess re-exec, same methodology as the two rows above | single-start vs. best-of-9 REML score, `converged` flag and thread-to-thread spread, same shape as the two rows above | ADR-213's own two prior readings: N=4 spread 0.001483 (single) / 0.000006 (multi, ~247x tighter); N=8 decoupled spread 0.001180 (single) / 0.001165 (multi, ~unchanged) | **Single-start CONVERGED at all three thread counts (441/522/504 evals); multi-start (best-of-9, 4617-6444 evals) matches it EXACTLY (to the printed digit) at every one of the three** -- gap `0.000000` throughout, `starts[0]` (bounds-centre) is the winner every time. **Spread across threads: single-start `0.000656`, multi-start `0.000656` -- identical**, and the SMALLEST spread of any structure measured across slices 5e/5f (tighter than both of ADR-213's own readings, not between them) | pure Python, no R/mgcv anywhere -- N/A tier | **CONFIRMED (not a comparison -- same status as the two rows above).** ADR-213's own registered reading question -- "if this design's spread sits closer to N=4 than to decoupled N=8, that is evidence covariate-sharing drives the pathology" -- is answered in the negative: this covariate-sharing structure sits BELOW both prior readings, not between them, refuting for this specific construction the hypothesis that shared covariates (as built here) compound the near-flat weak-identifiability the N=4 fixture shows. One structural feature partially persists regardless: the MI by-term (block index 1) lands exactly on the search's own upper bound at 2 of the 3 thread counts (`log_lambda[1]=11.0` at 2 and 4 threads; at 1 thread it lands at `10.859083`, `at_bound=False` -- PR #219 review [P1-1], corrected from an earlier "at every thread count" overstatement), the same signature ADR-211/212 found in the N=4 fixture -- but here it does not propagate into whole-search score instability the way it does there. See ADR-214. |
| 2026-08-31 | 6 | Can a single-factor `s(fac, x, bs="sz", xt=list(bs="cr"))` design/penalty be re-derived independently of `mgcv:::XZKr` (a recursive column-major reshape loop, not transcribable under Anchor 8's licensing rule) and still agree with `smoothCon(bs="sz", absorb.cons=TRUE)` bit-for-bit, not merely up to an equivalent reparameterisation? | instrumented `mgcv:::smooth.construct.sz.smooth.spec` (dumped `object$X`/`object$S`/`object$base$S` pre-`smoothCon`-extras), derived closed-form `M = D kron I_k` (contrast against the last factor level) from that dump, then implemented `gam_basis_cr.sz_basis`/`build_python_sz_term` and `scripts/gam_term_extract.R`'s `extract_smooth_sz` (own internal `smoothCon` vs `lpmatrix`/`m$smooth[[1]]` guard, ADR-191's discipline) | `design_X`, every `penalty_S` block, `rank` -- Python: `gam_basis_cr.sz_basis`; R: `smoothCon(s(fac, x, bs="sz", k, xt=list(bs="cr")), absorb.cons=TRUE)` -- all three INDEPENDENT (`SZ_BASIS_CLAIM`) | n/a (first measurement -- no prior Python `sz` producer existed) | **Agrees on the first attempt**, three cases: synthetic 3-level factor (k=6), target `AttdAge` (k=13, 2 levels), target `PolYear` (k=6, 2 levels) -- `max_X_diff` 1.4e-14 to 2.9e-14, `max_S_diff` (every block) 4.4e-15 to 5.8e-15, `rank_diff` all-zero across every block | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt); **tier 3 CONFIRMED**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33353326193](https://github.com/jonathancrawford05/polaris-re/actions/runs/33353326193) | **CONFIRMED (parity), tier 1 AND tier 3 identical to the printed digit.** All three quantities INDEPENDENT (ADR-193), agreeing at float round-trip precision on the first measurement -- same shape of result as ADR-194 (`cr`) and ADR-205 (`ti`). Tier 3 reproduces tier 1 exactly: `max_X_diff` 2.854e-14/1.443e-14/1.699e-14, `max_S_diff` (every block) 5.107e-15/4.441e-15/5.773e-15, `rank_diff` all-zero, all three cases. Scope: single factor, no `id` -- every target `sz` term fits this. See ADR-215. |
| 2026-08-31 | 6b (`sz` Stage B) | does an `sz`-carrying multi-term model (`s(AttdAge,k=13,bs="cr")` + `s(FaceSize,AttdAge,k=13,bs="sz",xt=list(bs="cr"))`, binomial/cloglog, `ExposCnt` weights, fixed sp) fit by `gam_model.assemble_model_design` + `gam_fit.penalized_irls_general` agree with `mgcv`'s own native `gam()` fit of the identical formula on `eta`? First multi-term Stage-B measurement to include `sz` -- ADR-206 built the pattern for `ti`/`by`, this is the same pattern for `sz` | built `gam_model.assemble_model_design`'s `sz` dispatch (`TermSpec.n_levels`, `_build_term_extract`'s new branch calling `build_python_sz_term`), `gam_multiterm_sz_conformance.py` (`SZ_MULTITERM_CLAIM`, `fit_sz_multiterm_case`, `compare_sz_multiterm_case`) and `scripts/gam_multiterm_sz_probe.R` | `max_abs_eta_diff`, `n=700, p=26` | n/a (first measurement) | **3.921e-12 (tier 1) / 3.912e-12 (tier 3)**, `agrees=True` at both, no iteration needed | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt); **tier 3 CONFIRMED**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33393744694](https://github.com/jonathancrawford05/polaris-re/actions/runs/33393744694) | **CONFIRMED (parity), tier 1 AND tier 3 agreeing to the same order of magnitude.** `eta` is the sole quantity `SZ_MULTITERM_CLAIM` declares, INDEPENDENT (ADR-193) -- `fit_sz_multiterm_case`'s signature structurally excludes `mgcv`'s own `eta`/`coef`. Same shape of result as ADR-206's own first multi-term measurement (1.242e-10), agreeing on the first attempt. Required conformance levels 1-3 show no regression (both jobs completed successfully). Fixed sp only -- `select_lambdas_continuous` not yet exercised on an `sz`-shaped block structure. See ADR-216. |
| 2026-09-01 | 7 (Stage A) | Does `select = TRUE`'s extra null-space penalty follow one basis-agnostic rule (eigendecompose the sum of a term's own existing penalty block(s) at their natural, unscaled magnitude; the extra penalty is `U0 @ U0.T` for the eigenvectors below `numpy.linalg.matrix_rank`'s own tolerance), or does it need per-basis special-casing (`cr`, numeric-`by` `cr`, `ti`, `sz`)? | read `mgcv`'s own `gam(..., select=TRUE, fit=FALSE)$smooth[[i]]$S` structurally at tier 1 first (no fit needed -- `S` never depends on `y`), formed the hypothesis from that reading, then wrote `gam_select_penalty.null_space_penalty` and `scripts/gam_select_penalty_probe.R` (6 cases: `cr-ref-attdage-k13`, `cr-ref-polyear-k6`, `cr-by-mi-attdage-k13`, `ti-attdage-polyear`, `sz-facesize-attdage-k13`, `sz-facesize-polyear-k6` -- one per term archetype, at the target formula's own knots) | `max_abs_s_null_diff` per case -- Python: `gam_select_penalty.null_space_penalty` over each term's own already-independently-verified penalty block(s) (ADR-194/200/205/215); R: `gam(..., select=TRUE, fit=FALSE)$smooth[[i]]$S`, last entry -- both INDEPENDENT (`SELECT_PENALTY_CLAIM`) | n/a (first measurement -- no prior producer existed) | **Agrees on the first attempt, one rule, no per-basis special-casing**: `cr-ref-attdage-k13` 2.554e-12, `cr-ref-polyear-k6` 9.825e-15, `cr-by-mi-attdage-k13` 1.716e-12, `ti-attdage-polyear` 8.936e-12, `sz-facesize-attdage-k13` 1.899e-12, `sz-facesize-polyear-k6` 2.712e-14 -- all float round-trip precision. `null_dim` matched mgcv's own reported rank in every case (1 for the two `cr` reference terms and `ti`, 2 for the by-term and each `sz` case) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) -- tier 3 not yet dispatched this session | **HYPOTHESIS, tier 1 only.** Same shape of result as ADR-194 (`cr`)/ADR-200 (`by`)/ADR-205 (`ti`)/ADR-215 (`sz`): agrees on the first measurement, no iteration needed. The rule's basis-agnostic form (no per-basis branch in the implementation) is itself part of the finding, not merely a coding convenience. Needs tier-3 confirmation (CI dispatch, `mgcv-conformance.yml`'s new "Compare select=TRUE's null-space penalty" step) before this may be cited outside the session log. |
| 2026-09-01 | 7 (Stage B) | Does the SAME three-term model ADR-206 already verified at fixed sp (`s(AttdAge)` + `s(AttdAge,by=StudyYear_C)` + `ti(AttdAge,PolYear)`, binomial/cloglog, `ExposCnt` weights), now fit with `ModelSpec.select=True` (7 penalty blocks: each term's own existing block(s) plus the null-space block from the row above) via `gam_model.assemble_model_design` + `gam_fit.penalized_irls_general`, agree with `mgcv`'s own native `gam(..., select=TRUE, sp=sp_fixed)` fit of the identical formula on `eta`? | wired `ModelSpec.select`/`assemble_model_design`'s null-space append (`gam_model.py`), split `fit_polaris_gam`'s at-bound guard so only the LOWER bound raises by default (`strict=` param added, PR #212 review [P1-new] finally addressed -- a term shrunk to nothing under select=TRUE is not a defect), wrote `gam_select_multiterm_conformance.py` (`SELECT_MULTITERM_CLAIM`, `fit_select_multiterm_case`, `compare_select_multiterm_case`) and `scripts/gam_select_multiterm_probe.R` (same recipe shape as `gam_multiterm_probe.R`, `select=TRUE`, `sp` of length 7) | `max_abs_eta_diff`, `n=900, p=86`, 7 blocks -- Python: `gam_fit.penalized_irls_general` over `assemble_model_design(ModelSpec(..., select=True))`; R: `mgcv::predict(m, type='link')` on a native `select=TRUE` fit at the same fixed `sp` -- INDEPENDENT (`SELECT_MULTITERM_CLAIM`) | n/a (first measurement) | **6.164e-11**, `agrees=True`, no iteration needed -- same order of magnitude as ADR-206's own first multi-term reading (1.242e-10) and ADR-216's `sz` Stage-B reading (3.9e-12) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) -- tier 3 not yet dispatched this session | **HYPOTHESIS, tier 1 only.** Confirms the block ordering `select=TRUE` uses (each smooth's own existing-then-null blocks, concatenated across smooths in formula order) matches what `sp=` expects, verified structurally at tier 1 first (`/tmp/probe_select_order.R`, not committed -- a tier-1 structural check per `ROUTINE_MGCV_PARITY.md` step 2) before any Python code assumed it. Needs tier-3 confirmation before this may be cited outside the session log. Not yet built: free-`sp` selection under `select=True` (extending `select_lambdas_continuous` to the doubled/increased block count) -- registered as slice 7's own remaining scope, not attempted this session. |
| 2026-09-01 | 7 (Stage A) | same hypothesis, re-measured on the authoritative oracle per `ROUTINE_MGCV_PARITY.md` step 2 (a tier-1 structural claim about mgcv's own `select=TRUE` setup path is exactly the "version change is different code" case the routine names) | none -- same six cases, dispatched via CI `pull_request` trigger on this PR's own push | same metric, same six cases | tier-1 reading above (2.554e-12 / 9.825e-15 / 1.716e-12 / 8.936e-12 / 1.899e-12 / 2.712e-14) | **1.257e-12 / 3.092e-14 / 8.979e-13 / 2.195e-12 / 1.562e-12 / 3.000e-14 -- all six `agrees=True`**, identical in verdict and order of magnitude to tier 1 | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33417357327](https://github.com/jonathancrawford05/polaris-re/actions/runs/33417357327) | **CONFIRMED (parity), tier 1 AND tier 3 agreeing to the same order of magnitude.** `S_null` is INDEPENDENT (ADR-193/ADR-217). Required conformance levels 1-3 also re-confirmed AGREE on this run (no regression), level 4 still DISAGREES (ADR-190, unaffected), level 5 AGREES. |
| 2026-09-01 | 7 (Stage B) | same hypothesis, re-measured on the authoritative oracle, same reasoning as the row above | none -- same recipe, dispatched via CI `pull_request` trigger on this PR's own push | same metric (`max_abs_eta_diff`, `n=900, p=86`, 7 blocks) | tier-1 reading above (6.164e-11) | **5.691e-11**, `agrees=True`, identical order of magnitude to tier 1 | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33417357327](https://github.com/jonathancrawford05/polaris-re/actions/runs/33417357327) | **CONFIRMED (parity), tier 1 AND tier 3 agreeing to the same order of magnitude.** `eta` is INDEPENDENT (ADR-193/ADR-217), same shape ADR-206/ADR-216's own first multi-term measurements had. Both jobs (`mgcv reference (R)`, `Compare against the Python reference`) completed successfully; required conformance levels show no regression (same run as the row above). Not yet built: free-`sp` selection under `select=True` -- slice 7's own remaining scope. |
| 2026-09-01 | 7b | Does `fit_polaris_gam`'s own free-`sp` search (`select_lambdas_continuous`, single bounds-centre start, its module default) select all 7 `log10(sp)` values (the same `select=True` structure ADR-217/`SELECT_MULTITERM_CLAIM` verified at fixed `sp`) anywhere near `mgcv`'s own free-`sp` `select=TRUE` selection? Registered prediction: single-start underperforms `select_lambdas_continuous_multistart` at least as much as it did at N=4 (ADR-211/212/213), and the null-space blocks are the more likely place for a genuinely new disagreement. | wrote `scripts/gam_select_multiterm_free_sp_probe.R` (same shared recipe as `gam_select_multiterm_probe.R`/`gam_multiterm_free_sp_probe.R`, `select=TRUE`, `method="REML"`, distinct seed 20260902) and `gam_select_free_sp_conformance.py` (`SELECT_FREE_SP_MODEL_CLAIM`, `fit_select_free_sp_case`, `compare_select_free_sp_case`) | `max_abs_log10_sp_diff`, `max_abs_eta_diff`, `edf_total_diff`, `n=900, p=86`, 7 blocks -- Python: `gam_reml_optimize.select_lambdas_continuous` over `assemble_model_design(ModelSpec(..., select=True))`; R: `mgcv`'s own free-`sp` `select=TRUE` selection (`gam(..., select=TRUE, method="REML")`) -- INDEPENDENT (`SELECT_FREE_SP_MODEL_CLAIM`) | n/a (first measurement) | `max_abs_log10_sp_diff=5.132`, `max_abs_eta_diff=0.4456`, `edf_total_diff=+2.4216` (python edf_total 16.98 vs mgcv 14.56), `agrees=False`, `converged=True`. Worst block: `ti(...)`'s own null-space block (python 5.02 vs mgcv -0.11). | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) -- tier 3 dispatched this session, pending | **HYPOTHESIS, tier 1 only.** Registered prediction's first half CONFIRMED (single-start underperforms badly, worse in absolute terms than any N=4 reading); the null-space-block half is only provisionally supported here -- see the multistart row below, which refutes it once the optimiser is fixed. |
| 2026-09-01 | 7b | Same case, one change: `multistart=True` (`select_lambdas_continuous_multistart`, best-of-9, ADR-213) in place of the single bounds-centre start -- does this close the gap the way it did at N=4 (ADR-213/214)? | `fit_polaris_gam` gained an opt-in `multistart`/`n_starts` parameter (default `False`, every other caller unchanged) rather than reimplementing the assembly/scoring around `select_lambdas_continuous_multistart` a second time in the conformance module | same metric, same case, `multistart=True, n_starts=9` | tier-1 single-start reading above | `max_abs_log10_sp_diff=1.475` (3.5x tighter), `max_abs_eta_diff=0.00268` (166x tighter), `edf_total_diff=-0.111` (22x tighter), `max_abs_term_edf_diff=0.103`, `agrees=False` (still misses the 1e-2 `log10(sp)` gate), `converged=True`. Per-block: the null-space blocks (2, 4, 7) now agree to <0.01 -- the registered prediction's "null-space blocks are the likely culprit" clause is REFUTED; the residual moved to two of the three terms' own EXISTING blocks (1, 3), both being driven toward large `lambda`. | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) -- tier 3 dispatched this session, pending | **HYPOTHESIS, tier 1 only.** Large, real movement on `eta`/`edf`; `log10(sp)` residual persists but relocates -- consistent with PLAN section 6's own standing prediction ("edf agrees far better than sp does"). Not yet a closed gap; see the warm-start row below for the discriminating check. |
| 2026-09-01 | 7b | Warm-start diagnostic (TRANSPORT, never part of `SELECT_FREE_SP_MODEL_CLAIM` — `mgcv`'s own selection is the supplied `x0`): does `select_lambdas_continuous` started AT `mgcv`'s own free-`sp` point stay there, and does it score better under our OWN (ADR-217-verified) criterion than best-of-9 multistart's own blind result? Same discriminator ADR-211/212 used at N=4 (slice 5d). | new diagnostic script `scripts/gam_select_free_sp_warmstart_diagnostic.py`, no production code change | `warm_log10_sp` vs `mgcv`'s own sp; `warm_reml_score` vs multistart's own best score -- both TRANSPORT (the warm start's `x0` IS mgcv's output) | multistart's own score 523.6594 | warm-started fit stays at `mgcv`'s point (`max abs diff=0.00148`) at score **523.6453 -- 0.0141 BETTER than multistart's own best**, `converged=False` (gradient near-zero, consistent with a weakly-identified surface at that point). | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) -- tier 3 dispatched this session, pending | **DIAGNOSTIC, tier 1 only, never a parity claim.** Decisive for hypothesis (1) vs (2): `mgcv`'s own point is a REACHABLE, BETTER-scoring optimum of our own criterion than best-of-9 blind multistart reaches on this 7-block structure -- the same mechanism ADR-211/212 found at N=4 (optimiser convergence on a weakly-identified surface), now at a larger, harder scale. Not evidence of a formula or basis defect. |
| 2026-09-01 | 7b | same hypothesis (single-start), re-measured on the authoritative oracle per `ROUTINE_MGCV_PARITY.md` step 2 | none -- same recipe, dispatched via `workflow_dispatch` on this session's own branch | same metric (`max_abs_log10_sp_diff`, `max_abs_eta_diff`, `edf_total_diff`, `n=900, p=86`, 7 blocks) | tier-1 reading above (5.132 / 0.4456 / +2.4216) | **5.1320 / 4.456e-01 / +2.4216, `agrees=False`**, identical in verdict and to 4 significant figures to tier 1 | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33458654272](https://github.com/jonathancrawford05/polaris-re/actions/runs/33458654272) | **CONFIRMED (parity), tier 1 AND tier 3 agreeing to the same order of magnitude.** Every declared quantity is INDEPENDENT (`SELECT_FREE_SP_MODEL_CLAIM`, ADR-193/ADR-218). Required conformance levels 1-3 also re-confirmed AGREE on this run (no regression), level 4 still DISAGREES (ADR-190, unaffected), level 5 AGREES. |
| 2026-09-01 | 7b | same hypothesis (`multistart=True, n_starts=9`), re-measured on the authoritative oracle, same reasoning as the row above | none -- same recipe, same run | same metric | tier-1 reading above (1.475 / 0.00268 / -0.111) | **1.4754 / 2.681e-03 / -0.1106, `agrees=False`**, identical in verdict and to 4 significant figures to tier 1 | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33458654272](https://github.com/jonathancrawford05/polaris-re/actions/runs/33458654272) | **CONFIRMED (parity), tier 1 AND tier 3 agreeing to the same order of magnitude.** Same run as the row above; both jobs completed successfully. |
| 2026-09-01 | 7b | warm-start diagnostic (TRANSPORT, never a parity claim), re-measured on the authoritative oracle, same reasoning as the two rows above | none -- same recipe, same run | `warm_log10_sp` vs `mgcv`'s own sp; `warm_reml_score` vs multistart's own best score -- both TRANSPORT | tier-1 reading above (score gap +0.0141, max abs diff 0.00148) | **score gap (multistart - warm) = +0.014092, max abs (warm - mgcv) = 0.001314**, identical conclusion and to 3 significant figures to tier 1 | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33458654272](https://github.com/jonathancrawford05/polaris-re/actions/runs/33458654272) | **CONFIRMED (harness -- TRANSPORT, never a parity claim), tier 1 AND tier 3 agreeing.** Decisive for hypothesis (1) at both tiers: `mgcv`'s own point is a reachable, better-scoring optimum of our own criterion than best-of-9 blind multistart reaches. |
| 2026-09-01 | 7c (Part 0) | Is `SELECT_FREE_SP_MODEL_CLAIM`'s own `1e-2` gate on raw `log10(sp)` REACHABLE on the `select=TRUE` 7-block structure, or does the criterion not resolve some of those directions at all? ADR-218 attributed its `1.48` residual to optimiser convergence and named an analytic gradient as the next hypothesis; this row asks the prior question that decides whether that hypothesis is worth testing. | wrote `scripts/gam_select_free_sp_identifiability_diagnostic.py` (eigenspectrum + step-stability scan + per-block profile) and `src/polaris_re/analytics/gam_sp_identifiability.py`; NO production search path changed | REML-score Hessian eigenspectrum w.r.t. `rho` at `mgcv`'s own point, via `gam_uncertainty_conformance.finite_difference_rho_hessian` | n/a (first measurement of this question) | **5 identified directions of 7.** The two exceptions load on `b1` (`s(AttdAge)` existing) and `b3` (`s(AttdAge,by=StudyYear_C)` existing) -- **exactly the two blocks ADR-218 found the residual had relocated to** -- with raw eigenvalues `-0.0087` and `-0.0035` | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) -- tier 3 NOT dispatched, no production path changed | **MEASUREMENT (own criterion)** -- the third provenance category, codified at `docs/VERIFICATION_STANDARD.md` §2.1 by this row's own ADR. This is not a comparison: it is our OWN criterion's curvature, with `mgcv`'s selection as the POINT of evaluation, not an operand. There is no second producer to name, so neither `CONFIRMED (parity)` nor `CONFIRMED (harness)` applies -- forcing either would be a category error, and leaving the cell BLANK would be indistinguishable from a forgotten label. **Says nothing about agreement with `mgcv`, and can never carry a parity claim.** **Tier 1 only; may not be cited outside the session log without tier-3 confirmation.** |
| 2026-09-01 | 7c (Part 0) | Are those two negative eigenvalues a real SADDLE in our criterion, or a noise floor on a FLAT direction? A real curvature is stable as the finite-difference step shrinks; a noise floor divided by `h^2` grows like `1/h^2`. Same discipline ADR-212 used to find the finite-difference-step defect. | none -- diagonal second differences of the existing criterion at four steps | diagonal second difference per block at `h` in {0.2, 0.1, 0.05, 0.025} (natural-log rho) | the single-step reading in the row above | **FLAT, not a saddle.** `b1`: `-0.00088 / -0.00324 / -0.01369 / -0.08094`; `b3`: `-0.00020 / -0.00234 / -0.01042 / -0.02382` -- both grow like `1/h^2`. Identified blocks are stable to ~2% over the same 8x range (`b5`: `1.5793 / 1.5777 / 1.5738 / 1.5428`). Profile confirms in plain units: moving `b3` TWO decades up changes the score by `-0.0002`, against `~+1.0` for HALF a decade on `b5` | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | **MEASUREMENT (own criterion)** (see the row above; `VERIFICATION_STANDARD.md` §2.1). **DECISIVE for slice 7c: the `1e-2` gate is ILL-POSED on `b1`/`b3`** -- no optimiser, however exact, can pin a parameter the objective does not resolve across two decades. This is why slice 7c's Part 1 (the analytic gradient) was NOT built and was re-scoped to slice 7d. **Tier 1 only.** |
| 2026-09-01 | 7c (Part 2) | Given Part 0, which acceptance metric actually tracks the modelling agreement on this structure -- raw `max abs log10(sp) diff` (the committed gate), the Hessian-weighted distance, or the score gap? | none to any committed gate -- added `gam_sp_identifiability.hessian_weighted_distance`, verified against closed forms (identity Hessian -> Euclidean norm; diagonal -> hand-computed quadratic form; rotation invariance) | all three metrics, single-start vs `multistart=True, n_starts=9`, same case | ADR-218's own committed-gate readings (`5.1320` -> `1.4754`) | single-start `5.1320 / 7.9265 / 5.9595`; multistart `1.4754 / 0.0617 / 0.0141` -- **the committed gate credits multistart with 3.5x for a change the criterion-aware metrics score at 128x and 424x.** The recomputed score gap `0.014057` reproduces ADR-218's warm-start `0.0141` | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | **MIXED PROVENANCE BY COLUMN, which IS the finding.** `max abs log10(sp) diff` INDEPENDENT (unchanged from ADR-218); H-weighted distance **INDEPENDENT** -- the SAME two operands, re-normed by our own criterion's curvature; score gap **DIAGNOSTIC** -- our criterion evaluated AT `mgcv`'s supplied point, the same status ADR-218 gave its warm-start reading. **Recommended: the H-weighted distance, because it preserves INDEPENDENT provenance** while the sharper-looking score gap does not and must never become a gate. **RECOMMENDED, NOT TAKEN** -- re-gating is a maintainer decision. **Tier 1 only.** |
| 2026-09-02 | 7c (Part 0) | same question, re-measured on the authoritative oracle -- the slice 7c diagnostic was wired into `mgcv-conformance.yml` (ADR-219 amendment 1 decision 1) and ran on the next push. Does the eigenspectrum reading reproduce? | none to the diagnostic -- new CI step only | REML-score Hessian eigenspectrum, step-stability scan and per-block profile at `mgcv`'s own point | tier-1 reading: eigenvalues `-0.008687 / -0.003479 / 0.020204 / 0.443909 / 0.495709 / 1.209047 / 1.653181`, sign count "5 of 7" | **SUBSTANTIVE FINDING CONFIRMED, HEADLINE NUMBER REFUTED.** `mgcv`'s point identical (`b1` `10.2964`, `b3` `11.7046`); profile identical (`b3` +2 decades: `-0.000188` vs tier-1 `-0.000219`); step-stability calls `b1`/`b3` FLAT at both tiers. **But the eigenvalues are all POSITIVE at tier 3** (`+0.005624 / +0.012057 / +0.025660 / ...`), so the sign count reads **7 of 7**, not 5 of 7 | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8`, run [33633783477](https://github.com/jonathancrawford05/polaris-re/actions/runs/33633783477) | **MEASUREMENT (own criterion)** (`VERIFICATION_STANDARD.md` §2.1). **The gate is ill-posed — CONFIRMED at two tiers.** But "5 identified directions of 7" is **RETRACTED** (ADR-219 amendment 2): it counted the SIGN of a quantity already shown to be noise, and the sign flipped. `identified_direction_count` now REQUIRES an explicit `floor` so a sign count cannot be taken by accident; the robust statement is **2 of 7 directions carry no resolvable curvature**, by step-stability, at both tiers. |
| 2026-09-02 | 7c (Part 2) | same three candidate metrics, re-measured at tier 3 on the same run -- do the improvement ratios reproduce? | none | `max abs log10(sp) diff`, H-weighted distance, score gap; single-start vs `multistart=True, n_starts=9` | tier 1: single `5.1320 / 7.9265 / 5.9595`, multi `1.4754 / 0.0617 / 0.0141` (3.5x / 128x / 423x) | **RATIOS DO NOT REPRODUCE.** tier 3: single `4.6424 / 7.2440 / 5.9312`, multi `5.9517 / 2.2095 / 0.0227` -- i.e. **0.8x (WORSE) / 3.3x / 261x**. The Hessian, profile and `mgcv` point are stable across tiers, so the divergence is in OUR OWN free-`sp` search: ADR-211/213's BLAS-environment sensitivity, dominating the multistart row even with `OPENBLAS_NUM_THREADS=1` pinned on both sides | **tier 3**, R 4.6.1 / mgcv 1.9.4, same oracle and run as the row above | **MIXED PROVENANCE BY COLUMN (unchanged).** The tier-1 ratios `3.5x / 128x / 424x` are **WITHDRAWN as tier-1 artefacts**. The qualitative conclusion survives and is SHARPER: at tier 3 the committed gate calls multistart a **regression** (`4.64 -> 5.95`) on a change whose score gap closed **261x** -- a metric that reports a 261-fold criterion improvement as a regression is measuring the optimiser's stopping place, not the model. Note for slice 7e: the H-weighted distance itself moved 36x between tiers on this row, so it is a better metric and **not yet a demonstrably stable one**. |
| 2026-09-02 | 7c (Part 0/2) | Third reading of the same fixture: the push carrying ADR-219 amendment 2 re-ran the diagnostic on the SAME tier-3 oracle. Is the amendment-2 divergence a tier-1-vs-tier-3 difference, as amendment 2 claimed, or something else? | none -- same code, same oracle image, same pinned `OPENBLAS_NUM_THREADS=1` | eigenvalue-sign count, step-stability count, and the three Part 2 metrics | tier 1: sign 5 of 7, `5.1320`/`1.4754`; tier 3 run 1: sign 7 of 7, `4.6424`/`5.9517` | **AMENDMENT 2's ATTRIBUTION REFUTED.** tier 3 run 2: sign **6 of 7**; `max abs log10(sp) diff` `5.1320`/`1.4754` -- **identical to TIER 1 to the printed digit**, score gap `0.014071` vs tier-1 `0.014057`. The two tier-3 runs (same image, same pins) differ from EACH OTHER by four decades on the multistart row, more than tier 3 differs from tier 1. **Step-stability read 5 of 7 in all three readings**; the eigenvalue-sign count read 5, 7 and 6 | **tier 3**, R 4.6.1 / mgcv 1.9.4, same oracle, run [33636732346](https://github.com/jonathancrawford05/polaris-re/actions/runs/33636732346) | **MEASUREMENT (own criterion)** (`VERIFICATION_STANDARD.md` §2.1). The divergence is **RUN-TO-RUN non-reproducibility of our own free-`sp` search across CI runner instances**, not a version effect -- ADR-211/213's class one level deeper (they measured thread-count sensitivity; this is identical pinned config, different runner). Amendment 2's retraction of `3.5x/128x/424x` STANDS and is reinforced: those are one draw from a distribution. Vindicates making `floor` required on `identified_direction_count` (three readings, three counts). Filed as a promoted follow-up. |
| 2026-09-02 | 7c (Part 2) | Fourth reading of the same fixture. Amendment 3 attributed the Part 2 instability wholly to the search -- is that right, and is the H-weighted metric itself stable? | made `floor` REQUIRED on `hessian_weighted_distance` (it defaulted to `0.0`, clipping only NEGATIVES -- asymmetric); the diagnostic now DERIVES the floor from the step-stability scan (smallest resolved eigenvalue) and prints both columns | `max abs log10(sp) diff` and the H-weighted distance at `floor=0` vs a derived floor, across all four readings | amendment 3's reading: "one draw from a distribution" | **AMENDMENT 3 REFINED TWICE.** (a) `max abs log10(sp) diff` reads `5.1320`/`1.4754` in **three of four** readings -- the search is mostly deterministic with an INTERMITTENT excursion (run 1), which for a gate is worse than consistent noise, not better. (b) The H-weighted metric moved (`0.0617` / `2.2095` / `0.1758` / `0.1343`) **even between runs that selected identically** -- a second instability, in the METRIC. Root cause: clipping only negative eigenvalues keeps positive noise while discarding negative noise; at `floor=0` the metric moves **4.7x on the sign of noise alone**, and above the noise floor it is **identical across all four readings** | **tier 3**, R 4.6.1 / mgcv 1.9.4, same oracle, run [33639284944](https://github.com/jonathancrawford05/polaris-re/actions/runs/33639284944) | **MEASUREMENT (own criterion)** (`VERIFICATION_STANDARD.md` §2.1). The H-weighted distance is now **demonstrably stabilisable** with a derived floor -- materially better news for slice 7e than amendment 3 left it. The residual intermittent search excursion is a property of the SEARCH, filed separately. `floor` is now required on both functions in `gam_sp_identifiability`. |
| 2026-09-03 | 7d | Is the `dW/drho` term in the analytic REML gradient negligible (`alpha_i - 1` proportional to `(y_i - mu_i)`, mean ~0), or does it need `d(alpha)/d(eta)` derived? The cheap check the slice's own spec requires before deriving anything. | none to any committed path -- new module `gam_reml_gradient.py`, terms 1/2/4 only (no `mgcv` anywhere in this check) | analytic gradient (terms 1/2/4, `dW/drho` OMITTED) vs a trustworthy `h=1e-3` central difference of the profile score, refitting at each perturbed point, at `select_lambdas_continuous`'s own production-converged point on the N=4 fixture (`tests/fixtures/gam_reml_optimize_near_flat_direction.json`) | n/a (first measurement) | **max abs diff ~0.0199** (block 1); the exact term (measured next row) collapses this to `~1.3e-5`-`1.9e-5`, three orders of magnitude smaller | n/a -- pure Python self-consistency, no `mgcv` involved, no tier question applies | **MEASUREMENT (own criterion)** (`VERIFICATION_STANDARD.md` §2.1). **CHEAP CHECK FAILS: the term is not negligible.** `d(alpha)/d(eta)` needed deriving -- not skippable. Decisive, first measurement, no iteration needed to reach this verdict. |
| 2026-09-03 | 7d | With the exact `dW/drho` term (`dw_deta_observed`/`dw_drho_observed`, the full Wood Appendix D chain, `alpha` included) does the SAME cheap check close? | derived and composed `third_deriv_mu_eta`, `variance_second_deriv`, `dalpha_deta`, `dw_deta_observed`, `dw_drho_observed` (`gam_derivatives.py`) and `dlogdet_s_plus_drho` (`gam_reml_appendix_b.py`, from Appendix B's own `E` via economy SVD, never a naive eigen-cut on the raw summed `S`); `gam_reml_gradient.reml_score_gradient` assembles all four Wood eq. (4) terms | same metric, same point, same fixture | tier-1-free reading above (`~0.0199`) | **max abs diff ~1.3e-5 to 1.9e-5** -- three orders of magnitude below the established noise floor, at the IDENTICAL point that showed `0.02` before. A second, well-conditioned interior point (`log10(lambda)=[2,1.5,3,4]`) agrees to `~1e-6`. Every intermediate function (`third_deriv_mu_eta` … `dw_deta_observed`) independently verified against a central difference of the function one order below it, on all three link/family combinations, before composition. | n/a -- pure Python self-consistency, no `mgcv` involved, no tier question applies | **MEASUREMENT (own criterion)** (`VERIFICATION_STANDARD.md` §2.1). **CHEAP CHECK NOW CLOSES.** Registered prediction's first clause ("closes to at or below the objective's own noise floor") holds at the derivative level, before any production search was touched. |
| 2026-09-03 | 7d | Does wiring the exact gradient into `select_lambdas_continuous` (SciPy `jac=True`, opt-in `analytic_gradient=True`) close the `0.0141` score gap ADR-218's warm-start diagnostic found, at lower cost, on the `select=TRUE` N=7 structure -- and does `converged` stop disagreeing with a near-zero gradient? | `select_lambdas_continuous` gained `analytic_gradient: bool = False`; threaded through `select_lambdas_continuous_multistart`/`fit_polaris_gam`/`fit_select_free_sp_case` unchanged; `penalized_fit_score_and_gradient` (new) supplies one fit for both score and gradient | `eta`/`log10(sp)`/`edf_total` (INDEPENDENT, `SELECT_FREE_SP_MODEL_CLAIM`, same comparator as ADR-218/219, `analytic_gradient` a new parameter not a new claim), plus total nfev and the TRUE gradient norm at SciPy's own reported minimum (`MEASUREMENT (own criterion)`) | ADR-218/219 tier-1 single-start `5.132/0.4456/+2.4216` (312 nfev), multistart `1.475/0.00268/-0.111` (4600 total nfev), warm-start score gap `+0.0141` | single-start analytic: `1.7036/0.0529/+0.1904` (42 nfev, converged=True, true `\|grad\|`=3.067 -- SEE VERDICT); multistart(9) analytic: `5.1929/0.0055/-0.2583` (549 total nfev, score gap `+0.0180` vs mgcv, true `\|grad\|`=0.0150); warm-start (TRANSPORT) analytic: score `523.645314` vs mgcv's own `523.645336` (gap `-0.0002`), `max abs log10(sp) diff=0.0010`, true `\|grad\|`=0.0014 | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | **HYPOTHESIS (parity rows), tier 1 only; MEASUREMENT (own criterion) for the true-gradient-norm/converged reading.** Registered prediction's first clause (score gap closes to at or below noise floor) **HOLDS**: warm-start reaches `mgcv`'s own quality (tightest reading this epic has produced on this structure), multistart(9) analytic matches the finite-difference default's own score gap at **8.4x fewer function evaluations** (549 vs 4600). Second clause (`converged` stops disagreeing with near-zero gradient) **REFUTED BY A NEW MECHANISM**: the blind single-start run reports `converged=True` via SciPy's own `ftol`-style rule (`message`: `RELATIVE REDUCTION OF F <= FACTR*EPSMCH`) while its TRUE gradient (the exact one, not finite-differenced) has norm `3.067` on directions not pinned at the search's upper bound (two of seven blocks ARE pinned there at this point). Different from ADR-212's finite-difference-noise defect (that supplied a NOISY gradient near a genuine optimum; this supplies the EXACT gradient and the optimiser still exits early). Filed as PLAN slice 7f, not fixed this session. |
| 2026-09-03 | 7d | same hypothesis, re-measured on the authoritative oracle -- three earlier dispatch attempts this session stalled on CI/runner contention (unrelated to this session's own code, timeline in ADR-220 amendment 1); a fourth, after the colliding runs were cancelled, completed normally | none -- same recipe, dispatched via the automatic `pull_request` run this PR's own push triggered | same metrics: `eta`/`log10(sp)`/`edf_total`/per-term-`edf` (`SELECT_FREE_SP_MODEL_CLAIM`); total nfev, REML score, true `\|grad\|`, `converged`, SciPy `message` (`gam_reml_gradient_diagnostic.py`, own-criterion) | tier-1 readings above | **IDENTICAL IN VERDICT to tier 1, several readings tighter.** `SELECT_FREE_SP_MODEL_CLAIM`: single-FD `424 nfev, eta=4.456e-01, log10sp=4.6424, edf=+2.4728`; multistart(9)-FD `4384 nfev, eta=5.388e-03, log10sp=5.9517, edf=-0.3101`; single-analytic `41 nfev, eta=4.013e-02, log10sp=1.7036, edf=+1.9610`; multistart(9)-analytic `462 nfev, eta=5.460e-03, log10sp=5.7950, edf=-0.2593` -- multistart(9)-analytic's `eta` (`5.460e-03`) now essentially MATCHES multistart(9)-FD's own (`5.388e-03`) at **9.5x fewer function evaluations** (462 vs 4384), tighter than tier 1's 8.4x. `gam_reml_gradient_diagnostic.py`: mgcv's own score `523.645331092`; warm-start reaches `523.645315` (gap `-0.000016`, TIGHTER than tier 1's `-0.0002` -- the tightest reading this epic has produced on this structure at EITHER tier); blind single-start analytic reports `converged=True` (`message`: `RELATIVE REDUCTION OF F <= FACTR*EPSMCH`) at true `\|grad\|=3.249347`, matching tier 1's `3.067` in order of magnitude and mechanism -- CONFIRMED REPRODUCIBLE, not a tier-1/BLAS artefact | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33766634959](https://github.com/jonathancrawford05/polaris-re/actions/runs/33766634959) | **CONFIRMED (parity) for the `SELECT_FREE_SP_MODEL_CLAIM` quantities (`eta`/`log10(sp)`/`edf_total`/per-term `edf`, all INDEPENDENT); CONFIRMED (own-criterion measurement) for the true-gradient-norm/`converged`/`message` reading -- tier 1 AND tier 3 identical in verdict.** Registered prediction's first clause HOLDS at both tiers (score gap closes; multistart(9)-analytic cost ratio even better at tier 3). Second clause REFUTED at both tiers by the identical NEW mechanism (SciPy `ftol` firing with a large true gradient on a bound-adjacent blind single start). Required conformance levels 1-3 also passed on this run (both jobs, all 27 steps in the compare job green) -- no regression. See ADR-220 amendment 1. |
| 2026-09-03 | 7d | An automated PR review (finding P1-1) noted run `33766634959`'s own run-level `conclusion` is `cancelled`, not `success` -- does a second, cleanly-`success` run on the same PR head reproduce the row above? | none -- same recipe; run 148 auto-triggered by this session's own docs-only propagation commit (`0787cbe`) via the `pull_request` trigger | same metrics as the row above, from run 148 | the row above (run 145, run-level `cancelled` despite all 27 steps `success`) | **`SELECT_FREE_SP_MODEL_CLAIM` table BIT-IDENTICAL to run 145 in every cell** (single-FD `424/4.456e-01/4.6424/+2.4728`; multistart-FD `4384/5.388e-03/5.9517/-0.3101`; single-analytic `41/4.013e-02/1.7036/+1.9610`; multistart-analytic `462/5.460e-03/5.7950/-0.2593`). `gam_reml_gradient_diagnostic.py`'s blind/multistart rows also bit-identical (nfev/score/true `\|grad\|` all match, including the slice-7f blind-analytic reading `41 nfev, 524.408084, true \|grad\|=3.249347`). Only the warm-start row moves: nfev `20` vs `23`, score `523.645313` vs `523.645315`, true `\|grad\|` `0.004842` vs `0.001461`, `MAX ABS log10(sp) diff` `0.001602` vs `0.001099` -- both order `1e-3`, both readings the tightest class this epic has produced, verdict unchanged either way | **tier 3**, R 4.6.1 / mgcv 1.9.4, same oracle digest, run [33768187631](https://github.com/jonathancrawford05/polaris-re/actions/runs/33768187631), run-level `status: completed, conclusion: success` (verified via the Actions API, not merely the step list) | **P1-1 FIXED, not reworded.** The `SELECT_FREE_SP_MODEL_CLAIM` parity table is now confirmed by TWO separately-dispatched tier-3 runs, one ambiguously-labelled (`cancelled` at run level despite every step succeeding) and one cleanly `success` -- both bit-identical. The warm-start row's small drift (order `1e-3`-`1e-5`) is attributed to cross-run floating-point non-associativity in `mgcv`'s own optimizer on different runner hardware (the same class ADR-219's amendments already documented for this search), not a defect, and changes no verdict. See ADR-220 amendment 2. |
| 2026-09-04 | 7e | Does re-gating `SELECT_FREE_SP_MODEL_CLAIM.agrees` on `eta`/`edf` (ADR-219 amendment 1 decision 4) instead of `log10(sp)` discriminate a real production choice, or does it merely pass everything the old gate failed? | `compare_select_free_sp_case` gained `agrees_log10_sp` (old gate, preserved) alongside a redefined `agrees` (new gate); `_AGREEMENT_TOLERANCE_ETA=2e-2` (new, derived from the best CONFIRMED-both-tiers reading with ~3.7x headroom, `compare_vc_case`'s own derivation style) and `_AGREEMENT_TOLERANCE_EDF=1.0` (reused verbatim from `gam_model_conformance`) | `agrees`/`agrees_log10_sp` on this session's own fresh tier-1 R dispatch, single-start vs `multistart=True, n_starts=9`, both FD gradient | n/a (first measurement of the new gate) | single-start: `eta=0.4457, edf=+2.4216, agrees=False, agrees_log10_sp=False`; multistart(9): `eta=0.00268, edf=-0.1106, agrees=True, agrees_log10_sp=False` -- the plain default STILL fails the new gate by >20x on eta, same margin as the old one; only multistart passes | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt), fresh dispatch this session -- tier 3 pending on this PR's own CI run | **CONFIRMED (parity) for `eta`/`edf` (INDEPENDENT, unchanged provenance from `SELECT_FREE_SP_MODEL_CLAIM`); the gate change itself is a criterion redefinition, not a new comparison.** Discriminates correctly: does not pass the module's own default search configuration. |
| 2026-09-04 | 7e | Re-stating every prior committed slice 7b/7d reading (single/multistart x FD/analytic gradient, tier 1 and tier 3) under BOTH gates -- does the new gate ever pass a reading the old one also passed, or ever fail one the old one passed (would signal a loosened, not narrowed, claim)? | none -- pure arithmetic on already-published `eta`/`edf`/`log10(sp)` numbers from the slice 7b (ADR-218) and 7d (ADR-220) ledger rows, no new fitting | `agrees`/`agrees_log10_sp` computed from each already-committed row's own `max_abs_eta_diff`/`edf_total_diff`/`max_abs_log10_sp_diff` | n/a (first cross-tabulation) | `agrees_log10_sp` reads False on EVERY row at EVERY tier for EVERY search configuration ever run on this fixture (8 rows) -- the old gate was never once satisfied here. `agrees` reads True on both multistart rows at both tiers (FD and analytic gradient, 4 of 8 rows), False on both single-start rows at both tiers (4 of 8) | tier 1 AND tier 3 (reusing ADR-218/ADR-220's own committed readings at both tiers) | **CONFIRMED (parity).** The new gate is satisfied by exactly the readings that used the maintainer-recommended production configuration (`multistart=True`), at both tiers -- it never passes a reading the old gate also failed to discriminate against, and never fails a reading the old gate passed (the old gate passed nothing on this fixture, ever). No evidence of loosening. |
| 2026-09-04 | 7e | Is the H-weighted companion distance (slice 7c, `gam_sp_identifiability.hessian_weighted_distance`) sensitive to WHICH endpoint's Hessian weights it -- `mgcv`'s point (the only one measured through slice 7d) or each search's OWN converged point (never measured until now)? | `gam_select_free_sp_identifiability_diagnostic.py` section (5), new: repeats the eigenspectrum/step-stability derivation at each search's own point (`_step_stability`/`_derived_floor`, factored out of the existing section (2) logic, not duplicated) | H-weighted distance at `mgcv`'s point vs at each search's own point, same `d_rho` displacement, this session's own fresh tier-1 payload | H(mgcv pt) only (section (4), unchanged): single-start 7.9265, multistart(9) 0.0617 | H(own pt): single-start **1.6499** (4.8x SMALLER than at mgcv's point -- own floor -0.000959, 6 of 7 resolved), multistart(9) **0.3121** (5.1x LARGER than at mgcv's point -- own floor 0.0, 7 of 7 resolved) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt), fresh dispatch this session -- tier 3 pending | **MEASUREMENT (own criterion) -- not a comparison, carries no VerificationClaim** (VERIFICATION_STANDARD.md Sec 2.1). Real, non-negligible shift in BOTH directions -- no safe default endpoint exists; reported as both readings rather than assumed negligible, per ADR-219 amendment 1's own DoD for this slice. |
| 2026-09-04 | 7e | Correction, found while re-reading the slice 7c diagnostic against `VERIFICATION_STANDARD.md` Sec 2.1 for this slice: does the diagnostic's own section (4) header still label H-weighted distance `INDEPENDENT`, a label ADR-219 amendment 1 decision 2 superseded? | fixed the print statement in `gam_select_free_sp_identifiability_diagnostic.py` (no numeric change, label only) | n/a -- a label correction, not a measurement | `H-weighted : INDEPENDENT` (stale, pre-dates ADR-219 amendment 1 by one session) | `H-weighted : MEASUREMENT (own criterion)`, with a note explaining the correction and citing the ratifying decision | n/a -- documentation-only, no tier | **CORRECTED.** Caught by this slice's own provenance re-check before writing a new claim sentence that depends on getting this right -- exactly the kind of drift `VERIFICATION_STANDARD.md` Sec 1 warns a caveat does not survive unless it is structural. |
| 2026-09-04 | 7e | Re-measuring the H-weighted-at-own-point companion (row above) on the authoritative oracle -- does it confirm, and does section (5)'s new code survive an independently-converged multistart search path? | Fixed a real bug the tier-3 dispatch found: section (5)'s per-search loop lacked the `try/except PolarisComputationError` guard section (3)'s profile scan already carries, so a finite-difference-perturbed point near multistart's own converged rho that failed to converge crashed the whole diagnostic script (masked at the job level by the step's own pre-existing `continue-on-error: true`) | same metric, same fresh tier-3 payload (`gam_select_multiterm_free_sp_probe.R`'s own regeneration, run 33870429467) | tier-1 reading above: single-start 1.6499, multistart(9) 0.3121 (both `floor` derived) | single-start: **1.529435** (own floor 0.000000, 7 of 7 resolved) -- same order as tier 1, expected run-to-run search variability (ADR-219 amendments 3-4), NOT a formula disagreement. multistart(9): **CRASHED pre-fix** (`PolarisComputationError`, a perturbed point near multistart's own converged rho did not converge in 100 iterations) -- fixed, not yet re-dispatched | tier 3, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33870429467](https://github.com/jonathancrawford05/polaris-re/actions/runs/33870429467) | **PARTIALLY CONFIRMED.** Single-start's own-point reading: `MEASUREMENT (own criterion)`, tier 1 AND tier 3 agreeing in order of magnitude. **Multistart's own-point reading remains TIER-1-ONLY (`0.3121`) — the fix has not yet been re-dispatched to tier 3.** Per `ROUTINE_MGCV_PARITY.md` step 2, not promoted; ADR-221 amendment 1 records the full finding. |
| 2026-09-04 | 7e | Does the section (5) robustness fix hold, and is multistart's own-point non-convergence a real, reproducible property of this fixture rather than a bug -- re-dispatched after the fix | fixed `_step_stability`/Hessian loop wrapped in `try/except PolarisComputationError` per row above; no other change | same metric, same fresh tier-3 payload, second independent CI dispatch | pre-fix: CRASHED. Post-fix, run 33870429467-generation payload: single-start `H(own pt)=1.529435`, multistart `PolarisComputationError` (graceful) | **Confirmed reproducible on a SECOND, independent tier-3 dispatch**: single-start `H(own pt)=1.529435` (bit-identical to the first post-fix reading), multistart `non-convergent near this point: ... (deviance 987.13)` (bit-identical failure to the first post-fix reading -- same deviance to the printed digit). `SELECT_FREE_SP_MODEL_CLAIM` table bit-identical across both tier-3 runs, identical in verdict to tier 1 (not bit-identical -- corrected, P1-2 review finding) | tier 3, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [33871712927](https://github.com/jonathancrawford05/polaris-re/actions/runs/33871712927) | **CONFIRMED (own-criterion measurement).** The fix works (no crash, graceful report). The underlying non-convergence is a genuine, reproducible property of where `select_lambdas_continuous_multistart` converges on THIS oracle/runner class for this fixture -- not a code defect. Single-start's own-point reading is now CONFIRMED at both tiers (order-of-magnitude agreement, real search-path variability per ADR-219 amendments 3-4, not a formula gap). Multistart's own-point reading stays UNAVAILABLE at tier 3 -- characterised, not chased further, per the routine's own discipline. See ADR-221 amendment 2. |
| 2026-09-05 | 7f | Is ADR-220's `converged=True`-at-`|grad|=3.067` an `ftol` stopping-rule defect, as ADR-220 read it — and does any of its three named candidates close it? | added `gam_reml_optimize.projected_gradient` and `select_lambdas_continuous(max_gtol_restarts=...)`, both opt-in; NO default behaviour changed (verified bit-identical on the finite-difference path) | KKT projected gradient `max|g^P|` at the search's own reported point, and the REML score there | ADR-220's blind-analytic reading: score `524.788031`, raw `|grad|` `3.067233` | **Reproduced bit-identically.** The residual is in FREE directions (`max|g^P| = 2.086049`; the two upper-bound-pinned blocks project to exactly `0`), so the point is genuinely not KKT-optimal. **Candidate 1 REFUTED at the stall ADR-220 named**: `factr` at `1e7`/`1e2`/`1.0` gives identical `nfev=42`, score `524.788031`, residual `2.086049` — while a plain re-entry at the SAME `factr` improves the score by `1.110350`, so the exit is state-governed, not threshold-governed (re-measured after PR #228 review [P1-1]; the post-restart plateau refutes it independently too). **Candidate 2 PARTIAL**: score `524.788031 → 523.677681`, residual `2.086 → 0.489`, then stops. **Root cause is neither**: `penalized_irls_general` fails to converge at neighbouring points (`h=1e-5`, `t=1e-5`), so `_REJECTED_SCORE`'s `1e10` blocks the line search from descent that exists at `t=1e-1` (`-1.591e-02`) | tier 1, R 4.3.3 / mgcv 1.9-1 (local apt) — tier 3 not owed, see the verdict column | **`MEASUREMENT (own criterion)` — NOT a comparison, and no `mgcv` quantity appears in any reading above.** Every number is our own criterion, our own optimiser and our own inner fitter; `mgcv` enters only as the generator of the fixture's recipe (the same asymmetry `SELECT_FREE_SP_MODEL_CLAIM` already documents), never as an operand. There is no second producer to name, so neither `CONFIRMED (parity)` nor `CONFIRMED (harness)` applies (ADR-219 amendment 1 decision 2's ratified category). **ADR-220's registered prediction that candidate 2 closes the gap is REFUTED; the slice reports a partial mitigation as partial.** Re-aimed as slice 7g. |
| 2026-09-05 | 7f (amendment) | Under the maintainer's definition of convergence (a result reproducible within a contextually meaningful tolerance, on BOTH a cross-start and a cross-environment axis), does any shipped configuration converge on the `select=TRUE` N=7 structure? | none — measurement only; no production path changed | `max abs d eta`, `max abs d edf_total`, `max abs d log10(sp)` pairwise across 12 starting points, 10 multistart seeds, and thread counts {1,2,4}; benchmarked against ADR-221's own `eta < 2e-2` / `edf < 1.0` as a reference scale | first measurement of this question — ADR-219 amendment 3 measured `log10(sp)` only and recorded no `eta`/`edf` at all | **NO configuration passes both axes.** Single-start cross-start: `eta` `4.178` (FD) / `0.447` (analytic), `edf` `40.18` / `14.90` — FAIL. `multistart(9)` cross-seed: `eta` `6.319e-03`, `edf` `0.505` over 10 seeds — PASS at 3.2x/2.0x margin. `multistart(9)` cross-thread: `eta` `0.356`, `edf` `10.002` — FAIL, intermittently (2 of 4 seeds immune; 2 move the score by `+34.34` and `+5.93`). Single-start FD cross-thread: `eta` `4.650e-03` — PASS. **The inversion is the finding** | tier 1, R 4.3.3 / mgcv 1.9-1 (local apt) | **`MEASUREMENT (own criterion)`** — every reading is our own engine against itself under a perturbation; no `mgcv` quantity is an operand. **Establishes that `SELECT_FREE_SP_MODEL_CLAIM`'s passing reading holds under CI's pinned `OPENBLAS_NUM_THREADS=1` and is NOT established across environments** — a qualification, not a withdrawal. ADR-222 amendment 1. |
| 2026-09-05 | 7f (amendment) | Is environment-independent reproducibility achievable at all on this structure — i.e. is the target real? | none | `mgcv`'s own `log10(sp)`, `eta`, `edf_total` compared ACROSS `OPENBLAS_NUM_THREADS` {1,2,4} and a repeat run, same data, same seed | n/a (first measurement) | **BIT-IDENTICAL: `0.000000e+00` on every quantity**, including the smoothing parameters. `edf_total = 14.5624` in all four runs. Our own best seed reached `14.5613` at score `523.645058` against `mgcv`'s `523.645336` (ADR-220) — **the engine can reach the answer; it cannot be relied on to** | tier 1, R 4.3.3 / mgcv 1.9-1 (local apt) | **`MEASUREMENT (external reference, self-consistency)` — NOT a parity comparison and not evidence about Polaris.** It compares `mgcv` against ITSELF across environments, so there is no second producer and no Polaris operand; it establishes only that the target is achievable. Motivates slice 8 (the Wood-shaped outer solver). ADR-222 amendment 1. |
