# The `mgcv` conformance ledger

Append-only, one row per hypothesis tried (`docs/ROUTINE_MGCV_PARITY.md`). The `tier +
digest` column is not bookkeeping: without it, a row measured on local apt R and a row
measured on the pinned image read identically, and the ledger's whole purpose —
stopping session N+3 from re-running session N's dead end — depends on a later reader
being able to tell whether a "no movement" verdict was a real result or a tier-1
artefact.

**Provenance is required on every new row (ADR-193,
`docs/VERIFICATION_STANDARD.md`).** The `metric` column must name *what produced
each side* of every compared quantity, and the `verdict` column must say
`CONFIRMED (parity)` only when the two producers are independent — neither
reading the other's output, and neither having supplied the quantity to the
other. A row that cannot name two independent producers is a harness row: it
records a real check (no-tampering, or a working round trip) and its verdict
reads `CONFIRMED (harness)`.

**Every row currently in the table below predates the rule, and all of them are
harness rows.** Naming them here rather than leaving "the earlier rows" to the
reader is the point of ADR-193 — a caveat that attaches to nothing does not
travel, which is the failure these rows themselves illustrate:

- the four **2026-08-15 slice-1** rows read `**exact**` / `**agrees**` on the
  `raw`/`paraPen` path, where Polaris supplied the design and penalties that
  `mgcv` was fitted on and then read back — **ECHO** on `design_X`, `penalty_S`
  and `index_range`; only `rank` (`numpy.linalg.matrix_rank` vs `mgcv`'s own rank
  determination) is INDEPENDENT;
- the two **2026-08-16 slice-1b** rows read `**exact**` on the mgcv-native path,
  where `extract_smooth_terms` parses the R payload and is then compared against
  that same payload — **TRANSPORT** on every column. What those rows do establish
  is the R script's own internal guard (`smoothCon` vs `lpmatrix`/`m$smooth[[j]]`,
  ADR-191), which is independent *inside R*.

Neither set is basis-parity evidence. §5 of the standard carries the full audit,
including what the conformance levels (which ARE independent) prove.

| date | slice | hypothesis | the one change | metric | before | after | tier + digest | verdict |
|---|---|---|---|---|---|---|---|---|
| 2026-08-15 | 1 | `predict(gam(...), type="lpmatrix")`'s smooth-term block and `smoothCon(s(x, bs="cr"), absorb.cons=TRUE)$X` are the SAME object (PLAN §5 risk 1), not two competing Stage-A referents | added `scripts/smoothcon_lpmatrix_probe.R`, ran it against a fresh `bs="cr"` case (no exchange dependency) | `max_abs_diff_lpmatrix_vs_smoothcon_x`, `max_abs_diff_gam_smooth_S_vs_smoothcon_S` | n/a (first measurement) | **0.0 exactly**, 3 cases (k=8 default knots, k=13 default knots, k=8 supplied knots) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed below |
| 2026-08-15 | 1 | same hypothesis, re-measured on the authoritative oracle per `ROUTINE_MGCV_PARITY.md` step 2 (a tier-1 structural claim about which mgcv function calls which is exactly the "version change is different code" case the routine names) | none — same probe, dispatched via CI `workflow_dispatch` on `173d186` | same two metrics | tier-1 reading above | **0.0 exactly**, all 3 cases, identical to the tier-1 reading at every printed digit | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [31907362222](https://github.com/jonathancrawford05/polaris-re/actions/runs/31907362222) | **CONFIRMED — settled.** `smoothCon(absorb.cons=TRUE)` is Stage A's referent; recorded in `CONTINUATION_mgcv_parity_engine.md`. Required levels 1-3 of the existing suite also still agree on this run — no regression from the CI workflow edit. |
| 2026-08-15 | 1 | the R-side per-term extractor (`scripts/gam_term_extract.R`, reading `m$paraPen$S`/`$rank` and `predict(type="lpmatrix")` off the fitted object) and its Python comparator (`gam_stage_a.py`'s `extract_raw_terms`/`compare_term_extract`, reading the already-fitted `DesignExport`) agree term by term on the existing verified `raw`/`paraPen` basis — Anchor 1's "prove the harness on a known-good basis first" | wrote both scripts; ran the R extractor against `data/mgcv_exchange/synthetic` and compared to the Python side for designs `d1` (tensor only) and `d2` (tensor + factor block) | `max_abs_design_diff`, `max_abs_s_diff` (per penalty), `rank_diff`, index-range agreement, per term | n/a (first measurement) | **exact** — design diff ≤5e-16 (float round-trip noise, not a disagreement), S diff `0.0`, rank diff `(0, 0)`, index ranges agree, all 3 terms (`d1/tensor`, `d2/tensor`, `d2/factor:sex`) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed below. Caught one real bug in the R script's own harness proof: the JSON key for the factor term was the literal `"factor"` while its `label` field said `"factor:sex"` — fixed before this reading. |
| 2026-08-15 | 1 | same hypothesis, re-measured on the authoritative oracle (a structural claim about the harness's own extraction/serialization logic, not a numeric value near precision limits, but still "does this code path work correctly on this mgcv version" — earns the same tier-3 requirement as ADR-191) | none — same two scripts, dispatched via CI `workflow_dispatch` on `d005061` | same metrics, read as the CI step's own pass/fail rather than transcribed per-digit (see note) | tier-1 reading above | **agrees** — both the R extraction step and the Python comparison step completed with `conclusion: success` (the comparison step is `continue-on-error: true` and would show `failure` on any disagreement or exception; it did not) | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [31915145674](https://github.com/jonathancrawford05/polaris-re/actions/runs/31915145674), both jobs completed in 55s | **CONFIRMED — settled.** Note what this does NOT claim: read from the step conclusion, not the per-metric job-summary table (artifact `9254708641` on that run) — same honesty framing as ADR-189 amendment 2's "I read the gate's verdicts, not the per-metric values." Required levels 1-3 of the existing suite also still agree on this run. |
| 2026-08-16 | 1b | `gam_term_extract.R`'s new `smoothCon()` branch (work order §2), with its own internal consistency guard against `predict(type="lpmatrix")`/`m$smooth[[1]]`, and its Python counterpart `extract_smooth_terms` (packaging, not independent computation — ADR-192) agree on 3 isolated `bs="cr"` cases (default knots `k=8`/`k=13`, supplied knots `k=8`), same synthetic generation as `smoothcon_lpmatrix_probe.R` | wrote `extract_smooth_one` in the R script and `extract_smooth_terms`/knots-comparison in `gam_stage_a.py`; ran the R extractor and compared via `compare_term_extract` | `max_abs_design_diff`, `max_abs_s_diff`, `rank_diff`, `max_abs_knots_diff`, index-range agreement, per case | n/a (first measurement) | **exact** — all diffs `0.0`, rank diff `(0,)`, knots agree, all 3 cases (`default-knots-k8`: dim 200×7 rank 6; `default-knots-k13`: dim 400×12 rank 11; `supplied-knots-k8`: dim 200×7 rank 6) — matches ADR-191's own `bs="cr"` measurements exactly | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed below. **Caught one real bug on the first run**, exactly what Anchor 1's harness-first discipline is for: jsonlite's `auto_unbox` silently collapsed the single-penalty `rank = sm$rank` (a length-1 vector) to a bare JSON scalar, and the Python side's `for v in r_term["rank"]` raised `TypeError: 'int' object is not iterable`. The `raw` path never hit this because it always carries two penalties. Fixed with `rank = I(sm$rank)`, which forces jsonlite to keep it an array regardless of length. |
| 2026-08-16 | 1b | same hypothesis, re-measured on the authoritative oracle (a structural claim about the R script's own `stop()`-gated internal guard and the Python packaging's exception-freedom on the pinned mgcv version, the same class of claim ADR-191 and slice 1's tier-3 rows earned) | none — same two scripts, dispatched via CI `workflow_dispatch` on `7f06ebd` | R step: did `extract_smooth_one`'s internal guard (against `lpmatrix`/`m$smooth[[j]]`) `stop()` on any of the 3 cases? Python step: did `extract_smooth_terms`/`compare_term_extract` raise on the tier-3 payload? | tier-1 reading above | **agrees on both** — job 1's "Extract per-term Stage-A artefacts…" step (which runs the same `gam_term_extract.R` invocation, smoothCon branch included) completed `conclusion: success`, meaning none of the 4 `stop()` guards fired on any of the 3 smooth cases; job 2's "Compare the per-term extraction… (slice 1 / 1b)" step also completed `conclusion: success`, meaning `extract_smooth_terms`/`compare_term_extract` raised no exception reading the tier-3 payload | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [31946132947](https://github.com/jonathancrawford05/polaris-re/actions/runs/31946132947), both jobs completed in ~58s | **CONFIRMED for the R-side guard — settled**; **weaker confirmation for the Python comparison.** What this DOES claim: the R script's `stop()` guard is a genuine hard assertion (not a print), so its `success` conclusion on the pinned image is real evidence, same standing as ADR-191's own measurement on this exact check. What this does NOT claim: the per-metric diff table (`gam_term_extract_report.md`) was not read — this environment's egress policy blocks the Actions artifact blob-storage host (`*.blob.core.windows.net`), and the job summary is not exposed through the workflow-run/job API this session used, so the exact printed diffs are unread, unlike slice 1's tier-1 reading above where every digit is `0.0`. The Python comparison step's `success` conclusion is weaker evidence than the R step's — that script does not `sys.exit` on `any_smooth_disagree`, only on an exception — so it rules out a structural break but not, on its own, a printed nonzero diff. Given (a) the R-side hard guard passed at tier 3, (b) the tier-1 reading showed exact `0.0` on identical code, and (c) ADR-191 already established tier-1/tier-3 identity on this exact `bs="cr"` construction to every printed digit, a tier-3 value disagreement here would require new nondeterminism ADR-189 amendment 2's build-8 fix was meant to close — but this row records what was actually read, not what is inferred, per the routine's own discipline. |
| 2026-08-17 | 2 | `gam_basis_cr.py`'s Wood natural-cubic-spline construction (`cr_default_knots`, `cr_basis`, `absorb_sum_to_zero_constraint` — an independent Python implementation, never reading mgcv's output) equals `mgcv::smoothCon(s(x, bs="cr", k), absorb.cons=TRUE)` on `design_X`/`penalty_S`/`rank`/`knots`, for all 5 of `gam_term_extract.R`'s isolated `bs="cr"` cases (default knots k=8/k=13, supplied knots k=8, and — extending the harness's original 3 — PLAN §1's own target-formula knot vectors, `AttdAge` k=13 and `PolYear` k=6) — the epic's first INDEPENDENT Stage-A claim (ADR-193) and the first slice-2 measurement against the literal target knots rather than a stand-in | derived every formula from `mgcv`'s own R source (`smooth.construct.cr.smooth.spec`, `smoothCon`) read directly off the installed tier-1 package (`deparse()`), not guessed: default knots are `quantile(unique(x), seq(0,1,length=k))`; the identifiability constraint is `colMeans(X)` (not `colSums`) absorbed via a full QR of the constraint's transpose, confirmed `numpy.linalg.qr(mode="complete")` reproduces R's `qr()`/`qr.Q()` bit-for-bit on a single-column input; `smoothCon`'s OWN `scale.penalty=TRUE` (distinct from `gam.control(scalePenalty=)` used on the `raw` path) rescales `S` by `norm_1(S)/norm_inf(X)^2`, found by ratio when the unscaled penalty was off by a constant factor. Extended `gam_term_extract.R` to also export the covariate `x` (shared recipe, not a compared quantity) so Python can evaluate its own basis at the same points R's RNG drew; added an `x_range` parameter so the two target-knot cases draw `x` from inside `[knots[0], knots[-1]]` (extrapolation is explicitly unverified, module docstring) | `max_abs_design_diff`, `max_abs_s_diff`, `rank_diff`, `max_abs_knots_diff`, index-range agreement, per case | n/a (first measurement) | **exact** — all diffs ~1e-14 (float round-trip noise), rank diff `(0,)`, knots agree, all 5 cases (`default-knots-k8`: max X diff 8.4e-15, S diff 9.3e-15; `default-knots-k13`: max X diff 1.2e-14, S diff 1.3e-14; `supplied-knots-k8`: max X diff 7.6e-15, S diff 4.4e-15; `target-attdage-k13`: max X diff 1.5e-14, S diff 3.6e-15; `target-polyear-k6`: max X diff 1.4e-14, S diff 8.0e-15) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed below. `design_X`/`penalty_S`/`rank` are INDEPENDENT (`CR_BASIS_CLAIM`, `gam_stage_a.py`): neither `build_python_cr_term` nor `gam_term_extract.R`'s `smoothCon` branch reads the other's `X`/`S`/`rank` output, only the shared covariate `x`. **`knots` agreement is a separate recipe-consistency check, not part of `CR_BASIS_CLAIM`** (PR #201 review [P1], corrected same-day): it is a genuine independent computation only in the two default-knot cases (each side's own quantile placement); in the three supplied-knot cases neither side computes it, both relay the same hand-declared literal, which is ECHO — a single `knots` entry cannot honestly claim both, so it was removed from the claim rather than mistagged. A disagreement on `design_X`/`penalty_S`/`rank` would be a real result about the basis, not a broken round trip — this row records the first pass and it agreed, confirming PLAN §6's registered prediction ("Stage A on `cr` agrees exactly once knots match, and any disagreement is knot placement rather than the spline recursion") on the first measurement. |
| 2026-08-17 | 2 | same hypothesis, re-measured on the authoritative oracle (a genuine numeric parity claim, INDEPENDENT on every quantity — the class of claim ADR-193 exists to let this epic finally make) | none — same two producers (`gam_basis_cr.py`'s `build_python_cr_term`, `gam_term_extract.R`'s `extract_smooth_one`), dispatched via CI `workflow_dispatch` on `6e619ce`; the first dispatch's per-term comparison step wrote its report only to the job-summary file (behind the blob-storage host this environment's egress policy blocks, same limitation slice 1b's tier-3 row hit) — fixed by also `print()`-ing the same report string to stdout (`7408ecc`), then re-dispatched, so this reading is the **actual per-metric table**, not a masked step conclusion | `max_abs_design_diff`, `max_abs_s_diff`, `rank_diff`, index-range agreement, knots agreement, per case, **read directly from job-log stdout** (`get_job_logs`), not inferred from a `continue-on-error` step's `conclusion` | tier-1 reading above | **exact** — all diffs ~1e-14 (float round-trip noise, same order as tier 1, last-bit differences consistent with the different mgcv version / BLAS the routine's tier discipline predicts), rank diff `(0,)`, knots agree, all 5 cases (`default-knots-k8`: max X diff 8.327e-15, S diff 9.548e-15; `default-knots-k13`: max X diff 1.213e-14, S diff 1.243e-14; `supplied-knots-k8`: max X diff 7.605e-15, S diff 4.441e-15; `target-attdage-k13`: max X diff 1.454e-14, S diff 3.553e-15; `target-polyear-k6`: max X diff 1.379e-14, S diff 7.994e-15) | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32033738454](https://github.com/jonathancrawford05/polaris-re/actions/runs/32033738454), both jobs completed in ~56s | **CONFIRMED (parity) — settled.** `design_X`/`penalty_S`/`rank` — every quantity `CR_BASIS_CLAIM` actually declares — are `INDEPENDENT`, so this is genuine Stage-A basis parity on those three, not a harness check — the first row in this ledger entitled to that verdict. `knots` agreement is reported alongside (all 5 cases agree) but, per the same-day correction above, is a recipe-consistency check outside the claim, not a fourth parity column. Required levels 1-3 of the existing suite also still agree on this run (no regression from the workflow edit). **What this ALSO settles, as a methodology fix:** slice 1b's tier-3 row (above) could only confirm "the comparison step raised no exception," because `continue-on-error: true` makes a step's API-visible `conclusion` read `success` whether the script actually agreed or silently disagreed without exiting non-zero, and the per-metric job-summary artifact sat behind a blocked host. Printing the identical report string to stdout put the real numbers in plain job-log text, which `get_job_logs` reads without touching the blocked host — closing that limitation for every later slice's tier-3 reading, not only this one. |
| 2026-08-17 | 3 | `gam_fit.penalized_irls_general` (an independent Python IRLS implementation for an arbitrary family/link, reading only the shared recipe — `x`, `s`, `y`, `weights`, `offset`, `sp` — off `scripts/gam_family_probe.R`'s payload, never its `eta`/`coef`/`dispersion`) reproduces `mgcv::gam(family=..., weights=..., paraPen=list(X=list(S, sp=sp)))` on `eta` (all 4 cases) and `dispersion` (the 1 case mgcv estimates it, quasi-Poisson), at a fixed `sp` on a shared design, for binomial logit/cloglog with prior weights, quasi-Poisson, and Poisson with a log offset (PLAN slice 3) | wrote `gam_family.py` (Family/Link abstraction, standard GLM IRLS theory per Wood §3.1.2 — no R-source archaeology needed, ADR-195 decision 2), `gam_fit.py` (`penalized_irls_general`, `effective_degrees_of_freedom`, `pearson_dispersion`) and `gam_family_conformance.py` (`FAMILY_CLAIM`, `fit_family_case`, `compare_family_case`); ran `scripts/gam_family_probe.R` against a deterministic shared `(X, S)` it builds itself (`set.seed(20260817)`, ADR-074) and compared | `max_abs_eta_diff` per case, `dispersion_diff` (quasipoisson-log only) | n/a (first measurement) | **agrees, all 4 cases** — `binomial-logit`: eta diff 1.998e-15; `binomial-cloglog`: eta diff 1.488e-14; `quasipoisson-log`: eta diff 7.994e-15, dispersion diff 9.671e-06; `poisson-log-offset`: eta diff 9.326e-15 | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed below. `eta`/`dispersion` are INDEPENDENT (`FAMILY_CLAIM`, `gam_family_conformance.py`): `fit_family_case`'s signature takes only the shared recipe, never the R script's own fit output (ADR-193's mechanical test). Coefficients are NOT compared, per Anchor 2 (never a Stage-B acceptance criterion). Confirms PLAN §6's registered-prediction spirit — the recursion agreed once family/link/weights matched, no iteration needed, same shape of result as ADR-194's `cr` basis. |
| 2026-08-17 | 3 | same hypothesis, re-measured on the authoritative oracle (a genuine INDEPENDENT parity claim, same class ADR-193 exists to let this epic make) | none — same two producers (`gam_fit.penalized_irls_general`, `scripts/gam_family_probe.R`), dispatched via CI `workflow_dispatch` on `90cbdba`; the comparison step `print()`s its report to stdout from the first version (the methodology fix slice 2 needed is now standard practice for every new probe), so this reading is the actual per-metric table via `get_job_logs`, not a masked step conclusion | same metrics as the tier-1 row above, read directly from job-log stdout | tier-1 reading above | **agrees, all 4 cases** — `binomial-logit`: eta diff 1.221e-15; `binomial-cloglog`: eta diff 1.488e-14; `quasipoisson-log`: eta diff 8.438e-15, dispersion diff 9.671e-06; `poisson-log-offset`: eta diff 9.326e-15 — same order as tier 1 at every case, dispersion diff identical to the last printed digit | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32057694949](https://github.com/jonathancrawford05/polaris-re/actions/runs/32057694949), both jobs completed in ~52s | **CONFIRMED (parity) — settled.** `eta` and `dispersion` — every quantity `FAMILY_CLAIM` declares — are `INDEPENDENT`, so this is genuine Stage-B parity across family/link/weight, the epic's first outside the already-verified Poisson case. Required levels 1-3 of the existing ten-cell suite also still agree on this run (`Required levels [1, 2, 3] all agree.`) — no regression from the workflow edit. See ADR-195. |
| 2026-08-17 | 3 | (PR #202 review [P2]) the `quasipoisson-log` dispersion diff (9.671e-06, bit-identical at both tiers) is `mgcv`'s `sig2` formula, not float noise or a bug in `gam_fit.pearson_dispersion` — is `pearson_rss/(n-edf)` (textbook Pearson dispersion, using `mgcv`'s OWN `edf` and fitted values) the wrong denominator, or is `mgcv`'s `m$sig2` itself not that formula? | none — a diagnostic-only tier-1 R script re-fitting the exact `quasipoisson-log` case and comparing `m$sig2` against `pearson_rss/(n-edf)`, `deviance/(n-edf)` and `pearson_rss/(n-rank(X))`, plus reading `mgcv:::gam.fit`'s source for how `G$sig2`/`magic()`'s `scale`/`gcv` arguments interact across IRLS iterations | `m$sig2` vs three candidate formulas | n/a (diagnostic) | **none of the three candidate formulas reproduce `m$sig2` exactly**: `pearson_rss/(n-edf)` off by 9.6709e-06 (the observed residual, bit for bit), `deviance/(n-edf)` off by 1.5299e-06 (closer, still not exact), `n-rank(X)` denominator also does not match. Source reading: for a scale-unknown family, `gam.fit`'s IRLS loop calls `magic(..., scale=G$sig2, gcv=(G$sig2<0), ...)`, and `G$sig2` is negative only on the FIRST call — every subsequent call passes `gcv=FALSE` (UBRE mode), under which `magic()` returns the SUPPLIED scale rather than a fresh GCV/Pearson recomputation (`?magic`: "scale: the estimated (GCV) or supplied (UBRE) scale parameter") | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt), diagnostic script not committed | **CHARACTERISED, not fully traced.** The evidence is consistent with `m$sig2` being the Pearson-dispersion estimate from an IRLS iteration at or near convergence but not necessarily the literal final one — a deterministic function of the fitting trajectory (explaining the cross-tier bit-stability) rather than a different formula in the ordinary sense. Full confirmation would need instrumenting `gam.fit`'s loop directly; not attempted (CLAUDE.md: mark the uncertainty rather than push a hypothesis past what was shown). **Decision (ADR-195 amendment): keep the textbook Pearson formula** rather than chase whichever candidate numerically landed closer, which would be tuning to a possibly-transient `mgcv` implementation artifact, not deriving the correct quantity (Anchor 8). The 1e-4 relative tolerance is unchanged and is now known to be absorbing a real, understood-in-shape ~0.2% discrepancy rather than pure headroom — a future dispersion diff materially larger than ~1e-5 on a similarly-conditioned case is a new finding worth the fuller trace this row stopped short of, not something to wave through under the same tolerance. |
| 2026-08-18 | 4 part A | `gam_reml.reml_score_general` (an independent generalization of the already-verified `experience_gam_penalized.reml_score` onto `gam_fit`'s general IRLS core — swaps only the deviance/working-weight source for a general `Family`, reduces bit-for-bit to the old score at `gamma=1` — superseded, see the RESOLUTION row below) reproduces `mgcv::gam(family=binomial(link='logit'), paraPen=list(X=list(S1,S2,sp=c(sp1,sp2))), method='REML')$gcv.ubre`'s PAIRWISE DIFFERENCE across three fixed `(sp1,sp2)` points on a shared two-block binomial/logit design — the first slice-4 measurement, and the first Stage-C REML-score comparison of any kind in this epic. **`deviance` is a second declared INDEPENDENT quantity** (`compare_reml_deviance`, added same day, PR #203 review [P1-1]), using the same independent fit `score_reml_point` scores | wrote `gam_reml.py` (the generalized score), `scripts/gam_reml_probe.R` (shared recipe, three points, `method="REML"` with a fully-supplied `sp` so no optimisation runs), `gam_reml_conformance.py` (`score_reml_point`/`deviance_reml_point` take plain arrays + the `sp` setting, no R-payload-shaped argument at all — a stronger form of the ADR-193 mechanical test); ran the probe and compared PAIRWISE score differences (not the absolute value — ADR-189 amendment 1 already found an unresolved offset there for the single-block Poisson case; differencing cancels any purely additive offset) and per-point deviance (absolute — no offset to cancel there) | `python_diff - r_diff` (residual) per pair, tolerance 1e-6; `python_deviance - r_deviance` per point, same tolerance | n/a (first measurement) | **First isolated the fit**: `deviance` matches `mgcv`'s to ~1e-11 at all 3 points, `agrees=True` on every point (the fit is correct, matching slice 3's eta parity). **Then the score: ALL 3 pairwise differences DISAGREE** against the 1e-6 tolerance — `(1,1)-(5,0.2)`: residual -0.741273; `(1,1)-(0.5,8)`: residual 0.000935 (smaller than the other two by ~800x, but still ~935x the tolerance — NOT agreement); `(5,0.2)-(0.5,8)`: residual 0.742208. Internally consistent (the three residuals sum as pairwise differences of one function must), ruling out a comparison-arithmetic slip | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS ("pairwise differencing cancels any offset and reproduces mgcv's score shape") — **REFUTED on all 3 pairs** (varying magnitude), confirmed below; deviance hypothesis CONFIRMED. An INDEPENDENT comparison that disagrees (ADR-193): a real result about the naive multi-block `log|S_lambda|_+`/`log|X'WX+S|` generalization, not a fit bug. See ADR-196 for the (corrected, same-day, PR #203 review [P1-3]) localizing evidence and the named next hypothesis. |
| 2026-08-18 | 4 part A | same hypothesis, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison whose disagreement magnitude, ~0.74, is five orders of magnitude above BLAS/version noise — still earns the tier-3 re-measurement per the routine's no-magnitude-carve-out rule, ADR-190 decision 5, since a formula question is "different code," not noise, regardless of size) — **score only**, `deviance` had no committed producer yet at this run (see the next row) | none — same producer (`gam_reml.reml_score_general`, `scripts/gam_reml_probe.R`), dispatched via CI `workflow_dispatch` on `6564b79`; comparison step prints its report to stdout from the start (the methodology fix ADR-194 needed partway through slice 2, applied here from the first measurement) | `python_diff - r_diff` (residual) per pair, tolerance 1e-6 | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — same three score residuals (-0.741273, 0.000934569, 0.742208), `agrees=False` on ALL 3 pairs | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32086738495](https://github.com/jonathancrawford05/polaris-re/actions/runs/32086738495), both jobs completed in ~57s | **CONFIRMED (parity) on `reml_score_pairwise_diff` — the disagreement is settled as real, not a tier-1 or BLAS artifact.** Required levels 1-3 of the existing suite also still agree on this run — no regression from the workflow edit. See ADR-196: the formula gap itself is OPEN, characterized with a named next hypothesis, not resolved this session. |
| 2026-08-18 | 4 part A | PR #203 review [P1-1]: does the SAME independent fit `score_reml_point` uses also reproduce `mgcv`'s deviance at every point — the committed comparison that licenses "the fit is correct," previously cited only in session prose with no producer in the repo | added `deviance_reml_point`/`compare_reml_deviance` to `gam_reml_conformance.py`, declared `deviance` as a second `ComparedQuantity` on `REML_SCORE_CLAIM`; re-ran the probe (unchanged) and the now-two-quantity comparator | `python_deviance - r_deviance` per point, tolerance 1e-6 | n/a (deviance had no committed comparison before this fix) | **tier 1: `agrees=True` on all 3 points**, diffs -8.06e-12/-3.88e-12/-8.20e-12 (all `(sp)`: `(1,1)`/`(5,0.2)`/`(0.5,8)` respectively) — float round-trip noise | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS ("the fit itself matches mgcv, only the score criterion diverges") — confirmed below |
| 2026-08-18 | 4 part A | same hypothesis, re-measured on the authoritative oracle — the first tier-3 run for BOTH declared quantities on `REML_SCORE_CLAIM` together | none — same two producers (`gam_reml.reml_score_general`, `gam_reml_conformance.deviance_reml_point`, `scripts/gam_reml_probe.R`), dispatched via CI `workflow_dispatch` on `a517800`; comparison step prints both tables to stdout | both metrics, read directly from job-log stdout via `get_job_logs` | tier-1 readings above (both rows) | **IDENTICAL to tier 1 at every printed digit, both quantities** — score: same three residuals (-0.741273, 0.000934569, 0.742208), `agrees=False` on all 3 pairs; deviance: `agrees=True` on all 3 points, diffs -8.06e-12/-3.98e-12/-8.14e-12 (same order as tier 1) | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32090689399](https://github.com/jonathancrawford05/polaris-re/actions/runs/32090689399), R job 34s + compare job 25s | **CONFIRMED (parity) on BOTH `reml_score_pairwise_diff` and `deviance` — the two quantities `REML_SCORE_CLAIM` declares.** `deviance` agreeing at tier 3 is what settles "the fit is correct" as a committed, re-runnable claim rather than an ad-hoc session observation (PR #203 review [P1-1], closed). Required levels 1-3 of the existing suite also still agree on this run — no regression from the workflow edit. See ADR-196 (also corrected same-day, review [P1-3]): the formula gap itself is OPEN, characterized with a corrected next hypothesis, not resolved this session. |
| 2026-08-18 | 4 part A | RESOLUTION (maintainer supplied Wood 2011 directly): does adding the penalized-deviance term `Dp = D(beta_hat) + beta_hat^T S beta_hat` (Wood 2011 §2 eq. 4 — a term the score's first generalization omitted entirely, using plain deviance instead) close the pairwise-score-difference gap this ledger's prior two rows measured | edited `gam_reml.reml_score_general` to compute `penalized_deviance = deviance + coef @ penalty @ coef` and use it in place of plain deviance; no other term changed; re-ran the same probe (`scripts/gam_reml_probe.R`, unchanged) and comparator | `python_diff - r_diff` (residual) per pair, tolerance 1e-6 — same metric, same tolerance, same fixture as the original characterization | the DISAGREEING readings above (rows 1-2 of this slice) | **CLOSED to float round-trip precision, all 3 pairs** — `(1,1)-(5,0.2)`: residual -1.90e-12 (was -0.741273); `(1,1)-(0.5,8)`: residual 8.42e-13 (was 0.000935); `(5,0.2)-(0.5,8)`: residual 2.74e-12 (was 0.742208). `agrees=True` on all 3 | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS ("the missing beta_hat^T S beta_hat term is the entire gap") — **CONFIRMED**, tier-3 re-measurement below. §3.1 of the same paper (the multi-penalty log-determinant numerical-stability machinery this ADR's original next-hypothesis pointed at) turned out to be inapplicable to this fixture for a well-grounded reason — its two blocks have disjoint column supports, so no cross-block "zero leakage" is possible — not merely coincidentally matching at two points, closing the [P1-3] question the review round raised. |
| 2026-08-18 | 4 part A | same hypothesis, re-measured on the authoritative oracle (a formula fix, not a magnitude question — earns tier 3 regardless of how large the closed gap was, ADR-190 decision 5) | none — same fixed producer, dispatched via CI `workflow_dispatch` on `1ae300d`; comparison step prints both tables to stdout as before | both metrics, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — same three near-zero residuals (-1.90e-12, 8.42e-13, 2.74e-12), `agrees=True` on all 3 pairs; deviance unchanged and still `agrees=True` on all 3 points | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32142352655](https://github.com/jonathancrawford05/polaris-re/actions/runs/32142352655), R job ~28s + compare job ~30s | **CONFIRMED (parity) — RESOLVED, not merely characterized.** `reml_score_pairwise_diff` and `deviance` — every quantity `REML_SCORE_CLAIM` declares — are both INDEPENDENT and both agree. Required levels 1-3 of the existing ten-cell suite also still agree — no regression from the fix. Slice 4 part A is DONE (ADR-196 resolution). Whether the SAME missing term affects the already-shipped `experience_gam_penalized.reml_score` is explicitly NOT settled by this row — scoped as `docs/WORK_ORDER_reml_penalized_deviance_production_check.md`, the epic's next session, ahead of slice 4 part B. |
| 2026-08-18 | work order (production REML check) | §3.1: does `experience_gam_penalized.reml_score`'s (production, `INDEPENDENT`) score, evaluated at each free-sp cell's own already-fitted `(design, coef, penalty)`, collapse its offset-adjusted residual against `mgcv`'s own `m$gcv.ubre` (`INDEPENDENT`) when re-scored with the corrected (Dp-based) criterion — same offset-removal convention as ADR-189 amendment 1 (`≈ -l_sat/gamma`) | added `gam_reml_production_check.corrected_reml_score`/`measure_production_score_gap` (a one-line call into the already-verified `gam_reml.reml_score_general(poisson_log())`, not a new formula) and `scripts/reml_production_check_probe.py`; ran against the committed exchange + `python_reference.json` + a locally-generated `mgcv_reference.json` | offset-adjusted residual (`gap + l_sat/gamma`) per free-sp cell, current vs corrected | current: -1.836194 / -2.497827 / -1.658464 (`l2-free-sp` / `-factors` / `-kb`) | **residual does NOT collapse — roughly DOUBLES**: corrected -3.739483 / -4.649192 / -3.786146. Not a refutation: each side selects a DIFFERENT lambda at a free-sp cell, so this comparison mixes any formula gap with the point mismatch (named in the work order itself; §3.2 below is the matched-point-free measurement built to separate the two) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed identical below. Both quantities `INDEPENDENT` (`PRODUCTION_REML_CHECK_CLAIM`, `gam_reml_production_check.py`) — neither producer's signature accepts mgcv's own score/coef. |
| 2026-08-18 | work order (production REML check) | same measurement, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison, ADR-193) | none — same producers, dispatched via CI `workflow_dispatch` on `170bd76`; new diagnostic step prints its report to stdout from the start | same metric, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — same three offset-adjusted residuals, current and corrected | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32181109927](https://github.com/jonathancrawford05/polaris-re/actions/runs/32181109927), R job ~33s + compare job ~29s | **CONFIRMED (parity) on `reml_score (current)` and `reml_score (corrected)` — both agree with tier 1.** Required levels 1-3 of the existing ten-cell suite also still agree on this run. See ADR-197: §3.1 alone is NOT the decisive measurement (see §3.2 below); it is a real, honestly-reported non-collapse. |
| 2026-08-18 | work order (production REML check) | §3.2, REGISTERED PREDICTION (per the work order's own Anchor-9-style discipline, before running): if the missing term is a real bug, re-scoring `select_lambdas_reml`'s SAME 2-D grid search with the corrected criterion should select a point measurably CLOSER to mgcv's own free-sp selection than the current shipped selection — landing on the same point, or not closer, is an equally valid finding | added `select_lambdas_corrected` — a diagnostic REPLICA of `select_lambdas_reml`'s exact coarse+refine grid search (same bounds/rejection rule), scored with the corrected criterion; `select_lambdas_reml` itself not called, imported, or modified | log10 distance from Python's selected `(λ_age, λ_year)` to mgcv's own free-sp selection, current vs corrected selection, per cell | current: 0.3149 / 0.1870 / 0.4559 (`l2-free-sp` / `-factors` / `-kb`) | **PREDICTION HELD on all 3 cells**: corrected 0.0663 / 0.1097 / 0.1248 — closer on every cell, not just on average. `l2-free-sp`'s move (λ_age 3162.28→5623.41, λ_year unchanged) independently reproduces the exact grid-step the maintainer's own local patch-and-refit experiment found (ADR-196 resolution, work order §2) — a second, structurally different confirmation of that number | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed identical below. This is the decisive measurement (work order §6: "the harder question"). |
| 2026-08-18 | work order (production REML check) | same measurement, re-measured on the authoritative oracle | none — same replica, same CI dispatch (`170bd76`, run 32181109927) as the §3.1 tier-3 row above | same metric, read directly from job-log stdout | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — same log10 distances, same 3-of-3 "closer" verdict, same grid-step move on `l2-free-sp` | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32181109927](https://github.com/jonathancrawford05/polaris-re/actions/runs/32181109927) | **CONFIRMED (parity) — the registered prediction is a settled result, not tier-1-only.** See ADR-197 decision 2: recommends fixing `experience_gam_penalized.reml_score`, re-baseline maintainer-gated. |
| 2026-08-19 | work order (production REML check), PR #204 review [P1] | §3.2 point 2, closed: does the corrected selection also land closer to mgcv on `edf_total`/`edf_tensor`/`edf_factors`, not only `(λ_age, λ_year)` — the original PR only reported lambda | `select_lambdas_corrected` now does one extra fit AT its own selected point (mirrors `fit_reml` refitting at the selected point after `select_lambdas_reml`'s own sweep) and returns `edf_total`/`edf_tensor`/`edf_factors` alongside the selection; `current`/`mgcv` EDF read straight off the already-committed `python_reference.json`/`mgcv_reference.json`, no new fit needed for either | `|edf_total - mgcv edf_total|`, current vs corrected, per cell (all three columns `INDEPENDENT` pairwise, same ADR-193 mechanical test as the λ-distance columns) | current: 0.6514 / 0.4806 / 0.8734 (`l2-free-sp` / `-factors` / `-kb`) | **CLOSER on all 3 cells, roughly an order of magnitude**: corrected 0.1014 / 0.0262 / 0.0109. `edf_tensor` moves identically (no factor block on `l2-free-sp`/`-kb`; `-factors`' `edf_factors` is a constant 1.0000 across current/corrected/mgcv — the single factor dummy is effectively unpenalized under every criterion, so it carries no signal either way). Agrees in direction and order of magnitude with the already-confirmed λ-distance result on the same 3 cells (rows above) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed identical below. Second, independent line of evidence for ADR-197 decision 2 (fix it; re-baseline maintainer-gated) — did not exist when decision 2 was written, does not change it. |
| 2026-08-19 | work order (production REML check), PR #204 review [P1] | same measurement, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison, ADR-193) | none — same producers, dispatched via CI `workflow_dispatch` on `138683b`; same diagnostic step (now emitting the EDF table) prints its report to stdout | same metric, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — same `edf_total`/`edf_tensor`/`edf_factors` for current, corrected and mgcv, all 3 cells | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8, same digest as the original ADR-197 measurement), run [32201059741](https://github.com/jonathancrawford05/polaris-re/actions/runs/32201059741), commit `138683b`, both jobs completed in ~62s | **CONFIRMED (parity) — settled, not tier-1-only.** See ADR-197 amendment (2026-08-19, PR #204 review). Also settled this run: the §3.2 null control (`select_lambdas_corrected(..., use_corrected_score=False)` reproduces the shipped `l2-free-sp` selection `(3162.2776601683795, 1000.0)` exactly, PR #204 review [P2]) — a unit test, not a CI-dispatched measurement, so it is not a separate ledger row, but it is what licenses reading this row's "closer" verdict as a statement about the score formula rather than a divergent replica. |
| 2026-08-18 | work order (production REML check) | §3.3: does the corrected score change `smoothing_uncertainty`'s finite-difference Hessian/eigenvalues/Kass-Steffey-correction magnitude at the ALREADY-SELECTED (not re-selected) `(lambda_age, lambda_year)` | added `score_shape_diagnostic` — a diagnostic REPLICA of `smoothing_uncertainty`'s exact 9-point central-difference construction, fitting once and scoring both ways per point so both Hessians share bit-identical coefficients; `smoothing_uncertainty` itself not called or modified | Hessian eigenvalues and resulting inflation ratio (`mean(diag(Vb+correction))/mean(diag(Vb))`), current vs corrected, per cell | current eigenvalues [0.4187,0.6711]/[0.5212,0.7925]/[0.2771,0.4133], current inflation 1.1109x/1.1591x/1.2139x (reproduces ADR-189 amendment 1's own published numbers exactly — an internal cross-check that this replica is faithful) | **shifts materially but does not close the level-4 gap**: corrected eigenvalues [0.2940,0.4977]/[0.3590,0.7306]/[0.2289,0.3915] (~25-40% relative shift), corrected inflation 1.1538x/1.1851x/1.2418x — a small move toward mgcv's 1.7392x/1.4863x/1.8670x, far short of the 3.2-4.1x gap ADR-190 already characterized via a separate, unrelated derivation | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS ("this bug materially explains the level-4 BLOCKER") — **REFUTED as a material contributor**, confirmed identical below; ADR-190's `dw/drho` diagnosis stands unaffected. |
| 2026-08-18 | work order (production REML check) | same measurement, re-measured on the authoritative oracle | none — same replica, same CI dispatch (`170bd76`, run 32181109927) | same metrics, read directly from job-log stdout | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — same eigenvalues, same inflation ratios, `n_floored` 0 everywhere both formulas | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32181109927](https://github.com/jonathancrawford05/polaris-re/actions/runs/32181109927) | **CONFIRMED — settled.** §3.3 is characterized: the bug's effect on level 4 is real but small, not the explanation for the standing BLOCKER. See ADR-197. |
| 2026-08-19 | work order (production REML check) — RESOLUTION | maintainer-authorized: fix `experience_gam_penalized.reml_score` for real (not a diagnostic replica) the same way ADR-196 fixed `gam_reml.reml_score_general`, re-baseline `python_reference.json` via `export_mgcv_case.py`, and re-run the existing ten-cell conformance suite end to end against the fixed module | one-line fix (`penalized_deviance = deviance + coef @ penalty @ coef`, used in place of plain `deviance`) applied directly to `experience_gam_penalized.reml_score`; `python_reference.json` regenerated via `write_python_reference` (the test's own regeneration path, not hand-edited); `scripts/mgcv_conformance.R` + `scripts/compare_mgcv_conformance.py` re-run against the SAME committed exchange, unmodified | `max_abs_log10_sp_diff`/`abs_edf_total_diff_free_sp` (levels 2), `max_abs_log10_sp_diff_gamma`/`abs_edf_total_diff_gamma` (level 5), `rel_unconditional_inflation_diff` (level 4) — all already-INDEPENDENT conformance metrics (`VERIFICATION_STANDARD.md` §5), now read against the FIXED module instead of the buggy one | before (buggy, same numbers as the original ADR-197 §3.1/§3.2/[P1] tables): `l2-free-sp`/`-factors`/`-kb` `max_abs_log10_sp_diff` 0.3145/0.1709/0.4322, `abs_edf_total_diff_free_sp` 0.6514/-0.4806/0.8733; `l5-gamma` `max_abs_log10_sp_diff_gamma` 0.6724 (FAIL), `abs_edf_total_diff_gamma` 1.1270 (FAIL); levels: `1 AGREES 2 AGREES 3 AGREES 4 DISAGREES 5 DISAGREES` | **after (fixed): closer to `mgcv` on every free-`sp` metric, matching §3.2's prediction to the printed digit** — `max_abs_log10_sp_diff` 0.0645/0.0791/0.1048, `abs_edf_total_diff_free_sp` 0.1013/-0.0262/0.0109; `l5-gamma` now **PASSES both metrics**: `max_abs_log10_sp_diff_gamma` 0.0776, `abs_edf_total_diff_gamma` -0.0024. Levels: `1 AGREES 2 AGREES 3 AGREES 4 DISAGREES (unchanged, ADR-190's separate dw/drho gap) 5 AGREES`. Required levels 1-3 unchanged (still AGREE) — no regression | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS ("the real fix reproduces §3.2's predicted improvement and does not regress levels 1-3") — **CONFIRMED**, and level 5 moved from DISAGREES to AGREES, an improvement beyond what §3.2 measured (which covered only the three plain free-`sp` cells). See ADR-197's 2026-08-19 resolution amendment. |
| 2026-08-19 | work order (production REML check) — RESOLUTION | same measurement, re-measured on the authoritative oracle | none — same fixed module, same regenerated reference, dispatched via CI `workflow_dispatch` on `ce0b9f1` | same metrics, read directly from job-log stdout | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — `max_abs_log10_sp_diff` 0.0645/0.0791/0.1048, `abs_edf_total_diff_free_sp` 0.1013/-0.0262/0.0109, `l5-gamma` `max_abs_log10_sp_diff_gamma` 0.0776 (PASS)/`abs_edf_total_diff_gamma` -0.0024 (PASS), `rel_unconditional_inflation_diff` still FAILs on `l2-free-sp`/`l2-free-sp-kb` (-0.322/-0.334, level 4 unaffected); levels `1 AGREES 2 AGREES 3 AGREES 4 DISAGREES 5 AGREES` | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8, same digest as every prior measurement in this epic), run [32204739991](https://github.com/jonathancrawford05/polaris-re/actions/runs/32204739991), both jobs completed in ~63s | **CONFIRMED — settled.** Required levels 1-3 still AGREE (no regression); level 5 moves from DISAGREES (pre-fix) to AGREES. Level 4 unchanged in kind (ADR-190's separate `dw/drho` gap). See ADR-197's 2026-08-19 resolution amendment. |
| 2026-08-22 | 4 part B (ADR-199) | ADR-198's registered decisive test: does a continuous (quasi-Newton) search over `log10(lambda)` — `gam_reml_optimize.select_lambdas_continuous`, built on the already-verified `gam_fit.penalized_irls_general`/`gam_reml.reml_score_general`, never reading mgcv's own selection — collapse `max_abs_log10_sp_diff` toward its own convergence tolerance rather than leaving it near 0.1, on the same four free-sp cells ADR-198 measured post-fix | wrote `gam_reml_optimize.py` (the search), `gam_reml_optimize_conformance.py` (`CONTINUOUS_LAMBDA_CLAIM`), `scripts/reml_continuous_optimizer_probe.py`; ran the probe against the committed exchange + `python_reference.json` (grid's own selection, read not recomputed) + a locally-generated `mgcv_reference.json` | `max_abs_log10_sp_diff` (continuous search) vs mgcv's own free-sp selection, per cell, against the grid's own value from the same reference | grid (ADR-198, post-fix): `l2-free-sp` 0.0645, `l2-free-sp-factors` 0.0791, `l2-free-sp-kb` 0.1048, `l5-gamma` 0.0776 | **collapses on every cell, 2-3 orders of magnitude**: `l2-free-sp` 5.002e-04, `l2-free-sp-factors` 4.393e-05, `l2-free-sp-kb` 5.429e-04, `l5-gamma` 7.283e-04 — all four `converged=True` (SciPy L-BFGS-B, `gtol=1e-8`, 27-48 function evaluations), landing at the search's own convergence floor rather than anywhere near 0.1. `edf_total` diffs also shrink (order 1e-3-1e-4, down from the grid's already-small 0.10/-0.03/0.01) | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — ADR-198's prediction HOLDS, decisively, at tier 1; confirmed identical below. |
| 2026-08-22 | 4 part B (ADR-199) | same measurement, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison whose effect size — 2-3 orders of magnitude — earns the tier-3 re-measurement per the routine's no-magnitude-carve-out rule, ADR-190 decision 5, regardless of how large the tier-1 finding was) | none — same producer (`gam_reml_optimize.select_lambdas_continuous`), same probe, dispatched via CI `workflow_dispatch` on `601b73f`; comparison step prints its report to stdout from the start | same metric, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL IN VERDICT to tier 1, same order of magnitude, last-bit differences consistent with the different mgcv release/BLAS the routine's tier discipline predicts** — `l2-free-sp` 6.904e-04, `l2-free-sp-factors` 5.084e-05, `l2-free-sp-kb` 1.656e-04, `l5-gamma` 9.764e-04, all four `converged=True` | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32544930172](https://github.com/jonathancrawford05/polaris-re/actions/runs/32544930172), both jobs completed in ~68s | **CONFIRMED (parity) — settled, not tier-1-only.** `max_abs_log10_sp_diff` (continuous search) and `edf_total` (continuous search) — every quantity `CONTINUOUS_LAMBDA_CLAIM` declares — are INDEPENDENT, so this is genuine parity evidence about the search, not a harness check. Required levels 1-3 of the existing ten-cell suite also still agree on this run — no regression from the workflow edit. See ADR-199. |
| 2026-08-22 | 5 (the MI term) | Slice 5's first hypothesis, derived by direct R probe before being written into `gam_term_extract.R` (Anchor 8 — not guessed): a numeric-`by` `cr` smooth (`s(AttdAge, by=StudyYear_C)`, the MI term's own basis) has `smoothCon(..., absorb.cons=TRUE)$C` with **zero rows** — mgcv absorbs NO identifiability constraint on it — so `build_python_cr_term`'s design for a by-term should be the UNCONSTRAINED k-column `cr_basis` output with each row scaled by `by` (`gam_basis_cr.by_scale_design`, new), penalty `S` untouched by the scaling and left unconstrained | extended `gam_term_extract.R`'s `extract_smooth_one` with a `with_by` branch (`s(x, by=z, ...)`, `z` drawn after `x`/`y` so no other case's RNG stream moves) and one new case `mi-term-attdage-by-k13` (target AttdAge k=13 knots); added `gam_basis_cr.by_scale_design`; extended `build_python_cr_term` to take an optional `by` array and skip `absorb_sum_to_zero_constraint` when it is set; extended the CI report's `smooth_cases` dict and the test's `_SMOOTH_CASES` dict to cover the new case | `max_abs_design_diff`, `max_abs_s_diff`, `rank_diff`, index-range agreement, knots agreement (recipe check only) | n/a (first measurement) | **agrees** — `max_X_diff=2.176e-14`, `max_S_diff=3.775e-15` (float round-trip noise, same order as slice 2's other 5 cases), `rank_diff=(0,)` (rank 11 both sides, k=13), `index_range` `[0, 13)` both sides (unconstrained — no column dropped, unlike every non-`by` case's `[0, 12)`), `knots_agree=True`. The R-side internal guard (`smoothCon()` vs `lpmatrix`/`m$smooth[[1]]`, ADR-191) also passed on the by-case at tier 1 — its column-name pattern `s(x):z.N` still matches the existing `^s\\(x\\)` grep | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS ("mgcv drops the sum-to-zero constraint entirely for a numeric-`by` smooth, rather than constraining it some other way") — confirmed by direct measurement before commit, tier-3 re-measurement below. `design_X`/`penalty_S`/`rank` are INDEPENDENT. (Originally shipped reusing `CR_BASIS_CLAIM`; corrected same day to a distinct `CR_BY_BASIS_CLAIM` after PR #206 review [P1] found that claim's published producer strings misdescribe the by-row — the classification and every number here are unaffected.) |
| 2026-08-22 | 5 (the MI term) | same hypothesis, re-measured on the authoritative oracle (a genuine INDEPENDENT comparison, ADR-193 — same class as slice 2's own tier-3 row) | none — same two producers (`gam_basis_cr` via `build_python_cr_term`, `gam_term_extract.R`'s `smoothCon` `with_by` branch), dispatched via CI `workflow_dispatch` on `08472b3`; the existing "Compare the per-term extraction against the Python side (slice 1 / 1b / 2)" step already prints its report to stdout (no new step needed — the CI script's own `smooth_cases` dict was extended alongside the test's) | same metrics, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **IDENTICAL to tier 1 at every printed digit** — `mi-term-attdage-by-k13`: `max_X_diff=2.176e-14`, `max_S_diff=3.775e-15`, `rank_diff=(0,)`, `index_range agrees=True`, `knots_agree=True` | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32571764900](https://github.com/jonathancrawford05/polaris-re/actions/runs/32571764900), R job ~40s + compare job ~35s | **CONFIRMED (parity) — settled, not tier-1-only.** `design_X`/`penalty_S`/`rank` — every quantity `CR_BY_BASIS_CLAIM` declares (see the tier-1 row's note on the same-day claim split) — are INDEPENDENT, so this is genuine Stage-A basis parity for the MI term's numeric-`by` construction, the same class of result as ADR-194's original `cr` basis. Required levels 1-3 of the existing ten-cell suite also still agree on this run (levels 1-3 gate step completed successfully) — no regression from the workflow/extractor edit. See ADR-200. |
| 2026-08-22 | level-4 prerequisite (ADR-201) | Wood (2011) sec 3.4 + Appendix D give `dbeta/drho` and `dw/drho` — the ingredient ADR-190 decision 2 named as missing ("it needs `dw/drho`, which nothing in the fitter currently computes"). Hypothesis: an analytic implementation reproduces `mgcv`'s own behaviour, i.e. it is the h -> 0 limit of central differences of `mgcv` refits at perturbed `sp`. REGISTERED IN ADVANCE (work order sec 3): agreement on the canonical-link cells, and possible material DISAGREEMENT on `binomial-cloglog`, because Wood's derivation uses the OBSERVED (Newton) Hessian while `gam_fit.penalized_irls_general` uses FISHER weights | wrote `gam_derivatives.py` (`d_beta_d_rho`, `d_eta_d_rho`, `newton_alpha`, `newton_working_weights`, `dw_deta`, `dw_drho`), `gam_derivatives_conformance.py` (`DERIVATIVE_CLAIM`), `scripts/gam_deriv_probe.R` (mgcv refits at rho +/- h over 3 h values) and `scripts/gam_deriv_compare.py`; 26 R-free tests in the gating pytest job | `max_abs_deta_drho_diff`, `max_abs_dw_drho_diff` (both vs a central difference of mgcv's OWN eta / `m$weights`), `max_abs_eta_diff` (control), and the Richardson ratio at the truncation-dominated h pair | n/a (first measurement) | **the prediction HELD, then was RESOLVED.** With FISHER weights in the Hessian: `poisson-log` 6.6e-12, `binomial-logit` 2.7e-11, **`binomial-cloglog` 6.9e-06** — six orders worse, exactly as registered, with `max\|alpha-1\|` = 6.7e-16 / 0.0 / 4.3e-03 confirming alpha is identically 1 on the canonical links and not on cloglog. Switching to the OBSERVED-Hessian weights (`newton_working_weights`) collapses cloglog to **1.1e-11**, the same floor as the canonical cells. Final vs mgcv: `poisson-log` d(eta)/d(rho) 6.409e-11 / dw/d(rho) 1.725e-07; `binomial-logit` 5.437e-11 / 8.606e-11; `binomial-cloglog` 7.004e-11 / 5.834e-10; Richardson ratio **4.00** on all three | tier 1, R 4.3.3 / mgcv 1.9.1 (local apt) | HYPOTHESIS — confirmed below. Both quantities INDEPENDENT (`DERIVATIVE_CLAIM`): `analytic_derivatives`' signature takes only the shared recipe (design, penalties, y, prior weights, family spec, rho) and never the R side's eta/w or any difference of them. Compared on `d_eta_d_rho`, NEVER on `dbeta/drho` (Anchor 2 — mgcv reparameterises). **An earlier version of the probe reported only small h and a Richardson ratio of ~0.6**, which would have overstated the convergence evidence; the probe now brackets both the truncation- and round-off-dominated regimes and reports which is which. |
| 2026-08-22 | level-4 prerequisite (ADR-201) | same hypothesis, re-measured on the authoritative oracle (a genuine INDEPENDENT parity claim, ADR-193) | none — same two producers (`gam_derivatives`, `scripts/gam_deriv_probe.R`), dispatched via CI `workflow_dispatch` on `d5cc7b5`; the comparison step prints its report to stdout from the first version (standard practice since ADR-194) | same metrics, read directly from job-log stdout via `get_job_logs` | tier-1 reading above | **AGREES on all three cases, same order as tier 1** — `poisson-log` d(eta)/d(rho) 5.757e-11 / dw/d(rho) 1.725e-07; `binomial-logit` 5.511e-11 / 7.397e-11; `binomial-cloglog` 5.316e-11 / 5.834e-10; eta control 8.442e-13 / 7.772e-15 / 8.362e-12; **Richardson ratio 4.00 on all three**, confirming the analytic derivative is the h -> 0 limit of mgcv's own behaviour rather than merely near it at one step. Last-bit differences from tier 1 are consistent with the different mgcv release / BLAS the routine's tier discipline predicts | **tier 3**, R 4.6.1 / mgcv 1.9.4, oracle `sha256:0d54c192e23c62bdc614eb5b534e04482f6cf92290e76cacb7956022cd806fd8` (build 8), run [32586279901](https://github.com/jonathancrawford05/polaris-re/actions/runs/32586279901), both jobs green | **CONFIRMED (parity) — settled.** `d_eta_d_rho` and `dw_drho` — every quantity `DERIVATIVE_CLAIM` declares — are INDEPENDENT, so this is genuine parity evidence about the derivative. Required levels 1-3 of the ten-cell suite also still agree on this run; level 4 DISAGREES exactly as before. **This slice does NOT close level 4** and must not be read as doing so: Wood (2011) contains no unconditional-covariance formula (searched — zero occurrences of "unconditional"); it supplies the ingredient, and the assembly of `dw/drho` into `Vc` is Wood, Pya & Saefken (2016)'s own contribution, still outstanding. See ADR-201. |
